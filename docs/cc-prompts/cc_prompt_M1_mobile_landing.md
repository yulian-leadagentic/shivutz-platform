# CC prompt — Wave M1: Landing (מובייל 390px) — ממוקד, עצמאי

## הקשר קצר
דף הלנדינג הוא המסך הכי שבור במובייל. מהצילום (`screens-export/mobile/root.png`):
- שדה חיפוש דחוס: 3 dropdowns נערמים מעל שדה חתוך + כפתור "חפש" אפור צפוף.
- כרטיס המודעה המקודמת (boosted carousel): טקסט נדחס, אייקון דוחק את הקופי, שבירות מכוערות.
- hero + אריחי קטגוריות: מרווחים לחוצים, כותרות נחתכות.

**המשימה כאן: להפוך את הלנדינג לעובד ב-390px.** לא לגעת בלוגיקת החיפוש (LLM re-rank), רק ב-layout/reflow.

---

## ⭐ STANDING RULE
Mobile-first RTL. כל קריטריון קבלה חייב לעבור ב-**390px וגם דסקטופ**. יעדי מגע ≥44px. אימות מול `screens-export/mobile/root.png` + `screens-export/desktop/root.png` אחרי deploy. `git tag pre-m1-mobile-landing` לפני העבודה. דווח before/after לשני ה-viewports.

---

## M1.1 — Search hero למובייל
**דסקטופ (לשמר/לוודא):** שדה smart יחיד + כפתור Primary "חפש" + chips; 3 הפילטרים מאחורי toggle מקופל "סינון מתקדם" (לא בשורה העליונה).

**מובייל (390px) — חובה:**
- Hero = **שדה smart אחד מלא-רוחב** + כפתור Primary "חפש" **מתחתיו**, מלא-רוחב, גובה ≥44px. אף פעם לא dropdowns נדחסים מעל שדה חתוך.
- "סינון מתקדם" מקופל כברירת מחדל; בפתיחה — הפילטרים נערמים אנכית מלא-רוחב.
- Placeholder לא נחתך; הכפתור לא חופף לשדה.
- הפילטור עדיין עובד ונשמר ב-URL; דירוג ה-LLM לא נגע.

## M1.2 — כרטיס המודעה המקודמת (boosted carousel)
- reflow: הטקסט נשבר נקי, האייקון לא דוחק את הקופי, גובה הכרטיס מסתגל לתוכן.
- תגית "מקודם" נשארת גלויה.
- אין overflow אופקי / חיתוך ב-390px.

## M1.3 — Hero + אריחי קטגוריות
- אריחי קטגוריה: 2×2 נושמים, כותרות לא נחתכות, מרווחים תקינים (בסיס 4: 8/12/16).
- כותרת ה-hero + תת-כותרת קריאות, בלי חיתוך.
- ריווח אנכי סביר בין hero → חיפוש → אריחים (לא לחוץ, לא ריק-מדי).

---

## Primary button (G1 — נעול)
כל כפתור Primary/CTA בלנדינג (כולל "חפש") = **כתום חי `#F5821F` + טקסט כהה `#111827`** (AA ~6:1); hover = `#a5530b` (brand-800). disabled = מובחן (מעומעם), לא זהה לפעיל. token-driven — בלי hex per-page.

---

## Acceptance (סיכום — חייב לעבור בשני ה-viewports)
- [ ] מובייל: שדה חיפוש יחיד מלא-רוחב + כפתור "חפש" מתחתיו ≥44px; אין dropdowns מעל שדה חתוך.
- [ ] "סינון מתקדם" מקופל; נפתח לאנכי מלא-רוחב; פילטור עובד + נשמר ב-URL.
- [ ] כרטיס מקודם: טקסט נשבר נקי, אין חיתוך/overflow, תגית "מקודם" גלויה.
- [ ] אריחי קטגוריה 2×2 נושמים, כותרות שלמות.
- [ ] כפתור "חפש" = כתום חי #F5821F + טקסט כהה; disabled מובחן.
- [ ] דסקטופ לא נסוג (single field + collapsed filters נשמר).

## Guardrails
`git tag pre-m1-mobile-landing`; layout/reflow בלבד — לא לגעת בלוגיקת חיפוש/LLM; DS tokens (בלי hex ישיר); אם ה-export עדיין מראה את העיצוב הישן (3 dropdowns) — לוודא ש-staging סיים deploy ולצלם מחדש לפני שקובעים "לא תוקן".
