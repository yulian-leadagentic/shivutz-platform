# R24 · סריקת מסכים — רשימת עבודה

## ⭐ `git tag pre-r24`. **ריצה רציפה. הפרונט בלבד.**

**עברתי ב-390×844 על שישה מסכים ציבוריים וקראתי את ה-DOM בכל אחד.**
**כל ממצא כאן הוא ציטוט מהעץ או מצילום, לא הסקה.**

⚠️ **מה שלא נבדק:** מסכים מאחורי לוגאין — דשבורד קבלן, תאגיד, ספק, מסכי ניהול. **אין לי חשבון.** ⚠️ **תן לי חשבון בדיקה ואסרוק גם אותם.**

⚠️ **R23 עדיין בתוקף.** שבעת הממצאים של דף הבית שם. **הרץ אותו קודם.** כאן רק מה שהוא לא מכסה.

---

# §1 · 🔴 הכפילות אינה של דף הבית — היא של הקומפוננטה

**R23 §1 תיאר אותה בדף הבית. היא זהה ב-`/marketplace`:**

```
region "שירותים ממומנים" [ref_29]
   heading "הסמכות משרד העבודה"
   heading "הובלת עובדים לאתרים"
   heading "ייעוץ משפטי לתאגידים ולעובדים"

heading "הסמכות משרד העבודה"        [ref_45]   ← שוב
heading "הובלת עובדים לאתרים"       [ref_50]   ← שוב
heading "ייעוץ משפטי לתאגידים..."   [ref_55]   ← שוב
```

🔴 **אותה קומפוננטה, שני מסכים, אותו באג.** **תקן פעם אחת ב-`MarketplaceSponsors.tsx` ואמת בשניהם.**

---

# §2 · 🔴 כל מודעות הזרע ב-`/marketplace` מפנות ל-`buildup.co.il`

```
link "קבל הצעה" [ref_27]  href="https://buildup.co.il/insurance"
link "לרישום"   [ref_34]  href="https://buildup.co.il/courses"
link "לפרטים"   [ref_39]  href="https://buildup.co.il/transport"
link "לייעוץ"   [ref_44]  href="https://buildup.co.il/legal"
```

**המוצר הוא `tagidai.com`. הדומיין הנטוש הוא `buildupai.net`. `buildup.co.il` הוא דומיין שלישי.**

> ⚠️ **אלה שורות זרע להדגמה — אבל הן מוגשות עכשיו לכל מבקר, ואם הן יגיעו לפרודקשן האתר מפנה תנועה לדומיין שאינו שלנו.**

**Do: הדומיין שייך ל-Yulian, מותג ישן, ה-URL עדיין פעיל (21.09). הקישורים עובדים — אין תקלה.** ⚠️ **אבל בהשקה מודעות ההדגמה מפרסמות את המותג הישן. Do: החלף ל-`tagidai.com` או השבת את השורות, מסומן `לאישור Yulian`. לא דחוף.**

---

# §3 · 🔴 `/how-it-works` מבטיח משהו ש-R13 ביטל

**שלושה משפטים בעמוד:**

```
ref_13  "אין טופס למלא, אין הרשמה — כל מה שצריך זו שאלה אחת"
ref_34  "המנוי כולל מספר חשיפות בחודש — הגלישה במודעות עצמה תמיד חינם"
ref_60  "הצפייה חופשית לגמרי — כמו שאר המודעות בפלטפורמה"
```

**אבל לפי R13: אנונימי רואה שירותים נלווים בלבד. מודעות עובדים ודיור — מאחורי לוגאין.**

> 🔴 **מבקר קורא ״אין הרשמה״, מחפש עובדים, ומקבל כלום.**
> **זה לא באג טכני — זו הבטחה שבורה בעמוד שכל תפקידו להסביר איך המוצר עובד.**

## Do

- **`ref_13`** → הבהר ש**החיפוש** חופשי, ו**מודעות עובדים ודיור** דורשות התחברות
- **`ref_34`** → ״הגלישה במודעות חינם״ נכון לקבלן מחובר. **הוסף את התנאי**
- **`ref_60`** → **נכון כמו שהוא לשירותים נלווים.** אל תשנה — רק ודא שברור שזה על שירותים נלווים
- ⚠️ **זו עריכת תוכן, לא קוד.** 🔴 **אל תמציא נוסח — הצע נוסח ב-`לאישור Yulian` והמשך הלאה**

---

# §4 · 🔴 מסכי המשפט מרונדרים בלי עיצוב

**צילום `/accessibility` ב-390:**

- **כותרות הסעיפים** — ״1. המחויבות שלנו״ · ״2. התאמות שבוצעו״ · ״2.1 ניווט והפעלה״ — **באותו גופן ובאותו משקל כמו גוף הטקסט.** בעץ הן `heading`. **ה-CSS לא מחיל כלום**
- **`<li>` בלי תבליט ובלי הזחה** — שלוש נקודות בסעיף 2.1 נקראות כמשפטים רצופים
- **אפס ריווח בין סעיפים** — קיר טקסט אחד

> 🔴 **וזה משפיע גם על `/terms` ועל `/privacy`** — אותו renderer.
> **ובעמוד שכל תפקידו להצהיר על נגישות, טקסט בלי היררכיה ורשימות בלי תבליטים הוא בדיוק מה שההצהרה מבטיחה שאין.**

## Do

- **עיצוב ל-`prose` של התוכן המשפטי:** `h2`/`h3` בגודל ומשקל נבדלים · `ul` עם `list-style` והזחה · `margin-block` בין סעיפים
- ⚠️ **אל תיגע בתוכן ב-DB.** זו שכבת התצוגה בלבד
- **אמת בשלושת העמודים:** `/accessibility` · `/terms` · `/privacy`. שלושה צילומים

## 4b · תאריך העדכון לא עודכן

**״עודכן לאחרונה: 12.9.2026״** — **וההצהרה עודכנה ב-R14 §2 ב-20-21.09** (בלוק רכז הנגישות).

🔴 **במסמך משפטי תאריך עדכון שגוי אינו קוסמטיקה.** **Do: שדה התאריך נגזר מ-`updated_at` של השורה, לא מוקלד.** אם כבר כך — **דווח למה הוא לא זז.**

✅ **ומה שכן עובד:** בלוק רכז הנגישות מלא — שם, `tel:0525278625`, `mailto:accessibility@tagidai.com`. **מיגרציה `088` נחתה. אל תיגע.**

---

# §5 · מסכי ההרשמה — פערים קטנים ואחידות

## 5a · מחוון שלבים רק באחד

```
/register/contractor   "שלב 1 מתוך 3"   ✅
/register/provider     — אין
```

**Do:** מחוון זהה בשלושת מסלולי ההרשמה. ⚠️ **אם לספק יש פחות שלבים — הצג את המספר הנכון, אל תעתיק ״3״.**

## 5b · 🔴 אין קישור לתנאי שימוש ולמדיניות פרטיות באף מסך הרשמה

**שלושת מסכי ההרשמה מציגים שדה טלפון וכפתור שליחה. אין בהם `/terms` ואין `/privacy`.**
**גם `/how-it-works` בלי פוטר.** **רק דף הבית מציג אותם.**

> 🔴 **המשתמש מוסר מספר טלפון בלי שהוצג לו למה הוא מסכים.** **זו נקודת איסוף המידע הראשונה במוצר.**

**Do:** שורה מתחת לכפתור — `בהרשמה אני מאשר את תנאי השימוש ומדיניות הפרטיות`, שני קישורים אמיתיים.
⚠️ **בלי צ׳קבוקס חובה** — הוא מוריד המרה ואינו נדרש כשהטקסט צמוד לכפתור.

## 5c · `<button>` בתוך `<a>` ב-`/marketplace`

```
link [ref_7] href="/register/corporation"
  button "פרסם מודעה" [ref_8]
```

**HTML לא תקין.** התנהגות לא צפויה במקלדת ובקוראי מסך. **Do: אחד מהם, לא שניהם.**

---

# §6 · ✅ מה שעובד — אל תיגע

- **`/marketplace` — כרטיסי המודעות הם `<a href="/marketplace/{id}">` אמיתיים.** 🔴 **זו התבנית הנכונה. השתמש בה כדי לתקן את תוצאת החיפוש בדף הבית (R23 §4)** — הקוד הנכון כבר קיים בריפו
- **מסנני `/marketplace`** — קטגוריות, אזורים, מונה ״16 מודעות״. תקין
- **`/accessibility`** — התוכן ובלוק הרכז נכונים. רק העיצוב
- **`/how-it-works`** — המבנה והתוכן טובים. רק שלושת המשפטים ב-§3
- **`/register/*`** — נקיים ומינימליים

---

# §7 · מה שלא נסרק

```
דשבורד קבלן · דשבורד תאגיד · דשבורד ספק · מסכי הניהול ·
יצירת מודעה · מסך החשיפה · /login אחרי OTP
```

🔴 **אלה כנראה המסכים עם הכי הרבה תקלות, והם גם אלה שאיש לא ראה מאז R21.**
**Do: אל תסרוק אותם בעצמך בסבב הזה — תקן קודם את §1-§5.** ⚠️ **אבל דווח אם נתקלת בהם במשהו.**

---

# Acceptance

- [ ] 🔴 **כפילות — `read_page` ב-390 על `/` ועל `/marketplace`, כל כותרת פעם אחת.** שני עצים
- [ ] **ואותו דבר ב-1280.** שני עצים
- [ ] 🔴 **`buildup.co.il` — דווח בעלות לפני שינוי.** אל תתקן בלי תשובה
- [ ] **שלושת המשפטים ב-`/how-it-works` — נוסח מוצע, מסומן `לאישור Yulian`**
- [ ] 🔴 **`/accessibility` · `/terms` · `/privacy` — כותרות, תבליטים וריווח.** שלושה צילומים לפני/אחרי
- [ ] **תאריך העדכון נגזר מ-`updated_at`.** הדבק
- [ ] **מחוון שלבים בשלושת מסלולי ההרשמה.** שלושה צילומים
- [ ] 🔴 **קישורי תנאים ופרטיות בשלושת מסכי ההרשמה.** שלושה צילומים
- [ ] **`<button>` כבר לא בתוך `<a>`.** הדבק
- [ ] **תוצאת החיפוש בדף הבית — `<a href>` כמו ב-`/marketplace`.** עץ
- [ ] 🔴 **צילום מלא של כל שישה המסכים ב-390 אחרי הכל**
- [ ] `npm run build` · `npm test` · `--suite all` · `--suite matrix`
- [ ] 🔴 `git rev-list --left-right --count origin/staging...origin/pivot/v2` → **`0 0`**

## דווח

1. 🔴 **בעלות `buildup.co.il`**
2. **הנוסח המוצע ל-`/how-it-works`**
3. **צילומי שלושת המסכים המשפטיים, לפני ואחרי**
4. **למה תאריך העדכון לא זז**
5. `git diff -w --stat`

---

## Guardrails

`git tag pre-r24`. **סטייג׳ינג בלבד. שני הענפים.**

🔴 **הפרונט בלבד. אפס תשתית, אפס סביבה, אפס חיוב, אפס מיגרציות.**

🔴 **אל תמציא נוסח שיווקי ואל תמציא כתובות.** הצע וסמן `לאישור Yulian`, והמשך.

🔴 **תוכן משפטי ב-DB — אל תיגע. רק התצוגה.**

🔴 **הרץ R23 קודם.** הוא מכסה את דף הבית.

🔴 **אל תיגע** בחשיפה · במטריצת R13 · ב-R15 · ב-R16 · ב-`cardcom.py`.

🔴 **`git add` לפי שם קובץ. לעולם לא `-A`.** **וכולל `docs/cc-prompts/*.md`.**
