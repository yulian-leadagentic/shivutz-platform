# CC — פרומט הרצה אחד, מקצה לקצה · השקה 14.10.2026

> **זה הקובץ היחיד שאתה מריץ.** שמונת הקבצים `cc_prompt_L*.md` הם ה**איך**; הקובץ הזה הוא ה**סדר**, ה**כללים** וה**אימות**.
> **הרץ הכל ברצף. אל תעצור בין שלבים. כל הבדיקות בסוף.**

---

# §0 · איך להריץ את זה

```
1  קרא את הקובץ הזה במלואו.
2  לכל שלב: קרא את קובץ ה-L שלו, בצע את סעיפי ה-Do שלו.
3  🔴 אל תריץ את סעיף ה-Acceptance של אף קובץ L. הוא מוחלף על ידי §10 כאן.
4  קומיט אחרי כל שלב, עם תג. בלי לעצור לדיווח.
5  אחרי שלב 8 — הרץ את §10 במלואו, ודווח פעם אחת.
```

**דווח באמצע רק בשני מקרים:**
- **חסם אמיתי** — משהו לא קיים, סותר את הפרומט, או דורש הכרעה של Yulian.
- **נקודת העצירה של §6.** אחת. חובה.

---

# §1 · כללי ברזל

1. **ענף: `pivot/v2`.** `git rev-parse --abbrev-ref HEAD` ואמת. **`CLAUDE.md` אומר `staging` וזה לא נכון.** **production — רק באישור מפורש של Yulian.**
2. **אין `git add -A`.** CRLF הופך את `git status` ללא-אמין. stage לפי שם קובץ.
3. **`git tag pre-l<N>` לפני כל שלב, קומיט בסופו.** התגים הם מה שיאפשר bisect כשמשהו יישבר ב-§10 — **ובלעדיהם תעמוד מול 30 קבצים ששונו בלי לדעת מאיפה.**
4. **RTL · 390 קודם · טוקנים של ה-DS.**
5. **אל תרחיב.** כל קובץ L מגדיר גבול. מה שמחוצה לו — **דווח, אל תתקן.**
6. **מיגרציות: בדוק `ls db/migrations | tail` בכל פעם.** אל תסמוך על מספר שכתוב בפרומט.

---

# §2 · שלב 1 — L1 · חורי הרשאה

📄 `cc_prompt_L1_authz_holes.md` — סעיפים §1, §1b, §2

חמישה נתיבים מגישים היום מספרי טלפון ומסמכים לכל מי שמחזיק טוקן, ו-`/api/uploads` מגיש כל קובץ בלי טוקן בכלל.

**זה ראשון כי חמישה שלבים אחרים נשענים על עוזר ההרשאה שייווצר כאן.**

`git tag pre-l1` → בצע → קומיט `L1: entity-access guard + upload auth`

---

# §3 · שלב 2 — L2 · מודל הנראות

📄 `cc_prompt_L2_visibility.md` — §2, §3, §4, §4b, **§4c**, §5

> 🔴 **`cc_prompt_h12_visibility.md` §2 מבוטל.** שם כתוב *"אל תסגור את `/api/search`"* — **Yulian הכריע מאז שכן.** אם H12 לא רץ: **הרץ רק את §1 (הבאנר) ו-§3 (דאטת הזרע) שלו, ודלג על §2.**

**אל תשכח את §5** — `GET /ads/public/{ad_id}` מ-H11 הוא ציבורי, ובלי לסגור אותו כל השלב עקיף.

🔴 **§4c נוסף ב-10.09** — *"כל קבלן הרשום בפנקס הקבלנים רשאי לראות מודעות תאגידים"*. **המנגנון כבר קיים** (`contractors.py:259-287` מאשר אוטומטית על התאמה למרשם). **אל תוסיף תנאי חדש.** §4c מתקן שלושה פערים סביבו: נפילת `data.gov.il`, היעדר מסלול אימות-עצמי, וצוואר הבקבוק בתור האדמין.

`git tag pre-l2` → בצע → קומיט `L2: gate search behind auth + enforce contractor approval`

---

# §4 · שלב 3 — L3 · חיווי אמון

📄 `cc_prompt_L3_trust_signal.md` — §2, §3, §3b

**עודכן 10.09 לפי הכרעת Yulian:** בתוצאות מוצג **התג בלבד**. **`corp_name` לא נוסף ל-`_serialize_ad` כלל** — לא מותנה, לא `null`. השם ממשיך להגיע רק מ-`/contact-reveal`.

`git tag pre-l3` → בצע → קומיט `L3: corporation trust badge in results`

---

# §5 · שלב 4 — L4 · תמחור ומושבים

📄 `cc_prompt_L4_pricing_seats.md` — §1, §2, §3, §4, §5

**חייב לקדום את L5** — אי אפשר לבדוק גבייה על מחיר `NULL`.

**שתי נקודות לדיווח בסוף (לא לעצור עליהן):**
- הסתירה מ-§0 של הקובץ: **5 משתמשים כלולים מול 1 במודל הפיננסי**
- `450` ו-`650` הן הצעה, לא מקור. **סמן `-- לאישור Yulian` במיגרציה**

`git tag pre-l4` → בצע → קומיט `L4: contractor pricing + seat model`

---

# §6 · שלב 5 — L5 · גבייה אמיתית

📄 `cc_prompt_L5_billing.md` — §3, §4, §5, §6, §7, §8 **לפי הסדר הכתוב שם**

> ## 🔴 נקודת העצירה היחידה בכל ההרצה
>
> **אחרי §3 (איחוד דגלי הדמה) — לפני שאתה כותב שורה אחת של קוד חיוב:**
>
> 1. אמת ש-**`PAYMENT_FAKE_MODE=1` מוגדר בסביבת הסטייג'ינג ב-Railway**
> 2. **דווח ל-Yulian ועצור עד לאישור**
>
> **זו לא בדיקה — זו נעילת בטיחות.** `charge_token` (`cardcom.py:227-303`) **לא בודק היום שום דגל**, ולכן חיוב בודד תמיד פונה ל-Cardcom אמיתי. **בלי הנעילה הזאת, בדיקה בסטייג'ינג מחייבת כרטיס אמיתי — וזה בלתי הפיך ויוצא מהמערכת שלנו החוצה.**
>
> **כל שאר האימות נדחה ל-§10. זה לא.**

`git tag pre-l5` → §3 → **עצור ודווח** → אישור → §4–§8 → קומיט `L5: real subscription billing + renewal + invoice`

---

# §7 · שלב 6 — L6 · דפים משפטיים

📄 `cc_prompt_L6_legal_pages.md` — §1, §2, §3

**עצמאי.** אם יש חלון קצר לפני חג — זה המועמד.

**הערת `<!-- טיוטה. טרם עברה עורך דין. -->` חובה בראש `/terms` ו-`/privacy`.** **אל תסמן את הפריט כסגור.**

`git tag pre-l6` → בצע → קומיט `L6: terms, privacy, accessibility, contact`

---

# §8 · שלב 7 — L7 · רשומות חשיפה

📄 `cc_prompt_L7_reveal_records.md` — §0, §1, §2, §3

**§0 קודם:** `SHOW CREATE TABLE contact_reveals`. **אם `ad_id` לא נשמר — §1 חסום, דווח מיד ועבור לשלב 8.**

`git tag pre-l7` → בצע → קומיט `L7: reveal history for both sides`

---

# §9 · שלב 8 — L8 · ניטור ותשתית

📄 `cc_prompt_L8_ops.md` — §1, §2, §3, §4

⚠️ **§2 (CORS) — רשימת הדומיינים חייבת להיות נכונה.** רשימה שגויה **משביתה את האתר לגמרי** וזה נראה כמו תקלת שרת. **אם אתה לא בטוח מה הדומיינים — כתוב את הקוד, השאר `CORS_ALLOWED_ORIGINS` ריק (מצב מתירני כמו היום), ודווח.** אל תנחש.

⚠️ **§1 — אם אין חשבון Sentry / DSN:** בנה את החלופה מ-§1 (לוג JSON מובנה) ודווח. **אל תדלג על השלב.**

`git tag pre-l8` → בצע → קומיט `L8: monitoring, CORS allowlist, CI`

---

# §10 · האימות — עכשיו, בבת אחת

**כאן אתה עוצר, בודק הכל, ומדווח פעם אחת.**

## 10.1 · שער ראשון — הכל עולה

- [ ] `npm run build` עובר
- [ ] כל השירותים עולים. **אף שירות לא ב-crash loop**
- [ ] `[payment] mode=fake` בלוג. **אם `mode=real` — עצור מיד ודווח לפני כל בדיקה אחרת**

## 10.2 · המסלול המרכזי — מקצה לקצה, כמשתמש אמיתי

**חלון פרטי. בלי קיצורי דרך.**

- [ ] מבקר אנונימי → `/` → **לולאת הדמו רצה עם תווית `הדגמה`**
- [ ] הקלדה + "חפש" → **מסך התחברות**
- [ ] הרשמה כקבלן → **חוזר עם השאילתה והתוצאות** (H11)
- [ ] קבלן `pending` → **ספירות בלבד** · `contact-reveal` → **403**
- [ ] אישור באדמין → קבלן `approved` → **תוצאות מלאות עם תגי אמון**
- [ ] פתיחת מנוי (fake) → `active`
- [ ] חשיפה → פרטים + **שם התאגיד**
- [ ] **היסטוריית חשיפות** — הרשומה שם
- [ ] דשבורד תאגיד → **מסך החשיפות, בלי פרטי קבלן**

**צילום מכל שלב. 390 ודסקטופ.**

## 10.3 · בידוד — הבדיקות שאסור לדלג עליהן

- [ ] קבלן A → נתוני קבלן B (5 הנתיבים) → **403**
- [ ] תאגיד A → חשיפות של תאגיד B → **חסום**
- [ ] קבלן A → היסטוריה של קבלן B → **חסום**
- [ ] `POST /api/search` בלי טוקן → **401**
- [ ] `GET /api/ads/public/<id>` בלי טוקן → **חסום**
- [ ] URL של מסמך בלי טוקן → **חסום**
- [ ] אדמין → **עובר בכל החמישה**

## 10.4 · דליפות — grep על התשובות בפועל

- [ ] תשובת `/search` — **אין `corp_name`, אין `company_name`, אין טלפון, אין מייל**
- [ ] מסך התאגיד — **אין שום שדה מזהה של קבלן.** לא ב-JSON ולא ב-CSV
- [ ] **טוקן Cardcom לא מופיע בשום לוג ובשום תשובה**
- [ ] אירוע Sentry עם טלפון וטוקן בגוף הבקשה → **האירוע נקי**
- [ ] `.env.example` — **אפס ערכים אמיתיים**

## 10.5 · כסף

- [ ] `grep -rn "CARDCOM_SUBS_FAKE_MODE"` → **ריק**
- [ ] fake דלוק → **אפס פניות רשת ל-Cardcom.** הוכח מהלוג
- [ ] המחיר מגיע מ-`subscription_plans`. **בקשה עם סכום אחר — מתעלמים ממנו**
- [ ] 🔴 **webhook פעמיים → חיוב אחד, הארכה אחת**
- [ ] 🔴 **אצוות חידוש פעמיים ברצף → חיוב אחד**
- [ ] webhook בלי חתימה → **401**
- [ ] `invoice_number` נשמר

## 10.6 · מכסות — שאף בדיקה חסומה לא עולה כסף

- [ ] `SELECT COUNT(*) FROM contact_reveals` **לפני ואחרי** ניסיון חשיפה של קבלן `pending` → **זהה**
- [ ] אותו דבר **לפני ואחרי פתיחת מסך ההיסטוריה** → **זהה**

## 10.7 · מושבים

- [ ] **ספירת משתמשים פעילים לכל ישות, לפני ואחרי המיגרציה. אף ישות לא חצתה תקרה**
- [ ] קבלן עם 3 → מזמין רביעי → **עובר**
- [ ] קבלן עם 5 → מזמין שישי → **`402 seat_upgrade_required` עם מחיר**
- [ ] תאגיד `basic` עם 3 → מזמין רביעי → **`402 seat_limit`, כמו קודם**
- [ ] **אין כפתור רכישת מושב בשום מקום**

## 10.8 · דפים ותשתית

- [ ] `/terms` · `/privacy` · `/accessibility` · `/contact` — נטענים
- [ ] **אפס `<span>` מתים בפוטר**
- [ ] הערת "טיוטה" קיימת
- [ ] CORS: מקור מותר עובד · מקור זר נחסם · `Vary: Origin`
- [ ] **Webhooks של Cardcom ו-Vonage עדיין עובדים**
- [ ] CI ירוק · `npm test` רץ — **דווח מה עובר ומה נופל**

## 10.9 · רגרסיה על מה שכבר עבד

- [ ] פריסת השורות (H9) · מודעות המפרסם (H10) · מסלול ההתחברות (H11) · `--sticky-h`
- [ ] near-match · הצ'יפים · מצב ריק
- [ ] רישום תאגיד · העלאת מסמך · אישור באדמין
- [ ] לוגו · אווטאר · תמונות דיור — **נטענים** (SEC-2 שינה את נתיב הקבצים)

---

# §11 · הדיווח — פעם אחת, בסוף

1. **טבלה: שמונת השלבים · SHA · תגית · עבר/נכשל/חלקי**
2. **כל הצילומים**, מקובצים לפי §10
3. **תוצאות שלוש בדיקות האידמפוטנטיות** (webhook ×2, אצווה ×2) — מילה במילה
4. **תוצאות שתי בדיקות המכסה** — ארבעה מספרים
5. **טבלאות ספירת המשתמשים** לפני/אחרי
6. **פלט ה-grep של §10.4** — כל אחד
7. **רשימת דומייני CORS — לאישור Yulian**
8. **ההכרעות שממתינות ל-Yulian:**
   - מחירי `450` / `650`
   - סתירת 5 המשתמשים מול המודל
   - עורך דין לדפים המשפטיים
   - כל דבר שדילגת עליו — **ולמה**
9. `git diff -w --stat` מ-`pre-l1` ועד HEAD

---

# §12 · Guardrails — חלים על כל ההרצה

🔴 **`PAYMENT_FAKE_MODE=1` בסטייג'ינג לפני כל קוד חיוב.** נקודת העצירה של §6.

🔴 **אף חיוב אמיתי בסטייג'ינג. בשום שלב.**

🔴 **בלי אידמפוטנטיות אין פריסה** של webhook או אצווה.

🔴 **אף סוד אמיתי לא נכנס לריפו.**

🔴 **שם התאגיד לא נכנס לתוצאות. פרטי קבלן לא יוצאים לצד התאגיד.**

🔴 **בדיקות הרשאה לפני בדיקות מכסה.** ניסיון חסום לא עולה למשתמש כלום.

🔴 **אל תיגע** ב-`query_rewriter.py` · דירוג התוצאות · לולאת הדמו · מסלול J5 של Cardcom · `refund_transaction`.

**נדחה במפורש לאחרי ההשקה:** רכישת מושב · שירותים נלווים · WhatsApp נכנס · חיוב פרסום · Tenders · חיפושים שמורים · פיד פעילות.

---

> **אם משהו סותר את מה שכתוב כאן — עצור ודווח. אל תמציא פתרון שלישי.**
