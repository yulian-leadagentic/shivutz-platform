# R30 · סבב מלא — 27 מקטעים

## ⭐ `git tag pre-r30`. **ריצה רציפה מקצה לקצה. אל תעצור לאישור ויזואלי.**

## 🔴 גל 0 קודם לכל השאר — ראה בתחתית הקובץ. §25 · §26 · §27

**נאסף ב-22.09.2026 בזמן ש-R29 רץ.** **כל ממצא כאן נמדד — הוא נכנס לקובץ רק אחרי שראיתי אותו בקוד או במסך החי. אין כאן הסקה.**

---

# סדר ההרצה — ארבעה גלים

⚠️ **הסדר אינו שרירותי. גל 1 מכיל דברים ששבורים פונקציונלית ונראים למבקר.**

## גל 1 — שבור למשתמש

```
§13  כרטיסי ״פרסום חדש״ לא מגיבים — ?ad= בלי קורא
§7   ״הצטרף עכשיו״ לא גולל · ו-/how-it-works בלי CTA
§12a ה-0 שמרונדר בכל מודעה לא מרוהטת
§5   ״נקה״ שלא מנקה ב-/admin/ads
§14a ספק שירותים לא יכול לפתוח פנייה — ENUM חסר
```

## גל 2 — חוסם עבודה של האדמין

```
§17  ה-401 שמוצג כ״Cloudinary לא הוגדר״ · ורמזי המידות
§21  שלושה מסכי ניהול בלי כניסה מהתפריט
§20  ״מסלולי מנוי״ מסתיר את הספק
§18  ה-sticky שנדבק לרקע במודאל
§11  תוויות provider · ובדיקת ספק אמיתי
```

## גל 3 — פריסה ותצוגה

```
§22  שורת הצ׳יפים — להחזיר למכולת התוכן
§16  המסגרת השבורה סביב ״איך זה עובד?״
§24  side_rail — בנייה מחדש כעמודת גריד
§23  סינון מתקדם לפי ad_type
```

## גל 4 — תוכן, נתונים ותשתית

```
§15  ולידציית טלפון ומייל + autocomplete — סריקה רוחבית
§9   ולידציית האימייל בטפסי ההרשמה
§8   תאריך סיום ההטבה
§10  מקטע האימות ב-״איך זה עובד״
§19  שמות פריטי התפריט
§1   הטילדה בייבוא ה-PDF
§2   הנרמול במסלול האדמין
§3   כפילות השם בטבלת הארגונים
§6   מסך בדיקת ההודעות
§12b §4  סלוטי פרסום חדשים · מדידה ומחירון
```

🔴 **אם גל 4 לא נגמר — עצור, דווח מה נשאר, ואל תמחק אותו מהקובץ.**

---

# §1 · 🔴 הטילדה ב-`בע~מ` אינה תקלת נתונים — היא באג בייבוא

**Yulian תיקן ידנית ב-22.09 חמש שורות ב-`/admin/orgs`:**

```
אליעזר ענבי ניהול פרויקטים בע~מ   →  …בע"מ
א.א פסגת העבודה בע~מ              →  …בע"מ
מזרח הירקון בניה␣␣בע~מ            →  מזרח הירקון בניה בע"מ
בוני הנגבבע"מ                      →  בוני הנגב בע"מ
עליונים בניה ויזמות␣␣בע~מ         →  …בע"מ
```

✅ **אימתתי אחרי התיקון — כל 16 הארגונים נקיים.** **אל תיגע בנתונים.**

## השורש

**השמות נשאבים מ-PDF של רשות התאגידים** — `services/admin/app/routes/gov_corp_list.py:146` · `import_gov_pdf`.
**חילוץ הטקסט מה-PDF ממיר את הגרשיים `״` לטילדה `~`.**

> 🔴 **ולכן התיקון הידני מחזיק עד הייבוא הבא בלבד.** **חמש שורות היום, עשרים בייבוא הבא.**
> **`grep` על טיפול בטילדה בכל הקוד מחזיר אפס — אין שום נרמול בשום מקום במסלול.**

## Do

- 🔴 **נרמול בשלב הייבוא, לפני ה-`INSERT`** — `~` בין שתי אותיות עבריות הופך ל-`"`
- ⚠️ **רק בין אותיות עבריות.** **אל תמיר טילדה גורפת** — היא תו לגיטימי במקומות אחרים
- **ו-`join(...split())` באותו מקום**, לרווח הכפול. **שתי התקלות מגיעות מאותו PDF**
- 🔴 **דווח כמה שורות בייבוא האחרון היו נגועות.** **אם המספר גבוה — זה אומר שכל ייבוא עתידי היה מייצר אותן**

---

# §2 · הנרמול חסר במסלול השמירה של האדמין — פתוח מ-R26 §2

**הנרמול קיים ב-`corporations.py:284,286` וב-`contractors.py` (נחת ב-`8ec5dc3`).**
🔴 **`grep 'join(.*split())' services/admin/app/routes/*.py` מחזיר אפס.**

> ⚠️ **כלומר: משתמש שנרשם בעצמו מקבל שם מנורמל. אדמין ששומר את אותו שם — לא.**
> **שני מסלולי כתיבה לאותה עמודה, התנהגות שונה.**

**Do: אותה שורה, באותו מקום שבה היא יושבת ב-`corporations.py`.** **דווח באילו קבצים הוספת.**

---

# §3 · שם הארגון מופיע פעמיים בכל שורה בטבלת האדמין

`services/frontend/src/app/admin/orgs/page.tsx:273-277`

```jsx
<td className="py-3 px-4 sm:px-0 font-medium text-slate-900">
  {o.company_name}
  {o.company_name_he && (
    <span className="block text-xs text-slate-400">{o.company_name_he}</span>
  )}
</td>
```

**קראתי את ה-DOM החי. שתי השורות מציגות את אותו ערך בדיוק, בכל 16 השורות:**

```html
בוני הנגב בע"מ<span class="block text-xs text-slate-400">בוני הנגב בע"מ</span>
```

**הכוונה ברורה — שם לועזי למעלה, עברי מתחת באפור.** ⚠️ **אבל `company_name` מחזיק את השם העברי, אז השורה האפורה היא כפילות.**

## Do

- **בדוק מה `company_name` באמת מחזיק בשאילתה שמזינה את המסך.** **דווח**
- **אם שתי העמודות זהות — הצג שורה אחת.** **תנאי על שוויון, לא הסרה של השדה**
- ⚠️ **אל תמחק עמודה ואל תיגע בסכימה.** **שכבת התצוגה בלבד**

---

# §4 · הפערים התפעוליים של מערך הפרסום

⚠️ **אחרי ההשקה. לא חוסמים את 14.10, חוסמים את המחירון.**

## 4a · 🔴 ל-`/admin/sponsor-stats` אין אף קורא בפרונט

**המדידה בנויה במלואה — R6 עשה את זה, ואימתתי:**

```
useAdImpression.ts   IntersectionObserver · 50% נראות · שנייה רצופה
adEvents.ts          impression · ad_click · inquiry
POST /events         allow-list על event_type ועל מפתחות metadata
065_promo_events.sql
GET /admin/sponsor-stats   impressions · clicks · inquiries · חלון עד 90 יום
```

🔴 **`grep 'sponsor-stats'` בכל הפרונט מחזיר אפס.** **הנתונים נאספים ואיש אינו יכול לראות אותם.**
⚠️ **זו בדיוק התבנית של R21 — ״API בלי מסך״.** **שם זה נסגר עם `sponsor_slots`. כאן לא.**

**Do: מסך אדמין לסטטיסטיקות מפרסם.** **לכל מפרסם: חשיפות · קליקים · פניות, לחלון נבחר.** **זה גם הדוח שנשלח למפרסם.**

## 4b · אין דה-דופליקציה של פניות

**`promo_events` רושמת כל `inquiry` כשורה נפרדת. אין מושג של פנייה ייחודית.**
🔴 **ולכן אי אפשר לחייב ₪120 לליד — נספור את אותו קבלן פעמיים.**

**Do:** **״ליד מזוהה״ = פנייה עם טלפון תקף, שאותו קבלן לא פנה לאותו מפרסם ב-90 הימים שקדמו.**
⚠️ **המונה בדוח מציג את שני המספרים בנפרד — פניות גולמיות וייחודיות.** **אל תחליף את הגולמי**

## 4c · `sponsor_ads_slots.price_nis` ריק

**אין אף שורת מדף.** **המחירון שאושר ב-22.09 — `claude/ad_slot_pricing_recommendation.md`:**

```
לידרבורד ₪750 · בילבורד ₪500 · רייל ₪400
באנר קטגוריה ₪350 · ממוקד ₪300 · קרוסלה ₪250
בלעדיות קטגוריה = תוספת 100%
```

⚠️ **מחיר לכל שורת סלוט בנפרד — Yulian מתמחר במשא ומתן.** 🔴 **אלה ברירות מחדל, לא מחירים נעולים.** **`note` מתעד למה המחיר הזה ומי אישר**

---

# §5 · 🔴 ב-`/admin/ads` כפתור ״נקה״ לא מנקה

**Yulian, 22.09, עם צילום:** *״במסך הניהול אחרי סינון הסינון לא מתנקה.״*
**בצילום: הפילים על ״הכל״, ״כולל מחוקים״ לא מסומן, בתיבת החיפוש `/`, ותוצאה אחת. לחיצה על ״נקה״ לא מחזירה את הרשימה.**

## השורש — שתי שורות שנוגדות זו את זו

`services/frontend/src/app/admin/ads/page.tsx`

```js
:53   useEffect(() => { refresh(); }, [typeFilter, showHidden]);   // ← אין q
:99   onClear={() => { setType(''); setHid(false); setQ(''); }}    // ← אין refresh
```

**`q` נשלח לשרת (`:45  params.set('q', ...)`) אבל אינו במערך התלויות.** **החיפוש רץ רק דרך `onSearchSubmit={refresh}`.**
**ו-`onClear` לא קורא ל-`refresh` — הוא סומך על ה-effect.**

> 🔴 **וכשהמסנן היחיד הפעיל הוא הטקסט — בדיוק המקרה בצילום — `setType('')` ו-`setHid(false)` כותבים את אותו ערך שכבר שם.**
> **React בולם, ה-effect לא רץ, ושום דבר לא נטען מחדש.**
> **התיבה מתרוקנת על המסך והטבלה נשארת מסוננת.**

## ⚠️ והמלכודת בתיקון

🔴 **`onClear={() => { setType(''); setHid(false); setQ(''); refresh(); }}` **לא יעבוד**.**
**`refresh` סוגר על ה-`q` של הרנדר הנוכחי — הוא עדיין יקרא לשרת עם `/`.**

## Do

- **`refresh` מקבל override מפורש:** `refresh({ q: '', type: '', hidden: false })`, ו-`onClear` קורא לו עם הערכים הריקים. **בלי להישען על סדר עדכוני ה-state**
- **או לחלופין — `q` נכנס למערך התלויות עם debounce.** ⚠️ **זה משנה גם את התנהגות ההקלדה: כל תו יפנה לשרת. אם בוחרים בזה — debounce של 300ms לפחות, ודווח שבחרת בזה**
- ⚠️ **ובדוק את `/admin/subscriptions:76`** — `useEffect(..., [statusFilter])` באותה תבנית. **אם יש שם גם חיפוש טקסט שנשלח לשרת, זו אותה תקלה. דווח לפני שאתה מתקן**
- ✅ **שאר מסכי האדמין משתמשים ב-`clearFilters` עם סינון בצד הלקוח.** **אל תיגע בהם**

## Acceptance §5

- [ ] 🔴 **חיפוש `/` ב-`/admin/ads`, ואז ״נקה״ — הרשימה חוזרת למלואה.** צילום לפני/אחרי
- [ ] **אותו דבר כששלושת המסננים פעילים יחד.** צילום
- [ ] **הקריאה לשרת אחרי ״נקה״ — בלי `q`.** הדבק מה-Network
- [ ] **`/admin/subscriptions` — דווח אם נגוע**

---

# §6 · 🔴 `/admin/notifications-test` — שלושה מתוך חמשת הקרונים במסך אינם קיימים

**Yulian, 22.09:** *״לא ברור מה לעשות במסך הזה.״* **והוא צודק — המסך מיושן.**

## מה שבדקתי

**הקטלוג `services/notification/src/testCatalog.js:455-485` מציג חמישה קרונים.**
**התיקייה `services/notification/src/cron/` מכילה:**

```
adExpiringReminder · contractorRevalidation · graceHardCap · graceReminder
subscriptionRenewal · trialEndingReminder · visaExpiry
```

**הצלבה:**

| בקטלוג | המודול | קיים? |
|---|---|---|
| `visaExpiry` | `../cron/visaExpiry` | ✅ |
| `contractorRevalidation` | `../cron/contractorRevalidation` | ✅ |
| **`dealLifecycle`** | `../cron/dealLifecycle` | 🔴 **לא קיים** |
| **`contractorApprovalReminder`** | `../cron/contractorApprovalReminder` | 🔴 **לא קיים** |
| **`corpResponseOverdue`** | `../cron/corpResponseOverdue` | 🔴 **לא קיים** |

> 🔴 **שלושת הקרונים החסרים הם שרידים של מודל ה-`deals`, ש-`058_drop_dead_pivot_tables.sql:14-19` מחק לגמרי** — `deals`, `deal_workers`, `deal_events`, `commissions`.
> **לחיצה על ״run״ באחד מהשלושה תזרוק module-not-found.**

🔴 **ובכיוון ההפוך — חמישה קרונים אמיתיים שקיימים על הדיסק אינם מופיעים במסך בכלל:**
`adExpiringReminder` · `graceHardCap` · `graceReminder` · `subscriptionRenewal` · `trialEndingReminder`

⚠️ **כלומר המסך מציג בעיקר את מה שמת, ומסתיר את מה שחי — כולל `subscriptionRenewal`, שלפי `gaps_vs_workplan` מעולם לא רץ.**

## Do

- 🔴 **הסר את שלושת הקרונים המתים מהקטלוג.** **וודא ש-`deal.corp_committed` ב-`testCatalog.js:171` וב-`handlers.js:565` הוא גם שריד — `grep` ודווח לפני מחיקה**
- 🔴 **הוסף את חמשת הקרונים הקיימים לקטלוג**, עם תיאור ולוח זמנים
- **התיאורים בעברית.** ⚠️ **זה מסך אדמין עברי, וכל חמשת התיאורים היום באנגלית**
- 🔴 **הקטלוג ייגזר מהתיקייה, לא יוקלד.** **`readdir` על `src/cron/` + מפת תיאורים.** **אחרת אותה סטייה תחזור בקרון הבא שנוסיף או נמחק**
- **בדיקת שפיות בעלייה: קרון בקטלוג בלי מודול → לוג אזהרה.** **הוא לא אמור להגיע למסך**

## 6b · המסך לא מסביר את עצמו

**הפאנל ״אירוע מערכת״ הוא dropdown ותחתיו שטח לבן ריק בגובה 500px.** **עד שבוחרים — אין שום דבר.**

- **טקסט הסבר בשטח הריק:** מה הכפתור עושה, לאן ההודעה נשלחת, ושזה לא נוגע במשתמשים אמיתיים
- **״היסטוריה (50 אחרונים)״ מציג ״אין עדיין שליחות״** — ✅ **נכון ותקין. אל תיגע**

## Acceptance §6

- [ ] 🔴 **כל קרון במסך — ״run״ מחזיר תוצאה, לא שגיאת מודול.** **צילום של כל אחד**
- [ ] 🔴 **חמשת הקרונים הקיימים מופיעים.** צילום
- [ ] **הקטלוג נגזר מהתיקייה.** הדבק
- [ ] **כל התיאורים בעברית.** צילום
- [ ] **`deal.corp_committed` — דווח אם שריד, לפני מחיקה**

---

# §7 · 🔴 ״הצטרף עכשיו״ לא עובד בכלל

**Yulian, 22.09:** *״לחצן הצטרף עכשיו לא עובד בכלל.״*

## ✅ שחזרתי את זה בדפדפן החי ומדדתי. זה לא ניחוש

**R27 §2 בחר עוגן.** **והעוגן תקין — `page.tsx:2285`:**

```jsx
<section id="register" style={{ scrollMarginTop: 'calc(64px + var(--sticky-h,72px) + 12px)' }}>
```

**הרצתי ניווט צד-לקוח מ-`/how-it-works` אל `/#register`. זו התוצאה המדודה:**

```
location.pathname   /
location.hash       #register      ← ה-hash הגיע
#register קיים      true
מיקומו המוחלט       1599px
scrollY             0              🔴 הדף לא גלל בכלל
```

> 🔴 **ה-URL מקבל את ה-hash, היעד קיים, והדף נשאר בראש.**
> **המשתמש לוחץ ונוחת בראש דף הבית. מבחינתו — ״לא עובד בכלל״. בדיוק כפי ש-Yulian דיווח.**

⚠️ **זו התנהגות ידועה של Next.js App Router:** **בניווט צד-לקוח ה-hash מגיע לפני שהיעד קיים ב-DOM, הדפדפן לא מוצא אותו, ואף אחד לא מנסה שוב.**
**מדף הבית עצמו זה כן עובד — ולכן קל לפספס את זה בבדיקה.**

## ועוד ממצא באותה נשימה

🔴 **ב-`/how-it-works` אין אף קישור ״הצטרף״.** **`querySelectorAll('a')` מסונן על הטקסט מחזיר מערך ריק — לא מוסתר, פשוט לא קיים.**
⚠️ **מתחבר ל-R24 §5b: לעמוד הזה אין פוטר בכלל.** **העמוד שכל תפקידו לשכנע, ואין ממנו דרך להירשם.**

## Do

- 🔴 **אל תסתפק ב-`id`. הוא קיים. התקלה היא בגלילה אחרי ניווט**
- **הפתרון: גלילה מפורשת אחרי שהיעד קיים** — `useEffect` על הגעה ל-`/` שבודק `location.hash`, מחכה ל-`#register` ב-DOM ואז `scrollIntoView({behavior:'smooth'})`
- 🔴 **או — עמוד `/register` אמיתי שמרנדר את `RoleRegisterPicker`.** **R27 §2 אישר את שתי האפשרויות.** ⚠️ **זו היציבה, והיא גם מאפשרת קישור ישיר מהמייל ומהפרסום. אני ממליץ עליה**
- **בכל מקרה — הוסף CTA ופוטר ל-`/how-it-works`**
- ✅ **שלושת הקישורים בפוטר של דף הבית עובדים.** אימתתי ב-22.09. **אל תיגע**

## Acceptance §7

- [ ] 🔴 **מ-`/how-it-works`, מ-`/marketplace` ומדף הבית — לחיצה מגיעה לבורר ורואים אותו.** שלושה צילומים
- [ ] 🔴 **הדבק `scrollY` אחרי הלחיצה בכל אחד מהשלושה.** **לא 0**
- [ ] **מובייל ודסקטופ.** שני צילומים
- [ ] **`/how-it-works` — יש CTA ויש פוטר.** צילום

---

# §8 · 🔴 בהרשמה חסר תאריך סיום ההטבה

**בצילום מסך ההרשמה לספק:**

```
בסיסי    ₪149 / חודש    עד 5 מודעות · לתקופה של 30 ימים
מקצועי   ₪399 / חודש    עד 20 מודעות · לתקופה של 30 ימים

״במסגרת מבצע ההשקה — הפרסום ללא עלות עד סיום המבצע.
  לא נחייב אותך ללא הודעה מראש.״
```

> 🔴 **״עד סיום המבצע״ בלי תאריך, מעל שני מחירים ממשיים, ליד כפתור הרשמה.**
> **המשתמש מוסר פרטי עסק בלי לדעת מתי מתחיל החיוב. זו בדיוק הנקודה שבה צריך לומר מתי.**

**⚠️ והתאריך כבר הוכרע:** **31.12.2026** (Yulian, סבב R10).

## Do

- 🔴 **המשפט מציג תאריך מפורש:** *״הפרסום ללא עלות עד 31.12.2026. לא נחייב ללא הודעה מראש.״*
- 🔴 **התאריך נקרא ממקור אחד — `FREE_LAUNCH_UNTIL` — ולא מוקלד בטקסט.** ⚠️ **R19 מצא ארבעה מקורות סותרים לתאריך הזה. אל תיצור חמישי**
- **אם `FREE_LAUNCH_UNTIL` ריק — אל תרנדר משפט מבצע בכלל.** 🔴 **לעולם לא ״עד סיום המבצע״ בלי תאריך**
- ⚠️ **`cardcom.py:32` — `if not FREE_LAUNCH_UNTIL: return False`. דווח אם המסך והחיוב קוראים את אותו מקור.** **אל תיגע ב-`cardcom.py`**
- **אותו משפט בשלושת מסלולי ההרשמה. דווח היכן הוא מופיע**

---

# §9 · 🔴 אין ולידציית אימייל, וההודעה שמתקבלת היא של הדפדפן באנגלית

**בצילום — הוקלד `קכרשאשנרש@עצשוןרץב`, ואחרי ניסיון שמירה קפצה בועה:**

```
A part followed by '@' should not contain the symbol 'ק'.
```

**שתי תקלות נפרדות:**

**א.** 🔴 **אין שום ולידציה בזמן הקלדה.** **השדה בולע עברית עד לחיצה על שמירה**
**ב.** 🔴 **ההודעה היא ההודעה המובנית של הדפדפן — באנגלית, בטופס עברי RTL, בניסוח שאף אחד לא יבין.** **היא מגיעה מ-`type="email"` ולא מאיתנו**

> ⚠️ **וזה עוד שדה שאי אפשר לטעות בו:** **ההערה מתחתיו אומרת ״לכאן נשלח מייל ההצטרפות ותקבל דואר בפניות מלקוחות״.** **כתובת שגויה = הספק לא יקבל אף ליד ולא יֵדע.**

## Do

- 🔴 **ולידציה ב-`onBlur`** — לא בכל תו, זה מציק בזמן הקלדה
- 🔴 **הודעה בעברית שלנו:** `כתובת אימייל לא תקינה — לדוגמה name@company.co.il`
- 🔴 **`noValidate` על ה-`<form>`** כדי שהבועה של הדפדפן לא תקפוץ, **וההודעה שלנו מוצגת מתחת לשדה** — לא כ-tooltip
- **ההודעה קשורה לשדה ב-`aria-describedby`, והשדה מקבל `aria-invalid`.** ⚠️ **קורא מסך חייב לשמוע אותה**
- ⚠️ **ובדוק את שאר השדות באותו טופס** — ח.פ/ע.מ, טלפון, אתר. **אותה תבנית, אותה בעיה. דווח רשימה**
- **אותו תיקון בשלושת מסלולי ההרשמה**

---

# §10 · ב-״איך זה עובד״ חסר כל נושא האימות

**Yulian, 22.09:** *״חסר לי החלק שבו מאמתים תאגידים, קבלנים וספקי שירותים נלווים — כולם חייבים להיות עסקים לגיטימיים ורשומים במוסדות הרלוונטיים.״*

**זה לא קישוט. זו טענת הערך המרכזית של המוצר** — קבלן מוסר פרטים לתאגיד שהוא לא מכיר, והדבר היחיד שמצדיק את זה הוא שאנחנו בדקנו.

## מה שקיים במערכת (מאומת)

| | המנגנון |
|---|---|
| **קבלן** | `פנקס הקבלנים` — `contractorRevalidation.js` בודק מחדש קבלני `tier_2` |
| **תאגיד** | `gov_corps_registry` — ייבוא מ-PDF של רשות התאגידים · `/admin/gov-corps-registry` |
| **ספק שירותים** | 🔴 **אין אימות — וזו החלטה מכוונת שמתועדת בקוד** |

**ציטוטים מהקוד, לא פרשנות:**

```
providers.py:11    "Auto-approved. No גוב-list cross-check, no admin queue."
providers.py:98    "format-only ח.פ check (9 digits). No registry lookup —
                    provider is deliberately outside the corp registry"
077:165            business_number ... -- ח.פ / ע.מ — format-checked,
                                          NOT verified against a registry
077:179-182        verified_at / verified_by_user_id — "means an admin manually
                    verified the provider. NULL is the shipping default;
                    nobody starts verified."
```

> 🔴 **כלומר: ספק שירותים נרשם, מאושר אוטומטית, ומופיע באתר — בלי שאיש בדק אותו.**
> **ה-ח.פ נבדק רק שיש בו תשע ספרות.**
> ⚠️ **ויש עמודת `verified_at` בסכימה, עם הערה שהיא נועדה לאימות ידני של אדמין — אבל לא מצאתי מי כותב אליה.** 🔴 **CC יבדוק ויאשר או יפריך. אל תניח**

## Do

- **מקטע חדש ב-`/how-it-works`: ״איך אנחנו מאמתים״.** **שלוש פסקאות, אחת לכל סוג**
- 🔴 **אל תמציא נוסח ואל תמציא הבטחה.** **הצע, סמן `לאישור Yulian`, והמשך**
- 🔴 **וזו השאלה האמיתית — דווח ואל תמציא תשובה:** **מה בודקים אצל ספק שירותים?** ⚠️ **אם אין בדיקה, אסור לכתוב שיש. או שנוסיף בדיקה, או שהמקטע יאמר במדויק מה כן ומה לא נבדק**
- ⚠️ **תיאום עם R24 §3** — שלושת המשפטים שם (״אין הרשמה״ וכו׳) עדיין פתוחים. **אותו עמוד, אותו סבב עריכה**

---

# §11 · `/provider/marketplace/new` — הבאנר תקין, אבל הוא חושף אי-התאמה בשמות

**Yulian, 22.09:** *״אני מקבל מסך מוזר, לא יודע מה המקור שלו.״**

## ✅ קודם כל — המסך והבאנר תקינים

**זה מסך יצירת מודעה של ספק שירותים.** **מגיעים אליו מדשבורד הספק (`provider/dashboard/page.tsx:117`) ומיד אחרי הרשמת ספק (`register/provider/page.tsx:333`).**

**והבאנר הצהוב הוא פיצ׳ר מכוון —** `RoleGuard.tsx:178-196`, עם הערה שמסבירה שהוא נועד בדיוק לזה: *״so an admin doesn't spend a minute wondering why the page is empty״*.
**Yulian הוא אדמין שהישות הפעילה שלו היא קבלן, ולכן נתוני הספק לא נטענים.** ✅ **אל תסיר את הבאנר.**

## §11a · 🔴 אבל הוא מרנדר את המילה `provider` באנגלית

`RoleGuard.tsx:187`

```js
const expectLabel = ENTITY_TYPE_HE[expect] || expect;
```

`lib/labels.ts:83-87`

```js
export const ENTITY_TYPE_HE: Record<string,string> = {
  contractor:       'קבלן',
  corporation:      'תאגיד',
  service_provider: 'ספק שירותים',     ← אין מפתח 'provider'
};
```

**ו-`provider/layout.tsx:8` מעביר `expect="provider"`.**

> 🔴 **`ENTITY_TYPE_HE['provider']` הוא `undefined`, ה-fallback מחזיר את הקוד הגולמי, והמשפט העברי מקבל את המילה `provider` באנגלית.** **בדיוק מה שבצילום.**

**Do:** **הוסף `provider: 'ספק שירותים'` למפה.** ⚠️ **אל תמחק את `service_provider` — שניהם בשימוש**

## §11b · ⚠️ ואי-ההתאמה הזו רחבה מהתווית — בדוק אותה

**שם המקטע בניתוב הוא `provider`. סוג הישות ב-JWT הוא `service_provider`. `RoleGuard` משווה ביניהם ישירות בשלושה מקומות:**

```
:109   if (entityType === expect) return;
:163   entityType !== expect &&            → :166 NoAccessCard
:183   entityType !== expect;              → הבאנר
```

**`:117` כן מטפל בזה במפורש:**

```js
if (expect === 'provider' || entityType === 'service_provider') { … }
```

> ⚠️ **כלומר מי שכתב את זה ידע על הפער — אבל טיפל בו במקום אחד בלבד.**
> 🔴 **השאלה שצריך לבדוק: ספק שירותים אמיתי שאינו אדמין — האם `:163` שולח אותו ל-`NoAccessCard`?**
> **אני לא טוען שכן. `:117` מקדים אותו והוא עשוי לחסום את המסלול. CC יבדוק בפועל ויענה.**

**Do:**

- 🔴 **התחבר כספק שירותים אמיתי — לא אדמין — וגש ל-`/provider/dashboard` ול-`/provider/marketplace/new`.** **דווח מה קורה. צילומים**
- **אם הוא נחסם — 🔴 זה חוסם השקה.** **ספק לא יכול להגיע למסך שלו**
- **התיקון הנכון: נרמול במקום אחד** — `provider` ו-`service_provider` ממופים זה לזה בפונקציה אחת, ו-`RoleGuard` משווה מנורמל. ⚠️ **אל תפזר עוד תנאים מיוחדים כמו `:117`**

## §11c · פגמים במסך עצמו

- ⚠️ **ב-1920 הטופס תופס שליש ימני, ושני שלישים לבנים.** **הגבל את רוחב הטופס ומרכז אותו** — כמו שאר מסכי המוצר
- 🔴 **״פנוי מתאריך״ מציג `dd/mm/yyyy`** — **placeholder אנגלי של `<input type="date">` בטופס עברי.** **קבע `lang="he"` על הקלט או השתמש בבורר התאריך של המוצר אם קיים**
- ⚠️ **״קטגוריה *״ מציגה אריח יחיד נעול ״דיור להשכרה״, וה-URL בצילום בלי `?category=`.** **בדוק מאיפה הערך הגיע.** **אם זו ברירת מחדל קשיחה — ספק ציוד או ביטוח יקבל קטגוריה שגויה. דווח**

## Acceptance §11

- [ ] 🔴 **הבאנר אומר ״ספק שירותים״ בעברית.** צילום
- [ ] 🔴 **ספק אמיתי שאינו אדמין — דשבורד ומסך מודעה חדשה נטענים.** שני צילומים
- [ ] **נרמול במקום אחד. הדבק**
- [ ] **הטופס ממורכז ב-1920.** צילום
- [ ] **שדה התאריך בעברית.** צילום
- [ ] **מקור ברירת המחדל של הקטגוריה. דווח**

---

# §12 · עמוד המודעה — שטח פרסום ממוקד, ובאג שנתקלתי בו בדרך

**Yulian, 22.09:** *״כשצופים במודעה גם פה יש מקום להוסיף שטח פרסום בהתאם לסוג המודעה.״*

## §12a · 🔴 ובדרך מצאתי `0` שמרונדר על המסך

**בצילום, בשורת המטא: `זמין מ-27.9.2026 · 0 · עד 24 עובדים · אשדוד`.** **ה-`0` הזה לא אמור להיות שם.**

`services/frontend/src/app/marketplace/[id]/page.tsx:220`

```jsx
{listing.is_furnished && (
  <span className="text-emerald-700 font-medium">מרוהטת</span>
)}
```

> 🔴 **`is_furnished` הוא `TINYINT` ב-MySQL וחוזר כ-`0`, לא כ-`false`.**
> **ב-JSX, `0 && …` מחזיר `0` — ו-React מרנדר את הספרה.**
> **כל מודעה שאינה מרוהטת מציגה `0` באמצע שורת המטא.**

**Do:**

- 🔴 **`{!!listing.is_furnished && …}`** או `{listing.is_furnished ? … : null}`
- ⚠️ **ו-`:214` — `{listing.capacity && …}` — אותה מלכודת בדיוק.** **קיבולת 0 תרנדר `0`**
- 🔴 **`grep` על `{listing.` ועל `{data.` בכל הפרונט אחרי `&&` עם שדה מספרי או בוליאני.** **דווח רשימה.** ⚠️ **זו תבנית, לא מופע — היא חוזרת בכל מקום שבו TINYINT פוגש JSX**

## §12b · שני סלוטים חדשים בעמוד המודעה

**בצילום: מתחת לכרטיס יצירת הקשר בטור השמאלי — כ-530px ריקים. ומתחת לתיאור — כל שאר העמוד ריק.**

| סלוט | מיקום | מידה |
|---|---|---|
| `listing_rail` | מתחת לכרטיס יצירת הקשר, דסקטופ ≥1440 | 300×600 |
| `listing_inline` | מתחת לתיאור | 1200×250 · מובייל 720×300 |

## §12c · 🔴 והמיקוד — זה העיקר, וזה כבר קיים בסכימה

**Yulian ניסח את הכלל בעצמו:** *״חברה שמתמחה במציאת דירות תופיע רק בתוצאות החיפוש של דירות או מיטות, לעומת פרסומת של הדרכה בגובה שתופיע בחיפוש של פועלים.״*

✅ **אין צורך לבנות מנגנון מיקוד חדש. הכל קיים:**

```
sponsor_ads.target_professions / target_ad_types / target_regions   (069)
sponsor_ads_slots.category_code                                      (084)
```

**Do:**

- 🔴 **המודעה שמוצגת נבחרת לפי `category` של הליסטינג הנצפה.** **ליסטינג `housing` → מפרסמי דיור בלבד**
- 🔴 **`NULL` ב-`category_code` פירושו ״כל הקטגוריות״, לא ״אף אחת״.** **זו התבנית שכבר קיימת ב-`069` ב-`target_*`. אל תמציא סמנטיקה שנייה**
- ⚠️ **אם אין מודעה תואמת לקטגוריה — אל תרנדר סלוט ואל תיפול למודעה גנרית.** **ספק F3: ״אין מקטעים בלי מודעות פעילות״.** 🔴 **מפרסם ביטוח שמופיע על מודעת דיור הוא בדיוק מה ש-Yulian ביקש למנוע**
- **תקרת §5 חלה גם כאן — לכל היותר שתי יחידות מעל הקיפול**
- **אותה מדידה של R6:** `useAdImpression` ו-`adEvents`. ⚠️ **אל תכתוב מנגנון ספירה שני**

## Acceptance §12

- [ ] 🔴 **מודעה שאינה מרוהטת — אין `0` על המסך.** צילום לפני/אחרי
- [ ] 🔴 **רשימת כל מקומות ה-`&&` עם שדה מספרי/בוליאני בפרונט.** דווח
- [ ] **`listing_rail` ב-1920 וב-1440; לא קיים ב-1280 וב-390.** ארבעה צילומים
- [ ] **`listing_inline` בדסקטופ ובמובייל.** שני צילומים
- [ ] 🔴 **מודעת דיור מציגה מפרסם דיור; מודעת ציוד לא מציגה אותו.** שני צילומים
- [ ] 🔴 **בלי מודעה תואמת — הסלוט לא מרונדר.** צילום
- [ ] **החשיפות נרשמות דרך `useAdImpression`.** הדבק מ-`promo_events`

---

# §13 · 🔴 ״פרסום חדש בפורטל״ — הקישור מצביע לפרמטר שאיש אינו קורא

**Yulian, 22.09:** *״לחיצה על מודעה לא מגיבה, לא מציגה עוד פרטים, נראה שממש לא עושה כלום.״**

## הוא צודק לחלוטין, וזה מדיד

**U10 אכן תוקן — הכרטיסים כבר אינם `<div>`. `page.tsx:2236`:**

```jsx
<Link href={`/?ad=${ad.id}`} aria-label={ad.title_he}>
```

**אבל `grep` על כל קוראי הפרמטרים בקובץ מחזיר:**

```
page.tsx:878   params.get('reveal')
page.tsx:888   params.get('reveal')
:367-379       'prof' · 'region' · 'origin' · 'q'
```

**וחיפוש בכל הפרונט אחרי `searchParams.get('ad')` מחזיר אפס.**

> 🔴 **`?ad=` נכתב ואיש אינו קורא אותו.**
> **הלחיצה מנווטת לאותו נתיב `/` עם פרמטר מת. שום state לא משתנה, שום דבר לא מרונדר.**
> ⚠️ **וזה גרוע מה-`<div>` המקורי: עכשיו יש hover, טבעת פוקוס וסמן יד — הכרטיס *נראה* לחיץ, ולא עושה כלום.**

## 🔴 וזו הפעם הרביעית באותה תבנית

**ההערה ב-`:2230-2234` מצהירה:**

> *"`?ad=<id>` on the landing route so H11's reveal-return code path and the existing prospect/returnTo flow both resolve without a new route."*

**זה פשוט לא נכון. מסלול H11 קורא `?reveal=`, לא `?ad=`.**

**ארבע הערות שמתארות מנגנון שלא נבנה:**

```
search.py:195              guard שלא נבנה → דליפת P0 של R15
smoke_test.py:2168         _matrix_skipped_any
MarketplaceSponsors.tsx:11 "Scrolls on mobile" → R20 §3
page.tsx:2230              "?ad= resolves via H11"   ← החדשה
```

⚠️ **CC — זו אזהרה לשיטת העבודה, לא נזיפה. הערה אינה מימוש. כשאתה כותב הערה שמתארת התנהגות, בדוק שהיא קיימת.**

## מה שצריך להחליט — ואני ממליץ

**מודעות עובדים ודיור אינן גלויות לאנונימי (R13). אין להן עמוד פרטים ציבורי.** **לכן ״עוד פרטים״ אינו היעד הנכון.**

- 🔴 **המלצתי: הכרטיס מפעיל את מסלול החשיפה הקיים** — אותו `?reveal=<id>` שכבר מטופל במלואו ב-`:878-944`, כולל החזרה אחרי לוגאין. **אפס קוד חדש, מסלול בדוק**
- **אנונימי → `RevealModal` → `/register/contractor?returnTo=<adId>`.** **זה גם מה שהספק v2.0 §7 מתאר**
- **קבלן מחובר עם מכסה → חשיפה רגילה**
- ⚠️ **אל תיצור נתיב חדש ואל תיצור פרמטר שלישי.** 🔴 **אם בכל זאת משאירים `?ad=` — הוא חייב handler. פרמטר בלי קורא הוא בדיוק הבאג הזה**

## Acceptance §13

- [ ] 🔴 **לחיצה על כרטיס — משהו נפתח.** צילום לפני/אחרי
- [ ] 🔴 **אנונימי → מודאל חשיפה → הרשמה, וחוזר לאותה מודעה.** צילום כל שלב
- [ ] **קבלן מחובר → חשיפה, המונה יורד.** צילום
- [ ] 🔴 **`grep` על `?ad=` — אין פרמטר בלי קורא.** הדבק
- [ ] **מקלדת: `Tab` לכרטיס, `Enter` פותח.** צילום
- [ ] **כל תשעת הכרטיסים, עובדים ודיור כאחד.** צילום

---

# §14 · טופס פנייה לשירות לקוחות — ובאג שמצאתי בדרך

**Yulian, 22.09:** *״אם אני כבר מחובר יש את כל הפרטים עליי, הכל צריך להיות מלא וניתן לשינוי אם המשתמש רוצה. מאחורי הקלעים אנחנו תמיד נדע לחבר למשתמש שהתחבר גם אם הוא משנה את הטלפון שלו.״*

## ✅ הדרישה השנייה כבר מתקיימת — ובדיוק כמו שביקשת

`services/user-org/app/routes/support.py:50`

```python
INSERT INTO support_tickets (id, entity_type, entity_id, user_id, …)
VALUES (…, x_user_id, …)
```

**`user_id` מגיע מכותרת שהגייטוויי אימת. `entity_id` מהקשר ה-JWT. שניהם לא מגוף הבקשה.**
**ו-`contact_phone` הוא העדפת חזרה בלבד — ההערה בקוד אומרת זאת במפורש:** *"Optional callback phone if the user wants to be reached on a number other than the one they signed up with."*

> ✅ **גם אם המשתמש מקליד טלפון של מישהו אחר — הפנייה עדיין מחוברת אליו. אין כאן מה לתקן.**

**וגם צד האדמין כבר שולף את הפרטים** (`admin/routes/support.py:29-57`): שם החברה, טלפון ומייל מהישות, ו-`full_name` ו-`phone` מ-`auth_db.users` לפי `user_id`.

## §14a · 🔴 אבל ספק שירותים לא יכול לפתוח פנייה בכלל

`db/migrations/036_support_tickets.sql:14`

```sql
entity_type  ENUM('contractor','corporation','admin') NULL
```

**ו-`support.py:41` כותב לשם `"service_provider"`** — U7 §1 קבע במפורש שספקים יכולים לפתוח פניות.

🔴 **ומיגרציה `077` הרחיבה את ה-ENUM ב-`entity_memberships` · `entity_documents` · `entity_audit` · `subscriptions` · `subscription_plans` — ופסחה על `support_tickets`.**

> 🔴 **ב-strict mode ה-`INSERT` נכשל ב-500. אחרת MySQL שומר מחרוזת ריקה והפנייה מתייתמת.**
> **בשני המקרים ספק שירותים לא יכול לפנות לשירות לקוחות. וזה סוג ישות שמשיקים איתו.**

⚠️ **ו-`admin/support.py` פותר פרטי ארגון ל-`contractor` ול-`corporation` בלבד** — אין ענף לספק. **גם אם השורה תישמר, האדמין לא יראה מי פנה.**

**Do:**

- 🔴 **מיגרציה חדשה: `MODIFY COLUMN entity_type ENUM('contractor','corporation','service_provider','admin')`.** **אל תיגע ב-036**
- **ענף `service_provider` ב-`admin/routes/support.py`** — שליפה מ-`service_providers`
- 🔴 **בדוק אם יש עוד טבלאות ש-077 פספסה.** **`grep` על `ENUM('contractor','corporation'` בכל המיגרציות. דווח רשימה** ⚠️ **זו תבנית, לא מופע**

## §14b · הטופס לא מציג כלום על המשתמש

**בצילום: נושא, תיאור, ״טלפון לחזרה (אופציונלי)״ ריק. אין שם, אין ארגון, אין אינדיקציה שאנחנו יודעים מי אתה.**

**Do:**

- **בלוק זהות בראש הטופס:** שם, ארגון, וטלפון רשום — **קריאה בלבד, מה-session**
- 🔴 **`contact_phone` מאותחל לטלפון הרשום וניתן לעריכה.** **התווית משתנה ל-״טלפון לחזרה״ בלי ״אופציונלי״ — יש בו ערך**
- ⚠️ **שדה עריך שלא נשמר גרוע משדה שלא קיים.** **בטבלה יש `contact_phone` בלבד — אין `contact_name` ואין `contact_email`.** 🔴 **אל תוסיף שדות שם/מייל עריכים בלי עמודות. אם Yulian רוצה אותם — מיגרציה, ואז השדות. דווח ואל תחליט**
- **״ביטול״ מחזיר לאן שבאו ממנו, לא לדף הבית**

## Acceptance §14

- [ ] 🔴 **ספק שירותים מחובר פותח פנייה בהצלחה.** צילום + השורה מה-DB
- [ ] 🔴 **האדמין רואה את שם הספק ואת הטלפון.** צילום
- [ ] 🔴 **`grep` על `ENUM('contractor','corporation'` בכל המיגרציות.** דווח
- [ ] **הטופס מציג שם וארגון, והטלפון מאותחל.** צילום
- [ ] 🔴 **המשתמש משנה טלפון → `user_id` בשורה עדיין שלו.** הדבק את השורה
- [ ] **036 ללא שינוי.** `git diff` ריק

---

# §15 · 🔴 ולידציית טלפון ומייל — סריקה רוחבית. הכלי קיים ורוב הקוד לא משתמש בו

**Yulian, 22.09:** *״שמרתי את ההודעה עם טלפון לא תקין. בכל מקום שיש טלפון, מייל וכו׳ — תוודא שיש בדיקת תקינות, ושיש גם מיפוי להזנה אוטומטית של גוגל כמו כתובת וכו׳.״*
**מה שנשמר: `090998798677868` — חמש-עשרה ספרות.**

## 🔴 והכי מתסכל: המנרמל קיים, נבדק, ונכתב בדיוק בשביל זה

`services/user-org/app/services/phone_normalize.py`

```python
normalize_israeli_phone(value)  →  '0525267879' | '+972525267879'
                                   raises InvalidPhone
```

**עם קובץ בדיקות מלא** (`test_phone_normalize.py`) **שכבר מכסה `0525267879גגג` · `052😀5267879` · `052/5267879`.**
**ההערה בראשו אומרת שהוא נכתב אחרי דיווח מ-14.09 על `"0525267879גגג"` שנחת ב-DB והתפשט משם.**

## הסריקה — מי משתמש ומי לא

| | טלפון מנורמל | מייל `EmailStr` |
|---|---|---|
| `contractors.py` | ✅ | ✅ |
| `corporations.py` | ✅ | ✅ |
| `providers.py` | 🔴 **לא** | ✅ |
| `support.py` | 🔴 **לא** | — |
| `marketplace.py` | 🔴 **לא** | — |
| `membership_requests.py` | 🔴 **לא** | — |
| `ads.py` | 🔴 **לא** | 🔴 לא |
| `admin_approvals.py` | 🔴 **לא** | 🔴 לא |
| `reveals.py` · `search.py` | 🔴 לא | 🔴 לא |

> 🔴 **שניים מתוך עשרה נתיבים שנוגעים בטלפון מנרמלים אותו.**
> **`providers.py` ביניהם — מסלול ההרשמה של סוג הישות החדש מקבל טלפון בלי בדיקה.**

⚠️ **CC — אמת אילו מהם באמת *מקבלים* טלפון בכתיבה ואילו רק קוראים.** **`search.py` ו-`reveals.py` ככל הנראה קוראים בלבד. דווח רשימה מדויקת לפני שאתה נוגע.**

## Do — שרת

- 🔴 **`normalize_israeli_phone` בכל מסלול כתיבה שמקבל טלפון.** **אל תכתוב מנרמל שני**
- 🔴 **`InvalidPhone` → `400` עם הודעה בעברית**, לא 500
- **`EmailStr` בכל מודל שמקבל מייל** — `ads.py` ו-`admin_approvals.py` חסרים
- ⚠️ **הערך שנשמר ל-DB הוא המנורמל**, לא מה שהוקלד. **כך `0525267879` ו-`052-526-7879` הם אותה שורה**
- 🔴 **השרת הוא מקור האמת.** **ולידציית הפרונט היא נוחות בלבד**

## Do — פרונט (§9 מרחיב את זה, כאן הצד הרוחבי)

- **ולידציה ב-`onBlur`, הודעה בעברית מתחת לשדה, `aria-invalid` + `aria-describedby`**
- 🔴 **`dir="ltr"` ו-`text-align: right` על שדות טלפון, מייל, ח.פ ו-URL.** ⚠️ **מספר בשדה RTL מוצג בסדר שגוי — זו תקלה אמיתית בטופס עברי**
- **`inputMode="tel"` · `type="email"` · `inputMode="numeric"` לח.פ** — מקלדת נכונה במובייל

## Do — `autocomplete`, מה ש-Yulian ביקש

🔴 **`support/page.tsx` — אפס תגי `autoComplete`.** **`register/provider/page.tsx` — ארבעה. זה הפער.**

**התגים התקניים שגוגל והדפדפנים מזהים:**

```
שם מלא        autocomplete="name"
איש קשר       autocomplete="name"
שם עסק        autocomplete="organization"
טלפון         autocomplete="tel"          inputMode="tel"
טלפון נייד    autocomplete="tel-national"
מייל          autocomplete="email"        type="email"
אתר           autocomplete="url"
כתובת         autocomplete="street-address"
עיר           autocomplete="address-level2"
מיקוד         autocomplete="postal-code"
מדינה         autocomplete="country-name"
```

- 🔴 **כל שדה בכל טופס במוצר מקבל את התג המתאים.** **סרוק, אל תתקן טופס אחד**
- ⚠️ **`autocomplete="off"` רק היכן שיש סיבה אמיתית** (OTP). **אל תפזר אותו — הוא שובר מילוי אוטומטי והדפדפנים בלאו הכי מתעלמים ממנו לרוב**
- **שדה חייב `name` ו-`id`** — בלעדיהם המילוי האוטומטי לא עובד גם עם התג הנכון

## Acceptance §15

- [ ] 🔴 **`090998798677868` בטופס הפנייה → `400` בעברית.** צילום + ה-`curl`
- [ ] 🔴 **רשימת כל מסלולי הכתיבה שמקבלים טלפון, ומי מנרמל אחרי התיקון.** טבלה
- [ ] 🔴 **הרשמת ספק עם טלפון פגום → נדחית.** הדבק
- [ ] **`052-526-7879` ו-`0525267879` נשמרים זהים.** שתי שורות מה-DB
- [ ] **מייל פגום ב-`ads.py` ו-`admin_approvals.py` → `400`.** הדבק
- [ ] 🔴 **מילוי אוטומטי של הדפדפן ממלא שם, טלפון, מייל ועיר.** **צילום או GIF**
- [ ] **טלפון ומייל מוצגים `ltr` ונקראים נכון.** צילום
- [ ] **מנרמל אחד בלבד בקוד.** `grep` ודווח
- [ ] **`test_phone_normalize.py` עובר**

---

# §16 · 🔴 המסגרת סביב החיפוש שבורה — ״איך זה עובד?״ יושב בתוך הקפסולה

**Yulian, 22.09:** *״שים לב הקווים בצדדים לא סגורים סביב ה׳איך זה עובד׳.״*

✅ **ואגב — בצילום שלו רואים ש-R28 §3 נחת. שורת השירותים הנלווים חיה.**

## מדדתי את שלושת האלמנטים. הם תוכננו להיות קפסולה אחת

```
.ai-search-form     border-top/left/right: 2px   border-bottom: 0
                    radius  12px 12px 0 0        ← פינות עליונות בלבד
                    צבע     rgb(226,232,240)     slate-200

div.mt-2 mb-1       border: 0   background: transparent
״איך זה עובד?״      ← יושב בדיוק באמצע

.readout            border-bottom/left/right: 2px   border-top: 0
                    radius  0 0 14px 14px        ← פינות תחתונות בלבד
                    צבע     rgb(247,130,3)       brand-600
```

> 🔴 **הטופס פתוח למטה, ה-readout פתוח למעלה — הם נבנו להידבק זה לזה ולסגור מלבן אחד.**
> **ובאמצע נדחף div עם `mt-2 mb-1` בלי מסגרת וברקע שקוף.**
> **שנים-עשר פיקסלים של מרווח, ועוד גובה השורה עצמה — ושני הקצוות נשארים פתוחים. בדיוק מה שרואים.**

## 🔴 ושני פגמים נוספים שצצו באותה מדידה

**גם אילו היו צמודים, המסגרת לא הייתה תואמת:**

| | הטופס | ה-readout |
|---|---|---|
| **צבע** | `slate-200` `rgb(226,232,240)` | **`brand-600` `rgb(247,130,3)`** |
| **רדיוס** | 12px | **14px** |

⚠️ **חצי מסגרת אפורה וחצי כתומה, בשני רדיוסים שונים. זה ייראה שבור גם אחרי שסוגרים את הפער.**

## Do

- 🔴 **הוצא את ״איך זה עובד?״ מהקפסולה.** **מתחת ל-`.readout`, לא בין השניים.** **זו התקלה עצמה — הפתרון הזול והבטוח**
- ⚠️ **אם הוא חייב להישאר בפנים** — אז הוא חלק מהקפסולה: `border-left/right: 2px` באותו צבע, ורקע לא שקוף. 🔴 **פחות טוב. אני ממליץ על הראשון**
- 🔴 **אחד הצבעים לשתי המסגרות.** **`brand-600` כשה-readout פעיל, `slate-200` כשלא — אבל שני החצאים תמיד באותו צבע.** ⚠️ **סמן `לאישור Yulian` איזה מצב נראה כתום**
- **רדיוס זהה. 12px או 14px, לא שניהם**
- ⚠️ **ובדוק את אותה קפסולה כשה-readout לא מרונדר** — הטופס לבדו עם `border-bottom: 0` ופינות עליונות בלבד **ייראה פתוח למטה**. 🔴 **במצב הזה הוא צריך מסגרת מלאה ורדיוס מלא. דווח מה מצאת**

## Acceptance §16

- [ ] 🔴 **מסגרת סגורה לגמרי סביב החיפוש וה-readout.** צילום לפני/אחרי
- [ ] 🔴 **הטופס לבדו, בלי readout — סגור ומעוגל מכל הצדדים.** צילום
- [ ] **צבע אחד ורדיוס אחד לשני החצאים.** הדבק את ה-CSS
- [ ] **ב-1280 וב-390.** שני צילומים
- [ ] **״איך זה עובד?״ עדיין נגיש ולחיץ במיקומו החדש.** צילום
- [ ] `לאישור Yulian` על צבע המסגרת

---

# §17 · 🔴 הודעת ה-Cloudinary משקרת — Cloudinary מוגדר, הקריאה נכשלת ב-401

**Yulian, 22.09:** *״אני שוב מקבל הודעה Cloudinary.״*
**ההודעה על המסך: ״העלאה מהמחשב תופעל כשמנהל השרת יגדיר Cloudinary״.**

## ✅ אבל Cloudinary כן מוגדר. שתי הוכחות

1. **Yulian, מוקדם יותר היום:** *״cloudinary הוגדר ב-railway stg.״*
2. 🔴 **הקריאייטיב החי בסטייג׳ינג מוגש מ-`res.cloudinary.com/dqzhmmq4p/image/upload/...`.** **אי אפשר להגיש משם בלי חשבון פעיל**

## מה שמדדתי בפועל

**התחברתי כאדמין, נכנסתי ל-`/admin/sponsors`, וקראתי לנתיב בדיוק כמו שהעמוד קורא לו:**

```
GET /api/uploads/cloudinary-signature   (credentials: include, מתוך /admin/sponsors)
→ 401   { "error": "Unauthorized" }
```

**ולא `501 {"code":"not_configured"}` — שזה מה שהשרת מחזיר כשחסרים משתני הסביבה** (`uploads.py:42`).

> 🔴 **הכשל הוא הרשאה, לא תצורה. וההודעה מאשימה את התצורה.**

**והסיבה בקוד:** `sponsors/page.tsx:653`

```js
apiFetch('/uploads/cloudinary-signature')
  .then(setSig)
  .catch(() => setSig(null))      ← בולע 401 · 403 · 500 · רשת · הכל
```

**כל כישלון מתורגם לאותה הודעה אחת.**

⚠️ **והחשוד המוביל ל-401 —** `gateway/src/index.js:235`: *"L1 §2 · SEC-2 — `/api/uploads` is NO LONGER public."* **SEC-2 נעלה את כל התחילית, ו-`/cloudinary-signature` יושב תחתיה ונסגר איתה.** 🔴 **CC יאמת. אל תניח**

## Do

- 🔴 **הפרד את המצבים.** **`501` → ״טרם הוגדר״. `401`/`403` → ״אין הרשאה או שפג תוקף החיבור״. אחר → ״שגיאה זמנית, נסה שוב״**
- 🔴 **אל תבלע את השגיאה.** **`catch` שמאבד את ה-status הוא הסיבה שחיפשנו את זה שעה**
- 🔴 **פתח את `/api/uploads/cloudinary-signature` לאדמין מאומת.** ⚠️ **נתיב אחד בלבד. אל תפתח מחדש את כל `/api/uploads` — SEC-2 סגר אותו מסיבה טובה. דווח איך פתחת**
- **אחרי התיקון — העלאה אמיתית מהמחשב, מקצה לקצה**

## §17b · 🔴 והמידות בטופס סותרות את מה ש-R29 §2 בדיוק אכף

**הטופס אומר היום:**

```
:820   "מומלץ 1200×628 (באנר) או 1080×1080 (קרוסלה)"
:661   "המודעה משמשת גם בבאנר (1200×628) וגם בקרוסלה (1080×1080)"
:49    const CARD_ASPECT = 1;
```

**והטבלה שאושרה ב-R29 §2 ונאכפת מאז `4599e16`:**

```
home_leaderboard  1200×150      home_billboard  1200×250
קרוסלה            640×360       search_inline   240×240
```

> 🔴 **`1080×1080` אינו קיים בטבלה המאושרת. `1200×628` אינו מידת סלוט לאף סלוט.**
> **המסך אומר למפרסם להכין מידה שהוולידציה החדשה תדחה.**
> ⚠️ **זה ייצור פנייה לשירות לקוחות ביום הראשון — והאדמין יאשים את עצמו**

**Do:**

- 🔴 **הרמזים נגזרים מאותו קטלוג `SIZES` שאוכף** — **לא מוקלדים.** ⚠️ **זה בדיוק כפל המקורות של §15 ושל הערת ה-SIZES המשוכפל. אל תוסיף מקור שלישי**
- **`CARD_ASPECT = 1` → היחס של הקרוסלה מהקטלוג**
- **הרמז משתנה לפי ה-`placements` שנבחרו, ומציג את המידה המדויקת שתתקבל**

## Acceptance §17

- [ ] 🔴 **`GET /api/uploads/cloudinary-signature` כאדמין → `200` עם `cloud_name`.** הדבק
- [ ] 🔴 **העלאת קובץ מהמחשב בטופס החסויות — עובדת.** צילום
- [ ] 🔴 **בלי הרשאה → ״אין הרשאה״, לא ״טרם הוגדר״.** צילום
- [ ] **`/api/uploads/*` האחרים עדיין סגורים.** `curl` אנונימי, הדבק
- [ ] 🔴 **הרמזים בטופס = הקטלוג.** הדבק את שניהם זה לצד זה
- [ ] **העלאת 1080×1080 לקרוסלה → נדחית, וההודעה נוקבת במידה הנכונה.** צילום

---

# §18 · 🔴 המודאל בטופס החסויות — הכותרת נדבקת לאלמנט הלא נכון

**Yulian, 22.09:** *״שים לב לקטיעה בגלילה למעלה בטופס.״*
**בצילומים: שדות הטופס מופיעים *מעל* שורת ״עריכת מודעה״ ונקטעים סביבה.**

## פתחתי את המודאל ומדדתי. שתי סיבות, שתיהן בשורה אחת

```
הגולל   div.fixed inset-0 z-50 bg-slate-900/50 flex items-start
            justify-center p-4 overflow-y-auto
        scrollHeight 1467 · clientHeight 752      ← זה מה שגולל

הכותרת  div.flex items-center justify-between p-4 border-b
            sticky top-0 bg-white rounded-t-2xl
        position: sticky · top: 0 · z-index: auto
```

### א. 🔴 ה-sticky נדבק לרקע, לא לכרטיס

> **`position: sticky` נדבק לאב הגולל הקרוב ביותר.**
> **וכאן הגולל הוא ה-overlay עצמו — לא כרטיס המודאל.**
> **הכותרת יושבת בתוך הכרטיס, אבל נצמדת ל-`top: 0` של הרקע — מעל הפינות המעוגלות ומחוץ ל-`p-4` שלו.**

⚠️ **ומכאן גם ההיסט המדויק:** **הרקע הוא `items-start` עם `p-4`, אז הכרטיס מתחיל ב-16px — והכותרת ננעצת ב-0.** **שישה-עשר פיקסלים של אי-התאמה, בדיוק מה שנראה בצילום.**

### ב. 🔴 `z-index: auto` על כותרת דביקה

**אין לה הקשר ערימה משלה, ולכן שדות שמופיעים אחריה ב-DOM נצבעים מעליה.** **זו ה״קטיעה״ — הטקסט של השדה חוצה את הכותרת.**

## Do

- 🔴 **הכרטיס הוא הגולל, לא הרקע:** **`max-h-[90vh] overflow-y-auto` על הכרטיס.** **הרקע — `overflow-hidden`**
- 🔴 **ואז `sticky top-0` נדבק לכרטיס — וזה מה שהתכוונו אליו מלכתחילה**
- 🔴 **`z-10` מפורש על הכותרת.** **`z-index: auto` על דביק הוא באג ממתין**
- ⚠️ **גם לשורת הכפתורים התחתונה, אם יש** — **אותה בדיקה, אותו תיקון**
- **`items-center` על הרקע כשהתוכן נמוך מהמסך.** **מודאל קצר שנתלה בראש נראה שבור**

## ⚠️ ובדוק אם זו תבנית

🔴 **`grep` על `sticky top-0` בכל הפרונט, וצלב מול מי הגולל בפועל ומול `z-index`.**
**דווח רשימה.** **אותו מודאל כנראה הועתק למסכי אדמין נוספים.**

## Acceptance §18

- [ ] 🔴 **גלילה מלאה במודאל — הכותרת נשארת למעלה, שום שדה לא חוצה אותה.** **צילום בראש, באמצע ובתחתית**
- [ ] 🔴 **הכותרת מיושרת לקצה הכרטיס, בלי היסט 16px.** צילום
- [ ] **מודאל קצר — ממורכז, לא תלוי בראש.** צילום
- [ ] **ב-1280 וב-390.** שני צילומים
- [ ] 🔴 **רשימת כל `sticky top-0` בפרונט והאב הגולל של כל אחד.** דווח
- [ ] **`Esc` והלחיצה על הרקע עדיין סוגרים**

---

# §19 · שני פריטי תפריט שנשמעים אותו דבר

**Yulian שאל ב-22.09 מה ההבדל ביניהם — וזו התשובה שהשאלה עצמה היא הממצא.**

`admin/layout.tsx:30-31`

```
/admin/leads     ״פניות ובקשות״          → טבלת leads
/admin/support   ״פניות שירות לקוחות״    → טבלת support_tickets
```

**הם באמת שני דברים שונים לגמרי:**

| | `leads` | `support_tickets` |
|---|---|---|
| **מי** | מבקר **לא רשום** | משתמש **מחובר** |
| **מאיפה** | טופס ״השאירו פרטים״ | ״פנייה לשירות לקוחות״ |
| **מה יש** | שם · טלפון · סוג ארגון | נושא · תיאור · `user_id` |
| **למה** | **ליד מכירתי** — להתקשר ולגייס | **תקלה** — לפתור |
| **סיום** | `handled_at` (מיגרציה 018) | `status: resolved` |

> ⚠️ **״פניות ובקשות״ מתאר תור מכירות. ״פניות שירות לקוחות״ מתאר תמיכה.**
> **שתי התוויות מתחילות במילה ״פניות״, ולכן הן נקראות כאותו דבר בשתי גרסאות.**

**Do:**

- **הצע שמות שמבדילים לפי *מה עושים* עם הפריט.** **הצעתי, `לאישור Yulian`:**
  - `/admin/leads` → **״לידים — השאירו פרטים״**
  - `/admin/support` → **״תמיכה ותקלות״**
- 🔴 **אל תשנה נתיבים, טבלאות או API.** **תוויות תפריט וכותרות מסך בלבד**
- **וכותרת המסך תואמת לתווית בתפריט** — `support/page.tsx:107` מציג היום ״פניות שירות לקוחות״

---

# §20 · 🔴 ״מסלולי מנוי״ מסתיר את הספק — והתמחור מפוזר על שלושה מסכים

**Yulian, 22.09:** *״מסלולי מנוי — אני רואה רק קבלן ותאגיד. חסר לי מודעות ושירותים נוספים.״*

## §20a · 🔴 שורת הספק קיימת ב-DB, והמסך מסתיר אותה

**`db/migrations/089_r10_s6_provider_seats.sql` מכניסה שורת `subscription_plans` אחת ל-`service_provider`:**

```
entity_type = 'service_provider' · tier = 'basic'
monthly_price_nis = 0 · included_users = 5 · extra_user_price_nis = 50
```

⚠️ **ה-₪50 הוא בדיוק מה ש-Yulian החליט ב-R10 §6.**

**וה-API מחזיר אותה** — `admin/routes/subscription_plans.py:32` שולף `FROM subscription_plans` **בלי `WHERE`**.

🔴 **הפרונט הוא שחוסם:**

```js
subscription-plans/page.tsx:15    entity_type: 'contractor' | 'corporation'
:135-136                          רק שתי הקבוצות האלה
:153                              (['contractor','corporation'] as const).map(…)
```

> 🔴 **השורה מגיעה מהשרת ונזרקת בפרונט.** **מחיר המושב של הספק לא ניתן לעריכה בשום מקום במוצר.**

⚠️ **והערה ב-`:33-34` מודה בזה:** *"ENTITY_TYPE_HE — includes the U7-reserved 'service_provider' key"*. **התווית הוכנה, העמודה לא נוספה.** **זו שוב אותה תבנית של חצי-מימוש.**

**Do:**

- 🔴 **הוסף `service_provider` לטיפוס, לקיבוץ ולמערך הרינדור.** **שלוש שורות**
- ⚠️ **אבל ההצגה שונה** — ל-`service_provider` יש `monthly_price_nis = 0` והמכסות שלו חיות ב-`marketplace_subscription_tiers`. 🔴 **אל תרנדר לו עמודות ״מודעות״ ו-״ימי חיים״ ריקות — הן לא שייכות לו.** **הצג מושבים ומחיר מושב נוסף בלבד, עם שורת הסבר לאן ללכת בשביל המכסות**
- 🔴 **הקיבוץ ייגזר מהערכים שחזרו מהשרת, לא ממערך קשיח.** **אחרת סוג ישות רביעי ייעלם בדיוק כך**

## §20b · ״שירותים נוספים״ קיים — במסך אחר, בשם אחר

✅ **המסלולים של השירותים הנלווים (₪149 / ₪399) כן ניתנים לעריכה — ב-`/admin/marketplace`:**

```
״ניהול קטגוריות שירותים נלווים״
  → לכל קטגוריה: פאנל ״מסלולי מנוי״ (כמות מודעות וזמן פרסום)
```

**וגם מחירי הפרסום חיים במסך שלישי — `/admin/sponsors` → לשונית ״סלוטים בלעדיים״.**

> ⚠️ **אז התמחור מפוזר על שלושה מסכים, ואף אחד מהם לא מפנה לשני.**

🔴 **תיקון למה שכתבתי כאן קודם:** **`/admin/marketplace` אינו ״מסך אחר״ — הוא מסך *בלי כניסה*.** **אין לו שורה בתפריט בכלל.** **ראה §21 — זה השורש, וזו הסיבה ש-Yulian לא מצא אותו.**

**Do:**

- **ב-״מסלולי מנוי״ — שורת הפניה בראש:** *״מסלולי השירותים הנלווים מנוהלים בקטגוריות שירותים נלווים · מחירי הפרסום בחסויות״*, **עם שני קישורים אמיתיים**
- **והפניה הדדית משני המסכים האחרים בחזרה לכאן**
- 🔴 **אל תאחד את שלושת המסכים בסבב הזה.** ⚠️ **שלוש טבלאות שונות עם סמנטיקה שונה — איחוד הוא פרויקט, לא תיקון. הצע בנפרד אם Yulian ירצה**

## Acceptance §20

- [ ] 🔴 **״מסלולי מנוי״ מציג שלוש קבוצות, כולל ספק שירותים.** צילום
- [ ] 🔴 **עריכת `extra_user_price_nis` של הספק נשמרת.** צילום + השורה מה-DB
- [ ] **לספק אין עמודות מכסה ריקות.** צילום
- [ ] **הקיבוץ נגזר מהנתונים, לא ממערך קשיח.** הדבק
- [ ] **שלוש ההפניות ההדדיות עובדות.** שלושה צילומים

---

# §21 · 🔴 שלושה מסכי ניהול קיימים ואין אליהם כניסה מהתפריט

**Yulian שלח ב-22.09 צילום של התפריט המלא. הצלבתי מול התיקייה.**

```
services/frontend/src/app/admin/   →  17 מסכים
admin/layout.tsx:21-34             →  14 שורות תפריט
```

## השלושה שנשארו בחוץ

| מסך | מה יש בו | למה זה כואב |
|---|---|---|
| 🔴 **`/admin/marketplace`** | ״ניהול קטגוריות שירותים נלווים״ — **הקטגוריות ומסלולי ה-₪149/₪399** | **זה בדיוק מה ש-Yulian חיפש ולא מצא (§20).** לא ניתן לערוך קטגוריה או מחיר |
| 🔴 **`/admin/tenders`** | אישור בקשות הצעות ובחירת זוכה | **שער הפרסום.** בלעדיו אף בקשה לא עולה |
| 🔴 **`/admin/legal`** | עורך תנאי שימוש · פרטיות · נגישות | **התוכן המשפטי ב-DB.** בלעדיו אי אפשר לתקן אף מילה |

> 🔴 **המסכים בנויים ועובדים. פשוט אין אליהם קישור.**
> **מי שלא יודע את ה-URL בעל פה — המסך לא קיים עבורו.**

## ⚠️ ולמה שלושת אלה דווקא מדאיגים

- **`marketplace`** — **ערוץ השירותים הנלווים כולו בלתי ניתן לניהול.** **קטגוריה שגויה או מחיר שגוי לא ניתנים לתיקון בלי גישה ל-DB**
- **`tenders`** — ⚠️ **מחקר המונטיזציה בפרויקט מעריך את ערוץ הבקשות ב-50-60% מההכנסות, והספק קובע ש״publish-gate של Admin״ נדרש כדי שבקשה תעלה.** 🔴 **אם השער לא נגיש — הערוץ לא מתפקד. CC יאמת אם הערוץ בכלל בהיקף ההשקה, ויציין זאת מפורשות**
- **`legal`** — 🔴 **R24 §4b ביקש שתאריך העדכון של הצהרת הנגישות ייגזר מ-`updated_at`.** **בלי המסך אין דרך לערוך את התוכן בכלל**

## Do

- 🔴 **שלוש שורות ל-`ADMIN_NAV` ב-`layout.tsx`.** **אלה שלוש שורות, לא פרויקט**
  - `/admin/marketplace` → **״קטגוריות שירותים נלווים״**
  - `/admin/tenders` → **״בקשות הצעות״**
  - `/admin/legal` → **״מסמכים משפטיים״**
- ⚠️ **התוויות `לאישור Yulian`**
- 🔴 **ואז בדיקה שתמנע את החזרה:** **טסט שמשווה את תיקיית `app/admin/*` למערך `ADMIN_NAV` ונכשל על כל מסך בלי כניסה.** **אחרת המסך ה-18 ייעלם בדיוק כך**
- ⚠️ **אם מסך מכוון להיות מוסתר — רשימת היתר מפורשת בקוד עם הסבר, לא השמטה שקטה**

## ⚠️ ובדוק את אותו דבר בצד המשתמש

🔴 **`grep` על כל התיקיות תחת `app/contractor/`, `app/corporation/` ו-`app/provider/`, וצלב מול תפריטי הניווט שלהן.**
**דווח רשימה.** **אם קיים שם מסך יתום — משתמש אמיתי לא מגיע אליו, וזה חמור יותר מאשר אצל האדמין.**

## Acceptance §21

- [ ] 🔴 **שלושת המסכים בתפריט ונפתחים.** שלושה צילומים
- [ ] 🔴 **`/admin/marketplace` — עריכת מסלול ₪149 נשמרת.** צילום + השורה מה-DB
- [ ] **`/admin/tenders` נטען בלי שגיאה. דווח אם הוא בהיקף ההשקה**
- [ ] **`/admin/legal` — עריכת תוכן נשמרת.** צילום
- [ ] 🔴 **הטסט שמשווה תיקיות לתפריט — עובר, ונכשל כשמוחקים שורה.** הדבק את שתי ההרצות
- [ ] 🔴 **רשימת מסכים יתומים בקבלן · תאגיד · ספק.** דווח

---

# §22 · 🔴 שורת השירותים הנלווים נבנתה כסרגל דביק — לא כמו ש-R28 §3 ביקש

**Yulian, 22.09, מהנייד שלו:** *״בנייד השינויים האחרונים נראים לא טוב.״*

## מדדתי ב-412×915 — ורוחב העמוד תקין

```
document.scrollWidth = 412 · clientWidth = 412   ← אין גלישה אופקית בעמוד
```

⚠️ **כלומר זו לא גלישה. זה נראה שבור מסיבה אחרת.**

## מה שנמצא בפועל

```
השורה   div.sticky top-16 z-40 bg-white/95 backdrop-blur-sm
            border-b border-slate-200 shadow-sm
        left 0 · right 412 · רוחב מלא, קצה לקצה

הטופס   .ai-search-form
        left 12 · right 400 · שוליים של 12px משני הצדדים
```

> 🔴 **R28 §3 ביקש שורה *בתוך* `.max-w-5xl mx-auto px-3 sm:px-4`, מעל הטופס — באותם שוליים בדיוק.**
> **מה שנבנה הוא סרגל דביק ברוחב מלא, עם מסגרת תחתונה וצל משלו.**

## ולכן זה נראה כמו שזה נראה — ארבע תוצאות ישירות

- 🔴 **שוליים לא תואמים.** **הסרגל ב-0..412, הטופס ב-12..400.** **העין קולטת את הסרגל כבורח מהמסך בעוד הטופס מיושר**
- 🔴 **`border-b` + `shadow-sm` הופכים אותו לכותרת שנייה.** **מתחת לכותרת האמיתית של האתר — שני סרגלים זה מתחת לזה**
- 🔴 **״ציוד״ נחתך באמצע המילה** בלי שום סימן שאפשר לגלול. **בצילום נראה גם פס הגלילה האפור מתחת לשורה**
- ⚠️ **`sticky top-16` אוכל גובה מעל הקיפול בדיוק במסך שבו הוא הכי יקר**

## Do

- 🔴 **החזר את השורה לתוך מכולת התוכן** — **אותו `max-w-5xl mx-auto px-3 sm:px-4` של הטופס.** **שוליים זהים. זו התקלה העיקרית**
- 🔴 **הסר `sticky` · `top-16` · `z-40` · `border-b` · `shadow-sm` · `backdrop-blur`.** **זו שורת רמז, לא סרגל ניווט**
- ⚠️ **הגלילה האופקית במובייל נשארת** (היא מונעת שש שורות — R28 §3), **אבל:**
  - **הסתר את פס הגלילה:** `scrollbar-width: none` **וגם** `::-webkit-scrollbar { display: none }`
  - 🔴 **הוסף הצללה מתפוגגת בקצה** (`mask-image` או שכבת gradient) **כדי שייראה שיש עוד — במקום מילה חתוכה**
  - **`scroll-snap-type: x proximity`** כדי שהעצירה תהיה על צ׳יפ שלם ולא באמצעו
- ✅ **תוכן השורה נכון ואושר. אל תיגע בטקסט ובצ׳יפים** — רק במכולה

## ⚠️ ובאותה נשימה — §16 נראה גם הוא בצילום הזה

**המסגרת סביב ״איך זה עובד?״ פתוחה בצדדים גם במובייל.** **תקן את שניהם יחד, זו אותה קפסולה.**

## Acceptance §22

- [ ] 🔴 **צילום ב-412 וב-390: השורה והטופס באותם שוליים בדיוק.** **הדבק `getBoundingClientRect().left` של שניהם — אותו מספר**
- [ ] 🔴 **אין `border-b`, אין צל, אין `sticky`.** הדבק את המחלקות
- [ ] 🔴 **פס הגלילה מוסתר.** צילום
- [ ] **הצללה בקצה — נראה שיש עוד.** צילום
- [ ] **גלילה עוצרת על צ׳יפ שלם.** צילום
- [ ] **תיבת החיפוש עדיין מעל הקיפול.** צילום
- [ ] **ובדסקטופ שום דבר לא נסוג.** צילום ב-1280

---

# §23 · הסינון המתקדם אינו יודע מה חיפשו

**Yulian, 22.09:** *״סינון מתקדם צריך להיבנות לפי מה שמוצג. אם אני מחפש מגורים ורואה מודעות של מגורים, אז סינון לפי כל המקצועות לא רלוונטי.״*

## הוא צודק, והנתון הדרוש כבר חוזר מהשרת

`page.tsx:1511-1552` — **שלושת ה-selects מרונדרים תמיד, קשיחים:**

```
כל המקצועות   ← professions
כל הארץ       ← regions
כל הארצות     ← origins
```

**ובתשובת החיפוש כבר יש** (`lib/api/search.ts:7`):

```ts
interface SearchFilters { ad_type: 'worker' | 'housing'; … }
```

> 🔴 **השרת כבר אומר מה סוג התוצאות. הפרונט לא שואל.**
> **בצילום: החיפוש ״מגורים״, התגית ״דיור״, ארבע מודעות דיור — ומעליהן ״כל המקצועות״ ו-״כל הארצות״.**
> ⚠️ **למודעת דיור אין מקצוע ואין ארץ מוצא. אלה שני מסננים שלא יכולים להחזיר כלום.**

## Do

**המסננים נגזרים מ-`resp.filters.ad_type`:**

| מצב | מה מוצג |
|---|---|
| `worker` | מקצוע · אזור · ארץ מוצא |
| `housing` | **אזור בלבד** |
| `primary_section: 'marketplace'` | ⚠️ **קטגוריה** — לא מקצוע ולא ארץ מוצא |

- 🔴 **וזה החלק שקל לפספס: כשמסתירים מסנן — מנקים את ערכו.**
  **אם המשתמש בחר ״ריצוף״, ואז חיפש ״מגורים״ — `fProf` נשאר `flooring` והוא מסונן בשקט על ידי מסנן שהוא לא רואה.**
  **`useEffect` על `ad_type`: כל מסנן שיוצא מהמסך מתאפס, והחיפוש רץ מחדש**
- **אם נשאר מסנן אחד בלבד — שקול לוותר על ״סינון מתקדם״ ולהציג אותו ישירות.** ⚠️ **סמן `לאישור Yulian`**
- 🔴 **אל תוסיף מסנני דיור חדשים בסבב הזה** (מיטות · מחיר למיטה · מרוהט). ⚠️ **השרת מקבל היום `prof` · `region` · `origin` בלבד — מסנן חדש דורש עבודת שרת. דווח כהצעה נפרדת**
- **המונה ״4 מודעות · 3 שירותים נלווים״ נכון ועובד.** ✅ **אל תיגע**

## Acceptance §23

- [ ] 🔴 **חיפוש ״מגורים״ — ״כל המקצועות״ ו-״כל הארצות״ לא מוצגים.** צילום
- [ ] 🔴 **חיפוש ״רצפים סינים״ — שלושת המסננים מוצגים.** צילום
- [ ] 🔴 **בוחרים ״ריצוף״, מחפשים ״מגורים״, חוזרים — `fProf` התאפס ולא סינן בשקט.** **צילום + ה-URL**
- [ ] **חיפוש ״ביטוח״ — המסננים מתאימים לשירותים נלווים.** צילום
- [ ] **ה-URL לא נושא פרמטר של מסנן מוסתר.** הדבק
- [ ] **ב-390 וב-1280.** שני צילומים

---

# §24 · 🔴 `side_rail` — נבנה מחדש כעמודת גריד. יש מפרסם אמיתי

**22.09 — Yulian: יש מפרסם בדרך, הסלוט נשאר.** **התלונה: הרצועה חופפת לתוכן ושורת החיפוש רוכבת עליה. מספר ניסיונות תיקון לא הצליחו.**

🔴 **אל תתקן את המימוש הקיים. מחק אותו ובנה לפי המפרט כאן.** ⚠️ **טלאי על גישה שגויה הוא הסיבה שזה נמשך.**

## למה זה נכשל — השורש

> 🔴 **רצועה צדדית אינה שכבה צפה. היא עמודה בפריסה.**
> **`fixed` או `absolute` יתנגשו תמיד, כי התוכן לא שומר לה מקום — הוא פשוט לא יודע שהיא שם.**

## ⚠️ ושתי מלכודות שכנראה בזבזו את רוב הזמן

**א. `backdrop-filter` על אב שובר `fixed` ו-`sticky`.**
🔴 **§22 מצא `backdrop-blur-sm` על סרגל הצ׳יפים.** **אב עם `backdrop-filter`, `filter` או `transform` יוצר בלוק-מכיל חדש, ואז `position: fixed` מתמקם ביחס *אליו* ולא ביחס לחלון.**
**בדוק את כל שרשרת ההורים של הרייל לשלושת המאפיינים האלה. דווח מה מצאת.**

**ב. `sticky` בתוך גריד לא עובד בלי `align-self: start`.**
🔴 **ברירת המחדל היא `stretch` — התא נמתח לגובה כל השורה, אין לאן ״להידבק״, וה-`sticky` לא עושה כלום.**

## המפרט

```css
/* מכולת העמוד */
@media (min-width: 1440px) {
  grid-template-columns: minmax(0, 1fr) 300px;
  gap: 24px;
}
/* מתחת ל-1440 — עמודה אחת, הרייל לא מרונדר כלל */
```

- 🔴 **הרייל הוא *ילד של הגריד*.** **אפס `fixed`, אפס `absolute`, אפס שוליים שליליים**
- 🔴 **`position: sticky` + `top: <גובה הניווט + 12px>` + `align-self: start`**
- 🔴 **עמודת התוכן ממלאת את התא שלה.** ⚠️ **היא לא יכולה להישאר `mx-auto max-w-6xl` ממורכזת בנפרד — אחרת שתיהן נלחמות על אותו רוחב, וזה בדיוק מה שנראה בצילום**
- **`z-index` נמוך מהניווט.** **הרייל אף פעם לא מעל סרגל עליון**
- 🔴 **אין מודעה פעילה → הרייל לא מרונדר, והגריד קורס לעמודה אחת.** **`display:none` לא מספיק — התא יישאר**
- ⚠️ **מ-1440 בלבד.** **ב-1280 העמודה גונבת רוחב מהתוצאות**

## Do

- **מחק את מימוש `side_rail` הנוכחי לגמרי, ואז בנה לפי המפרט**
- **המכולה משותפת לדף הבית, לתוצאות החיפוש ול-`/marketplace`** — **רכיב פריסה אחד, לא שלושה עותקים**
- 🔴 **בדוק את שרשרת ההורים ל-`transform` · `filter` · `backdrop-filter` לפני שאתה מתחיל.** **דווח**

## Acceptance §24

- [ ] 🔴 **1920 ו-1440: הרייל בצד, התוכן לידו. אפס חפיפה.** שני צילומים
- [ ] 🔴 **שורת החיפוש לא נוגעת ברייל בשום רוחב.** **צילום ב-1920 · 1680 · 1440**
- [ ] 🔴 **1280 ו-390: הרייל לא ברנדר, התוכן מלא.** שני צילומים
- [ ] **גלילה — הרייל נדבק ולא מכסה את הניווט.** צילום
- [ ] 🔴 **בלי מודעה פעילה — עמודה אחת, בלי תא ריק.** צילום
- [ ] **`grep` על `fixed`/`absolute` ברכיב הרייל — ריק.** הדבק
- [ ] **דוח `transform`/`filter`/`backdrop-filter` בשרשרת ההורים**

---

# 🔴 גל 0 — שלושת אלה ראשונים, לפני גל 1

**דווחו על ידי CC ב-23.09 בתום אימות R29 §3-§5. אימתתי את שלושתם בקוד.**

---

# §25 · 🔴 הקריאייטיב נחתך במובייל — והקטלוג כבר יודע את התשובה

**CC:** *״375px: the paid ad shows headline only. `MarketplaceSponsors.tsx:396` — `aspect-ratio:8/1` + `overflow-hidden`. scrollHeight 80 vs clientHeight 43.״*

> 🔴 **זו מודעה בתשלום שה-CTA שלה בלתי נראה בנייד. זה הממצא היחיד שמבקר אמיתי פוגש היום, והנייד הוא רוב התנועה.**

## ⚠️ אבל אל תתקן את זה עם `aspect-auto`

**הקטלוג מגדיר מידה נפרדת למובייל, והיא נכונה:**

```
home_leaderboard   desktop (1200,150) = 8:1     mobile (720,200) = 3.6:1
home_billboard     desktop (1200,250) = 4.8:1   mobile (720,300) = 2.4:1
```

**והרכיב מקבל מספר יחיד** — `MarketplaceSponsors.tsx:294  aspectRatio: number` — **ומחיל אותו בכל רוחב (`:398`).**

🔴 **כלומר הרכיב מחיל את יחס הדסקטופ על המובייל. הקטלוג מעולם לא נשאל.**

**והחשבון מוכיח שהקטלוג פותר את זה לבד:**

```
375px ÷ 3.6  =  104px    התוכן דורש 80px      ✅ ובעודף
375px ÷ 2.4  =  156px    לבילבורד             ✅
```

## Do

- 🔴 **`aspectRatio` הופך ל-`{ desktop, mobile }` ונקרא מהקטלוג.** **מתחת ל-`sm` מוחל יחס המובייל**
- 🔴 **אל תסיר את `aspect-ratio` ואל תעבור ל-`aspect-auto`.** ⚠️ **הוא שם כדי למנוע CLS — הערה `:328` מסבירה זאת. הסרה תחזיר קפיצת עמוד**
- **`overflow-hidden` נשאר**
- ⚠️ **`body_he` מוסתר היום ב-`sm:block`.** **ב-104px הוא כנראה נכנס. בדוק ודווח — אם נכנס, החזר אותו**
- **בדוק את שני הסלוטים הרחבים ואת הקרוסלה. דווח טבלה של גובה נדרש מול גובה בפועל בכל רוחב**

## Acceptance §25

- [ ] 🔴 **375 · 390 · 412: כותרת, טקסט ו-CTA נראים במלואם.** שלושה צילומים
- [ ] 🔴 **`scrollHeight <= clientHeight` בכל השלושה.** הדבק
- [ ] **`aspect-ratio` עדיין מוגדר — לא `auto`.** הדבק
- [ ] **דסקטופ לא נסוג.** צילום ב-1920
- [ ] **היחס נקרא מהקטלוג, לא מוקלד.** הדבק

---

# §26 · 🔴 מגבלת הקצב חונקת את העמוד — ו-12 מתוך 17 הבקשות מיותרות

**CC:** *״`rateLimit.js:12` — anon gets 30 req/min on one shared counter; a single homepage load costs 17 requests (12 are duplicates: `/api/events` ×4, each enum ×2, legal ×2).״*

> 🔴 **זה לא באג של הרייל. זה באג של העמוד.**
> **שתי טעינות של דף הבית בדקה = 34 בקשות מול תקרה של 30. מבקר אמיתי מקבל 429.**
> ⚠️ **הרייל הוא רק הקורבן הראשון כי הוא נורה אחרון.**

## Do — בסדר הזה

1. 🔴 **תחילה חסל את הכפילויות. זה השורש ולא התקרה.**
   - **כל enum נטען פעמיים** — **מטמון מודול או ספק הקשר אחד. לא שני רכיבים ששולפים במקביל**
   - **`legal` פעמיים** — אותו דבר
   - ⚠️ **`/api/events` ×4 אלה ארבע חשיפות אמיתיות ולגיטימיות.** 🔴 **אבל אגד אותן לבקשה אחת עם מערך.** **אל תמחק חשיפות — הן המדידה שעליה נשען המחירון**
   - **דווח את מספר הבקשות לפני ואחרי**
2. **ורק אז התקרה:** 🔴 **`/api/events` לא חולק מונה עם נתוני העמוד.** **מונה נפרד ורחב יותר לטלמטריה** — ⚠️ **אחרת חשיפות של מודעות מרעיבות את התוכן, וזה בדיוק ההפך מהכוונה**
3. **ובדוק את התקרה עצמה מול טעינה אמיתית אחרי הניקוי.** **הצע מספר ודווח**

## Acceptance §26

- [ ] 🔴 **מספר הבקשות בטעינת דף הבית, לפני ואחרי.** **המטרה: מתחת ל-8**
- [ ] 🔴 **חמש טעינות רצופות בדקה — אפס 429.** הדבק
- [ ] **כל ארבע החשיפות עדיין נרשמות ב-`promo_events`.** הדבק את השורות
- [ ] **מונה נפרד לטלמטריה.** הדבק
- [ ] **הרייל נטען בטעינה החמישית.** צילום

---

# §27 · שורת זרע עם CTA שבור

**CC:** *״`cta_url: "Www.tagidai.com"` → `https://staging.tagidai.com/Www.tagidai.com`. `admin/sponsors/page.tsx:389` already documents this exact row and guards the form, but the guard only covers edits.״*

⚠️ **הגנה שמכסה עריכות בלבד אינה מגנה על שורה שנכנסה לפניה.** 🔴 **וזו הפעם החמישית בסבב הזה שהערה מתארת הגנה שלא מכסה את המקרה שהיא מתארת.**

**Do:**

- **תקן את השורה דרך מסך האדמין** — 🔴 **לא ב-`UPDATE` ולא במיגרציה חדשה**
- 🔴 **הנורמליזציה עוברת לשרת:** **`cta_url` בלי סכימה מקבל `https://`, ונדחה אם אינו URL תקין.** ⚠️ **כלל לקוח בלבד מגן על טופס אחד, לא על הנתונים**
- **סרוק את כל שורות `sponsor_ads` ל-`cta_url` בלי סכימה. דווח רשימה לפני תיקון**
- ⚠️ **ואם `cta_url` ריק — CTA כ-`<span>`, לא קישור מת.** **הכלל מ-069 נשאר**

## Acceptance §27

- [ ] 🔴 **כל ה-CTA בסלוטים מובילים ליעד חי.** **הדבק קודי סטטוס**
- [ ] **`Www.tagidai.com` ב-API → `400`.** הדבק
- [ ] **רשימת השורות שהיו פגומות.** דווח
- [ ] **`cta_url` ריק → `<span>`.** צילום

---

# §28 · 🔴 ״600×300״ — כל זוג מספרים בעברית מתהפך על המסך

**Yulian, 23.09:** *״חלון הפרסום בצד שמאל לא נראה טוב.״* **בצילום הכרטיס אומר `סלוט צד · 600×300`.**

## ובמיגרציה כתוב ההפך

`db/migrations/095_r29_side_rail_dedicated_seed.sql:49`

```sql
'סלוט צד · 300×600',
```

🔴 **הנתון נכון. התצוגה הפוכה.**

> **זהו bidi.** **בפסקה עם כיוון בסיס RTL, `×` הוא תו ניטרלי בין שני מספרים, ולכן הוא מקבל את כיוון הבסיס.**
> **שני מספרי ה-LTR נפרסים מימין לשמאל — ו-`300×600` נקרא על המסך `600×300`.**

## ⚠️ וזו אינה תקלה של כרטיס אחד

🔴 **כל זוג מספרים בתוך טקסט עברי מתהפך:** **מידות · טווחי מחיר · טווחי תאריך · גרסאות · מספרי טלפון.**

**והנזק הממשי — §17b:** **טופס החסויות אומר ״מומלץ 1200×628״.** 🔴 **קורא עברי רואה ״628×1200״, מזמין קריאייטיב במידה הפוכה, והוולידציה החדשה תדחה אותו.** **המפרסם יאשים את עצמו.**

## Do

- 🔴 **כל זוג מספרים מוצג בתוך `<bdi dir="ltr">` או `<span dir="ltr">`.** ⚠️ **`unicode-bidi: isolate` — לא `embed`**
- 🔴 **סרוק את כל הפרונט אחרי הדפוס ״מספר × מספר״ ואחרי טווחים** (`–`, `-`, `..`) **בתוך מחרוזת עברית. דווח רשימה**
- **החל גם על שדות הטופס מ-§15** — **אותו שורש, שם זה קלט וכאן תצוגה**
- 🔴 **וגם על ההודעות שהשרת מחזיר.** **הודעת דחייה שאומרת ״נדרש 1200×150״ תתהפך בדיוק כך**

## §28b · והכרטיס עצמו הוא placeholder של פיתוח

**״זהו מיקום התצוגה של פרסום מסוג side rail. מיועד למחשבים בלבד ברוחב 1440 ומעלה.״**

⚠️ **זה טקסט למפתח, על מסך שמוצג כאילו הוא המוצר. הוא כהה ובולט באתר בהיר, והוא מה ש-Yulian שופט.**

- **`is_seed = TRUE` על השורה** (הדגל מ-069 קיים בדיוק לזה). **ודא שהיא מסומנת**
- 🔴 **הסתר שורות `is_seed` בפרודקשן.** **דגל סביבה, לא מחיקה**
- ⚠️ **ובינתיים בסטייג׳ינג — הפוך אותו לנייטרלי ויזואלית** (רקע בהיר, מסגרת מקווקוות, תווית ״הדגמה״). **כדי שלא ייקרא כעיצוב**
- **`cta_url` שלו — ודא שאינו שבור.** **§27**

## Acceptance §28

- [ ] 🔴 **הכרטיס מציג `300×600`.** צילום
- [ ] 🔴 **טופס החסויות מציג את המידות בסדר הנכון.** צילום
- [ ] **רשימת כל זוגות המספרים בפרונט ובהודעות השרת.** דווח
- [ ] **הודעת דחייה של מידה — נקראת נכון.** צילום
- [ ] **שורות `is_seed` מסומנות ומוסתרות בפרודקשן.** הדבק
- [ ] **הכרטיס נייטרלי ויזואלית בסטייג׳ינג.** צילום

---

# §29 · 🔴 הרייל 300×139 במקום 300×600 — לסלוט אין `aspect-ratio` בכלל

**Yulian, 24.09:** *״הבאנר הצדדי עדיין לא נראה טוב, הוא לא מלבני כמו שביקשתי.״*

## מדדתי ב-1920 על סטייג׳ינג החי

✅ **הגריד של §24 נחת ועובד:**

```
grid-template-columns:  1120px 300px
<aside> position: sticky · top: 96px · align-self: start
```

🔴 **אבל הכרטיס עצמו:**

```
רוחב  300   ✅
גובה  139   🔴  צריך 600
aspect-ratio: auto   ← בכל חמש רמות השרשרת
```

## השורש

**כל השרשרת היא `h-full`:** `div.rounded-2xl w-full h-full` → `a.block w-full h-full` → `div.flex flex-col h-full p-4`.
**כולם ממלאים את ההורה — ול-`<aside>` אין גובה, אז הוא קורס לגובה התוכן: 139px.**

> 🔴 **הרצועות הרחבות קיבלו `aspectRatio` ב-§25. הרייל לא קיבל כלום.**
> **הקטלוג אומר `side_rail: desktop (300, 600)` — יחס 0.5 — ואף אחד לא מחיל אותו.**

## Do

- 🔴 **החל `aspect-ratio` על סלוט הרייל מאותו קטלוג ובאותו מנגנון שכבר בנית לרצועות.** **`300/600`. זו הרחבה של קוד קיים, לא מנגנון חדש**
- ⚠️ **ה-`aspect-ratio` יושב על המכולה החיצונית של הכרטיס** — **לא על `<aside>`, כדי לא לשבור את ה-`sticky`**
- **`h-full` הפנימי נשאר** — ברגע שלמכולה יש גובה, הוא יעבוד כמתוכנן
- ⚠️ **התוכן קצר מהמסגרת עכשיו.** **פרוס אותו לגובה: כותרת למעלה, CTA למטה, `justify-between`.** **אל תמתח את הטקסט**
- 🔴 **ובדוק שכל שאר הסלוטים בקטלוג מקבלים יחס.** **`search_inline` 240×240 · `logo_wall` 240×120 · הקרוסלה 640×360.** **דווח טבלה: סלוט → יחס מוחל כן/לא**

## ✅ ואישור ל-§28 — הנתון תקין, התצוגה הפוכה

**`labelText` ב-DOM הוא בדיוק `סלוט צד · 300×600`.**
🔴 **הצילום של Yulian מראה `600×300`. זה מאשר סופית שזהו bidi ולא נתון שגוי.** **תקן את הרינדור לפי §28, לא את המחרוזת.**

## Acceptance §29

- [ ] 🔴 **הרייל 300×600 ב-1920 וב-1440.** **הדבק `getBoundingClientRect()` של שניהם**
- [ ] **התוכן פרוס לגובה, CTA למטה.** צילום
- [ ] 🔴 **ה-`sticky` עדיין נדבק אחרי השינוי.** **הרץ שוב את מבחן הגלילה ב-`/marketplace`**
- [ ] 🔴 **טבלת כל הסלוטים: יחס מוחל כן/לא.** דווח
- [ ] **1280 ו-390 — הרייל לא מרונדר.** שני צילומים

---

# Guardrails

⭐ **ריצה רציפה. `git tag pre-r30`. סטייג׳ינג בלבד, שני הענפים.**

🔴 **אל תעצור לאישור ויזואלי באמצע.** **צלם, המשך, ודווח הכל בסוף.**

🔴 **אחרי כל גל: `npm run build` · `npm test` · `--suite all` · `--suite matrix`, ו-`git rev-list --left-right --count origin/staging...origin/pivot/v2` → `0 0`.**

🔴 **רגרסיה בכל גל:** **R29 (מידות · רצועות מורכבות · תקרת צפיפות) · R28 (`primary_section` · שורת הצ׳יפים) · R15 (אפס דליפה ב-`/api/marketplace`) · R16 · R13.**

🔴 **אל תיגע בנתונים ב-`/admin/orgs` — Yulian תיקן ידנית ואימתתי. רק הקוד.**

🔴 **אל תיגע** בחשיפה · במטריצת R13 · ב-R15 · ב-R16 · ב-`cardcom.py` · ב-`query_rewriter.py` · וב-R28/R29 שכבר רצו.

🔴 **מיגרציה חדשה בלבד אם צריך. אל תיגע במיגרציה שרצה.**

🔴 **`git add` לפי שם קובץ. לעולם לא `-A`.** **וכולל `docs/cc-prompts/*.md`.**
