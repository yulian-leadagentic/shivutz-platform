# R10 · מסלול פרסום לספק · קליטה · מושבים · הודעות

## ⭐ `git tag pre-r10`. מסבב ידני של Yulian, 18.09.

> **Yulian:** *"המשתמש צריך לבחור מסלול פרסום שלפיו הוא משלם… הוא חייב לבחור מסלול פרסום למודעות שלו — לכל תחום מסלול פרסום עם עלות מותאמת."*

---

# §0 · 🔴 קרא לפני שאתה מתכנן — רוב זה כבר קיים

**`021_marketplace_subscriptions.sql` בנתה בדיוק את מה שמבוקש, וזרעה אותו.**

```sql
CREATE TABLE marketplace_subscription_tiers (
  category_code, name_he, name_en,
  slot_count, duration_days, price_nis, is_active, sort_order
)
```

**והזרעים — שני מסלולים לכל קטגוריה, במחיר שונה לכל תחום:**

| קטגוריה | בסיסי | מקצועי |
|---|---|---|
| דיור | ₪199 · 5 מודעות | ₪499 · 20 |
| ציוד וכלי עבודה | ₪149 · 5 | ₪399 · 20 |
| שירותים | ₪149 · 5 | ₪399 · 20 |
| כללי | ₪99 · 5 | ₪299 · 20 |

> 🔴 **״מסלול פרסום לכל תחום עם עלות מותאמת״ קיים, מתומחר ועריך באדמין. אף אחד לא חיבר אותו להרשמת הספק.**

**ולמה אין מסלול לספק היום** — `services/user-org/app/routes/providers.py:15`, בתיעוד של הקובץ עצמו:

```
3. Free. No `marketplace_subscriptions` row is created here.
```

**כלומר לספק אין מסלול, אין מכסת מודעות, ואין קשר לטבלת המחירים.**

🔴 **אל תבנה טבלת מחירים חדשה. אל תמציא סכימה. חבר את הקיימת.**

---

# §1 · בחירת מסלול בהרשמה

## Do

**R5 §2b הוסיף בחירת קטגוריה כשלב ראשון. המסלול הוא השלב שאחריו.**

```
1  קטגוריה ראשית        ← קיים מ-R5
2  מסלול בתוך הקטגוריה   ← חדש. מ-marketplace_subscription_tiers
3  שאר הפרטים
4  OTP
5  יצירת marketplace_subscriptions עם ה-tier שנבחר
```

- **הצג לכל מסלול: שם · כמה מודעות · לכמה זמן · מחיר.** ⚠️ **המחיר לחודש, לא ל-`duration_days`** — 30 יום זה חודש, אבל אם אדמין ישנה ל-90 המשתמש צריך לראות מה הוא משלם בפועל
- **קרא רק `is_active=TRUE`, לפי `sort_order`**
- 🔴 **אין מסלולים פעילים בקטגוריה → אל תחסום את ההרשמה.** הרשם, סמן לאדמין, ואפשר לו לשייך מסלול ידנית. **חסימת הרשמה בגלל טבלת מחירים חסרה היא לא התנהגות סבירה**
- **ספק יכול לפרסם ביותר מקטגוריה אחת** *(R5 §2b — שיוך, לא שער)*. **אז המסלול הוא לכל קטגוריה בנפרד.** בהרשמה בוחרים אחד; **הוספת קטגוריה נוספת מאוחר יותר = מסלול נוסף**. 🔴 **בנה את זה כך מהיום** — `marketplace_subscriptions` כבר בנויה עם `category_code`

## §1b · ולמה זה בנפרד ממנוי הישות

⚠️ **לספק יהיו שתי ישויות מנוי, וזה נכון ולא כפילות:**

| טבלה | מה היא מייצגת |
|---|---|
| `subscriptions` | **החשבון** — משתמשים, מושבים, סטטוס |
| `marketplace_subscriptions` | **המלאי הפרסומי** — כמה מודעות בכל קטגוריה |

**לתאגיד יש את שתיהן כבר היום.** 🔴 **אל תנסה למזג אותן.**

---

# §2 · תקופת החינם — בלי קוד חדש

> **Yulian:** *"שישלם או שיהיה במסלול חינם למספר חודשים, אבל שיפרסם מודעות."*

**`marketplace_subscription_tiers` כבר מכילה `price_nis` ו-`duration_days`.**
**מסלול עם `price_nis=0` ו-`duration_days=180` הוא בדיוק תקופת החינם. אפס קוד.**

## 🔴 אבל אל תציג אותו כאפשרות בבורר

**אם ״חינם״ מופיע לצד ₪199, כולם יבחרו חינם ואף אחד לא יֵדע מה המחיר האמיתי.**

**במקום זה:**

- **הבורר מציג את המסלולים בתשלום עם המחירים האמיתיים** — זה עוגן המחיר, והוא שווה כסף בהמשך
- **בעת ההרשמה מוחל אוטומטית קידום השקה:** *״המסלול שבחרת — ₪149 לחודש. חינם עד 31.03.2027.״*
- **טכנית:** השורה ב-`marketplace_subscriptions` מקבלת את ה-tier שנבחר, עם `price_paid=0` ו-`expires_at` בסוף תקופת הקידום
- 🔴 **`promo_note` או שדה מקביל על השורה** — **מי שיסתכל על זה בעוד חצי שנה חייב לדעת למה לא שולם**

⚠️ **תאריך סיום הקידום — לא בקוד.** `site_settings` מ-`074` או טבלת האדמין. **Yulian קובע ומשנה.**

---

# §3 · 🔴 ההודעה לאדמין אומרת ״תאגיד״ על ספק

## הצילום

```
Yulian, תאגיד חדש ממתין לאישור: יוליאן ובניו
```

**נרשם ספק שירותים.**

## השורש

`services/notification/src/consumers/handlers.js:158`

```js
`${firstName}, ${orgTypeLabel} חדש ממתין לאישור: ${payload.org_name}\n${adminLink}`
```

**`orgTypeLabel` מגיע ב-payload. התבנית תקינה — מי שמפרסם את האירוע שולח ״תאגיד״.**

## Do

- **מצא את המפרסם** ותקן ל-**״ספק שירותים״**
- 🔴 **`grep -rn "org_type_label\|orgTypeLabel" services/`** — **כל מפרסם שמרכיב את התווית הזו.** **אותה תבנית בת-שני-ערכים שהפילה את `select-entity` ב-R5**
- ⚠️ **`046_admin_approval_alert.sql:20,24` — תבנית האימייל משתמשת באותו משתנה.** **בדוק שגם היא מקבלת את הערך הנכון**
- **ומה בכלל ההודעה אומרת לגבי ספק?** 🔴 **ספק נרשם ל-`status='active'` מיד — הוא לא ממתין לאישור.** **או שהנוסח משתנה ל״ספק שירותים חדש נרשם״, או שההודעה לא נשלחת עליו בכלל. הכרע ודווח**

---

# §4 · אחרי ההרשמה — ישר ליצירת מודעה

> **Yulian:** *"לאחר הרישום המשתמש צריך לעבור למסך יצירת המודעה שלו, ולא להזין שוב את מספר הטלפון."*

- **יעד אחרי OTP: `/provider/marketplace/new`** *(קיים מ-R5)*, **עם הקטגוריה שנבחרה כברירת מחדל**
- 🔴 **הטלפון שכבר אומת ממלא מראש את שדה איש הקשר במודעה.** **בקשה חוזרת של נתון שהמשתמש נתן לפני שלושים שניות היא הסימן המובהק ביותר לטופס שלא נבדק מקצה לקצה**
- **`grep` את שאר שדות ההרשמה** — שם העסק, עיר, אימייל. **כל מה שכבר יש — ממלא מראש**
- **״אולי מאוחר יותר״ מוביל לדשבורד הספק**, לא לדף ריק

---

# §5 · מייל קליטה

> **Yulian:** *"שיישלח אליו מייל עם הוראות — איפה הוא יכול לראות את המודעות שלו, איך הוא עוקב אחרי הפרסומים, ואיפה הוא מנהל את המשתמשים שלו."*

## 🔴 חסם שצריך להכריע לפניו

**`register/provider/page.tsx:266` — ״אימייל עסקי (אופציונלי)״.**

**בלי אימייל אין לאן לשלוח.** **הפוך אותו לשדה חובה** — אותה החלטה כמו ח.פ ב-R5 §2a. **ולידציית פורמט בשרת, לא רק בלקוח.**

## התוכן — ארבעה דברים, לא יותר

1. **קישור ישיר לדשבורד** — *לא* לדף הבית
2. **המודעות שלך** — איפה לראות, לערוך, ולפרסם חדשה
3. **המעקב** — כמה צפיות ופניות, ואיפה זה מוצג
4. **ניהול משתמשים** — הקישור, וכמה מושבים כלולים

- **תבנית ב-`notification_templates`** כמו כל השאר. 🔴 **אל תשתול HTML בקוד** — התבניות נערכות במיגרציה ובאדמין, וזו המוסכמה
- **שלח אחרי אימות ה-OTP**, לא אחרי שליחת הטופס
- ⚠️ **כישלון שליחה לא מפיל את ההרשמה.** תעד והתרע
- 🔴 **בסטייג׳ינג — רק לכתובות בדיקה.** אל תשלח לכתובת אמיתית

---

# §6 · יותר מ-5 משתמשים לספק

> **Yulian:** *"משרדי תיווך אולי יהיו להם יותר מ-5 משתמשים."*

## מה שמצאתי

**`services/user-org/app/services/subscription_limits.py` לא מכיר `service_provider` בכלל** — המפה מכילה `contractor` ו-`corporation` בלבד.

**כלומר כל מנגנון המושבים של R4 — `included_users`, `extra_seats_paid`, `extra_seats_granted`, שער ה-402 — לא חל על ספקים.**

## Do — לחבר, לא לבנות

1. **שורות `service_provider` ב-`subscription_plans`.** ⚠️ **ה-ENUM שם הורחב ב-077? בדוק. אם לא — מיגרציה, הוספת ערך בלבד**
2. **מסלול אחד להתחלה** — `included_users=5`, `extra_user_price_nis` כמו לקבלן, `max_users` NULL
3. **`subscription_limits` מחזיר אותם**
4. **לספק נוצרת שורת `subscriptions`** — כמו לשתי הישויות האחרות. 🔴 **בלעדיה כל R4 לא מגיע אליו**
5. 🔴 **אז שער ה-402, רכישת המושב, והענקת האדמין עובדים בלי שורת קוד נוספת** — **זו כל הפואנטה. אם אתה כותב לוגיקת מושבים חדשה לספק, עצור: עשית טעות**

**דווח: כמה קוד חדש נדרש. אם זה יותר ממיגרציה ומפה — נמק.**

---

# Acceptance

## §1 + §2

- [ ] 🔴 **אין טבלת מחירים חדשה.** `git diff` על המיגרציות — מוכיח שהשתמשת ב-`marketplace_subscription_tiers`
- [ ] בורר מסלול אחרי הקטגוריה, עם מחיר ומכסה. צילום
- [ ] בחירה → **`marketplace_subscriptions` נוצרה עם ה-tier ו-`category_code` הנכונים.** הדבק `SELECT`
- [ ] **המכסה נאכפת** — מעבר ל-`slot_count` נחסם. הדגם
- [ ] **קידום ההשקה מוחל: `price_paid=0` + `expires_at` + ההערה.** הדבק
- [ ] 🔴 **המחיר האמיתי מוצג למרות הקידום.** צילום
- [ ] **קטגוריה ללא מסלולים → ההרשמה עוברת + התרעה לאדמין.** הדגם
- [ ] תאריך סיום הקידום **לא קשיח בקוד.** `grep`

## §3

- [ ] הרשמת ספק → **ההודעה אומרת ״ספק שירותים״.** צילום SMS
- [ ] **אותו דבר באימייל** (תבנית 046)
- [ ] 🔴 **רשימת כל המפרסמים שמרכיבים `orgTypeLabel`**
- [ ] **הכרעה: האם בכלל שולחים על ספק** — נמק

## §4

- [ ] אחרי OTP → **`/provider/marketplace/new` עם הקטגוריה מוגדרת.** צילום
- [ ] 🔴 **הטלפון ממולא מראש. אין הקלדה חוזרת.** צילום
- [ ] שאר השדות ממולאים. **רשימה של מה מולא**
- [ ] ״מאוחר יותר״ → דשבורד

## §5

- [ ] אימייל **חובה**, בלקוח ובשרת. שני צילומים
- [ ] **המייל נשלח אחרי OTP.** הדבק את גוף ההודעה
- [ ] ארבעת הקישורים עובדים. **לחץ על כל אחד**
- [ ] תבנית ב-`notification_templates`, לא בקוד. `grep`
- [ ] **כישלון שליחה → ההרשמה שרדה.** הדגם
- [ ] **רק כתובות בדיקה**

## §6

- [ ] `service_provider` ב-`subscription_plans`. `SELECT` — הדבק
- [ ] `subscription_limits` מחזיר `included_users=5`
- [ ] **שורת `subscriptions` נוצרת לספק.** הדבק
- [ ] 🔴 **ספק מזמין משתמש שישי → 402 `seat_upgrade_required` עם מחיר.** הדבק
- [ ] **רכישת מושב עובדת** (fake). `extra_seats_paid` עלה
- [ ] **הענקת אדמין עובדת.** `extra_seats_granted` עלה
- [ ] 🔴 **חידוש: מסלול + `paid` בלבד. `granted` לא נגבה.** הדבק את החישוב
- [ ] 🔴 **כמה קוד חדש נכתב למושבי ספק.** אם יותר ממיגרציה ומפה — נמק

## רגרסיה

- [ ] קבלן ותאגיד — מושבים, חידוש, חשבונית: ללא שינוי
- [ ] R9: `comped` · דילוג ללא אמצעי תשלום
- [ ] R5: קטגוריה · דשבורד · הרשאות ספק
- [ ] U3 · U6 · U7 · U8 · `--suite all`
- [ ] `npm run build` · `npm test`
- [ ] 🔴 `git rev-list --left-right --count origin/staging...origin/pivot/v2` → `0 0`

## דווח

1. 🔴 **הוכחה שלא נוצרה טבלת מחירים חדשה**
2. **`SELECT` של `marketplace_subscriptions` של ספק חדש**
3. **רשימת מפרסמי `orgTypeLabel`**
4. **מה מולא מראש בטופס המודעה**
5. **גוף מייל הקליטה**
6. **כמה קוד חדש למושבי ספק**
7. `git diff -w --stat`

## Guardrails

`git tag pre-r10`. **סטייג׳ינג בלבד. שני הענפים, ובדוק `rev-list`.**

🔴 **אל תבנה טבלת מחירים חדשה.** `marketplace_subscription_tiers` קיימת וזרועה.

🔴 **אל תמזג `subscriptions` עם `marketplace_subscriptions`.** חשבון מול מלאי פרסומי.

🔴 **מסלול חינם לא מופיע בבורר.** קידום מוחל אוטומטית, המחיר האמיתי נראה.

🔴 **אל תכתוב לוגיקת מושבים חדשה לספק.** חבר את R4.

🔴 **תאריכי קידום ומחירים — אדמין, לא קוד.**

🔴 **מיילים בסטייג׳ינג — כתובות בדיקה בלבד.**

🔴 **`PAYMENT_FAKE_MODE`. אל תשלים רכישה אמיתית.**

🔴 **`git add` לפי שם קובץ. לעולם לא `-A`.** **וכולל `docs/cc-prompts/*.md`.**

**אל תיגע** ב-`viewer_scope_wheres` · חשיפה · `query_rewriter.py` · הלוגיקה של R4 ו-R9.
