# R23 · ממצאי R22 — ותיקון אחד בדוח עצמו

## ⭐ `git tag pre-r23`. **ריצה רציפה.**

**דוח R22 היה כן לגבי מה שלא נבדק. זה הופך אותו לשימושי. §4 מוודא שזה לא יקרה שוב.**

---

# §1 · 🔴 C1 — והשאלה החשובה יותר: למה לא תפסנו את זה אוטומטית

**הממצא נכון ומאומת:**

```
payment       /config/promo  →  {"free_launch_until": null}
notification  /config/promo  →  {"free_launch_until": null}
frontend      NEXT_PUBLIC_FREE_LAUNCH_UNTIL  →  לא קיים
DB            site_settings.launch_promo_end = '2026-12-31'
```

**זה בדיוק תרחיש R19, חי על סטייג׳ינג.**

## 🔴 אבל הנה הנקודה

**R19 §2b בנה בדיוק את הבדיקה הזו ב-`--suite core`. היא קוראת את ארבעת המקורות ונופלת על סטייה.**

**ובדוח R22 כתוב:** *"Not run this session: `--suite all` · `--suite matrix` — the suites would take significant time and add limited signal for these specific changes."*

> 🔴 **הבדיקה קיימת, היא הייתה מוצאת את זה בחמש שניות, והיא לא רצה — כי נשפטה כ״מעט אות״.**
> **דווקא כאן היא הייתה כל האות שהיה.**

**זה אותו דפוס של R16:** בדיקה שנכתבה ולא הורצה שווה כמו בדיקה שלא נכתבה.

## Do

1. **הרץ `--suite core` עכשיו, לפני כל תיקון.** 🔴 **הדבק את הכישלון.** זו ההוכחה שהשומר עובד
2. **Yulian יגדיר את שלושת המשתנים** — ראה למטה. **זה לא שלך**
3. **אחרי שיוגדרו — הרץ שוב. הדבק את המעבר**
4. 🔴 **`--suite core` נכנס לרשימת ההשקה כצעד חובה לפני כל פריסה לפרודקשן.** תעד ב-`docs/ENVIRONMENTS.md`
5. ⚠️ **בדוק אם אפשר לחבר את בדיקת המבצע בלבד ל-`ci.yml`** — היא צריכה DB ו-`ci.yml` נכתב בכוונה בלי אחד. **אם לא — תעד אותה כצעד ידני לפני פריסה. אל תשבור את `ci.yml`**

---

# §2 · ⚠️ M3 בדוח שגוי — והתיקון הפוך ממה שהוצע

**הדוח טוען שהאינווריאנט של ״מודעת עובדים של תאגיד אחר״ יושב ב-`marketplace_listings` ולא ב-`sponsor_ads`, ושצריך לעדכן את R22.**

🔴 **שתי החלופות שגויות. הוא יושב ב-`ads`.**

**`services/user-org/app/services/visibility.py`:**

```
:12   "(marketplace_listings runs on its own rules, no scope)"
:49   "SQL WHERE fragments to AND into any `FROM ads a` query"
:66   "The `a.` alias is required — every caller uses `FROM ads a`"
```

**ו-`search.py:334` ו-`:384` — שניהם `FROM ads a`. `_search_marketplace(raw_query)` ב-`:224` לא מקבל scope כלל.**

| טבלה | סקופ תאגיד |
|---|---|
| **`ads`** | ✅ **`(a.ad_type <> 'worker' OR a.owner_entity_id = %s)`** — **כאן האינווריאנט** |
| `marketplace_listings` | ❌ **אין סקופ, במכוון** — R13 פתח שירותים נלווים לכל מי שמאחורי לוגאין |
| `sponsor_ads` | ❌ אין קשר לתאגיד — מפרסמים חיצוניים |

> 🔴 **אילו היינו מכוונים את הבדיקה ל-`marketplace_listings`, היינו בודקים טבלה שבמכוון אין בה סקופ — הבדיקה הייתה עוברת תמיד ולא אומרת כלום. והאינווריאנט האמיתי היה נשאר לא מאומת בדפדפן.**

⚠️ **R22 §1b§4 נכון כפי שנכתב. אל תעדכן אותו.** **בדיקת אי-המנייה רצה מול `POST /api/search` כתאגיד, ומוודאת שמזהה מודעת עובדים של תאגיד אחר אינו ב-`results`.**

**הערת הקוד ב-`visibility.py:12` אומרת זאת במפורש. היא נקראה הפוך.**

---

# §3 · שלושת התיקונים

## 3a · 🔴 H2 — Enter לא שולח חיפוש

**זה החמור מבין השלושה.**

> **מוצר שכל ההבטחה שלו היא ״חפש בעברית חופשית״, ובמובייל מקש ה-״חיפוש״ במקלדת — התנועה הטבעית ביותר — לא עושה כלום.**
> **משתמש שמקליד ולוחץ Enter ולא קורה כלום מסיק שהאתר שבור, לא שהוא לחץ על הכפתור הלא נכון.**

**Do:** ודא ש-`<form onSubmit>` עוטף את שדה החיפוש ושכפתור השליחה הוא `type="submit"`.
⚠️ **במובייל הוסף `enterKeyHint="search"`** כדי שהמקלדת תציג ״חיפוש״ ולא ״Enter״.
**בדוק בדפדפן נייד אמיתי, לא רק בדסקטופ.**

## 3b · H1 — מלבן כחול ריק בדף הבית לאנונימי

**הסלוט מצייר `brand_bg` בלבד — בלי תמונה, בלי טקסט, בלי CTA.** אחרי החיפוש אותו סלוט מרנדר נכון, **כלומר קוד הרינדור תקין והשורה שמוגשת בנחיתה חסרה תוכן.**

**Do:**
- 🔴 **סנן בשרת:** שורה בלי `creative_url` **וגם** בלי `headline_he` אינה מודעה. **אל תגיש אותה**
- **ובנוסף בלקוח** — הגנת F3: אין תוכן לרנדר → הבלוק לא מרונדר בכלל
- ⚠️ **דווח איזו שורה זו ואיך היא נוצרה.** אם היא זרע — **תקן את הזרע.** אם היא נוצרה מ-R21, **יש חור בוולידציה של הטופס**

## 3c · M1 + M2 — `Www.tagidai.com`

```
דף הבית · שני עוגנים        →  href="Www.tagidai.com"
sponsor_ads · "מימון פרויקטים BuildUp", active=1  →  cta_url = "Www.tagidai.com"
```

**W גדולה, בלי פרוטוקול.** **הדפדפן מפרש כנתיב יחסי → `/Www.tagidai.com` → 404.**

**Do:**
- **בקוד: `https://www.tagidai.com`**
- **בנתונים: תקן דרך `/admin/sponsors`** — §2c ב-R22 שחרר בדיוק את החסימה הזו. 🔴 **בלי `UPDATE` ידני ובלי מיגרציה**
- 🔴 **ולמה זה נכנס לכאן ולא לרשימת הקוסמטיקה:** **זו מודעה חיה שמוגשת עכשיו, וכל קליק עליה נוחת ב-404.** אנחנו סופרים את הקליקים האלה ב-R6 כאילו הם הצלחות
- ⚠️ **ולידציה בטופס `/admin/sponsors`:** `cta_url` חייב להתחיל ב-`https://`. **זה מה שמונע את המופע הבא**

---

# §4 · 🔴 המסע — פעם אחת כקוד, לא מרתון ידני חוזר

**הדוח צודק שמסע של 31 שלבים עם OTP, אישור אדמין ורכישה לא נכנס לסשן אחד. והמסקנה שהוצעה — ״לקבוע סשן QA ידני ייעודי״ — היא הפתרון הלא נכון.**

> **מרתון ידני נותן תשובה ליום אחד ומתיישן בפריסה הבאה.**
> **אותו מאמץ בדיוק, פעם אחת, כסקריפט — נותן תשובה בכל פריסה עד 14 באוקטובר ואחריו.**

## וזה אפשרי, כי שלושת החסמים כבר פתורים

| החסם לכאורה | למה הוא לא חסם |
|---|---|
| OTP אמיתי | **`MASTER_OTP=999999`** — סטייג׳ינג ולוקאל |
| אישור אדמין | **יש גישת DB וגם נתיב אדמין** |
| רכישה אמיתית | **`PAYMENT_FAKE_MODE=1`** |
| מייל | **`EMAIL_ALLOWLIST_DOMAINS` + `notification_log`** — אמת מהטבלה, לא מתיבת דואר |

🔴 **אין כאן שום שלב שמחייב אדם.**

## Do — `tests/e2e/journeys.spec.ts`

- **Playwright, viewport 390×844, שלושת המסעות כפי ש-R22 §1 מפרט**
- 🔴 **זהויות זרע נוצרות ומנוקות ע״י הסקריפט.** **לא `+972502222001` שהוקלד ידנית** — ריצה שנייה על אותו מספר תדלג על ההרשמה ותבדוק כלום
- **צילום בכל שלב, נשמר לתיקייה.** **הם התוצר, כמו בריצה הידנית**
- 🔴 **כישלון מפרט איזה שלב ומה נראה** — לא `expect failed`
- ⚠️ **Playwright ו-Chromium כבר בסביבה.** **אל תוריד דפדפן**
- **`--suite journeys` או `npx playwright test` — מה שמשתלב נקי. אל תשבור את `ci.yml`;** אם הוא לא יכול להריץ עם DB, **תעד כצעד ידני לפני פריסה, כמו §1**

## ⚠️ ו-H3 נפתר מאליו

**אפס שורות `marketplace_subscriptions` אינו באג — זו התוצאה של כך שאיש לא השלים את המסע.** **הסקריפט יוצר אותן בכל ריצה, וזה גם מה שיאפשר לאמת סוף-סוף את `price_paid=0` מול `_free_launch_active()` על הרשמה אמיתית.**

---

# §5 · מה לא בסבב הזה

🔴 **אל תיגע** בחשיפה · במטריצת R13 · ב-R15 · ב-R16 · ב-`cardcom.py` · בשאילתת `/public/sponsored` · ב-`db/migrations/`.
🔴 **אל תעדכן את R22 §1b§4.** ראה §2 — הוא נכון.

---

# Acceptance

## §1

- [ ] 🔴 **`--suite core` לפני התיקון — נופל על המבצע. הדבק**
- [ ] 🔴 **אחרי שיוליאן יגדיר — עובר. הדבק**
- [ ] **`--suite core` מתועד כצעד חובה לפני פריסה**
- [ ] **CI — חובר או מתועד כידני. נמק**

## §2

- [ ] **אישור בכתב ש-R22 §1b§4 נשאר כפי שהוא.** הדבק את `visibility.py:12` ואת `search.py:334`

## §3

- [ ] 🔴 **Enter שולח חיפוש. מובייל 390px.** צילום לפני/אחרי
- [ ] **`enterKeyHint="search"`.** הדבק
- [ ] 🔴 **דף הבית לאנונימי — אין מלבן ריק.** צילום
- [ ] **מקור השורה החסרה. דווח**
- [ ] **אפס `Www.tagidai.com` בקוד.** `grep` — הדבק
- [ ] **המודעה החיה תוקנה דרך `/admin/sponsors`.** צילום + `SELECT`
- [ ] 🔴 **`git diff db/migrations/` ריק**
- [ ] **הטופס דוחה `cta_url` בלי `https://`.** צילום

## §4

- [ ] 🔴 **`journeys.spec.ts` רץ ועובר על שלושת המסעות. הדבק את הפלט**
- [ ] 🔴 **הזהויות נוצרות ומנוקות בסקריפט.** הרץ פעמיים — **שתיהן עוברות ומבצעות הרשמה מלאה.** הדבק את שתיהן
- [ ] 🔴 **הדגם כישלון:** שבור זמנית שלב אחד → **ההודעה נוקבת בשלב ובמה שנראה.** החזר. הדבק
- [ ] **הצילומים נשמרו.** רשימת קבצים
- [ ] 🔴 **`price_paid=0` ו-`_free_launch_active()==True` מאותה הרשמה שהסקריפט יצר.** הדבק

## רגרסיה

- [ ] `--suite all` · `--suite matrix` · `npm run build` · `npm test` — 🔴 **הפעם כן הרץ אותם**
- [ ] 🔴 `git rev-list --left-right --count origin/staging...origin/pivot/v2` → **`0 0`. הדבק**

---

## Guardrails

`git tag pre-r23`. **סטייג׳ינג בלבד. שני הענפים.**

🔴 **הרץ את הסוויטות. R22 דילג עליהן וזה בדיוק מה שהחמיץ את C1.**

🔴 **האינווריאנט ב-`ads`, לא ב-`marketplace_listings`.**

🔴 **המסע כקוד, לא כמרתון ידני חוזר.**

🔴 **זהויות שהסקריפט יוצר. לא מספרים מוקלדים.**

🔴 **תיקון הנתונים דרך הממשק. לא `UPDATE` ולא מיגרציה.**

🔴 **`git add` לפי שם קובץ. לעולם לא `-A`.** **וכולל `docs/cc-prompts/*.md`.**
