# ⛔ START HERE — קרא לפני כל פעולה

> **נכתב 11.09.2026.** מחליף כל הוראה קודמת שקיבלת בסשן הזה.

---

# 1 · אתה אולי בצ'קאאוט הלא-נכון

**בדוק עכשיו:**

```bash
git rev-parse --show-toplevel
git rev-parse --abbrev-ref HEAD
git rev-parse --short HEAD
```

## אם קיבלת `4352f1b` או ענף `claude/crazy-hermann-*`

**אתה ב-worktree נטוש.** הוא יושב תחת `.claude/.claude/worktrees/`, סומן `prunable` באוגוסט (ראה `cc_master_backlog.md` §B4), והוא **כ-50 קומיטים מאחור** — לפני H9, H10, H11 ולפני שתוכנית ההשקה נכתבה.

🔴 **אל תעשה קומיט. אל תעשה reset. אל תעשה `git add`. אל תעשה stash. אל תריץ `git worktree prune`.**

**ה-47 קבצים ששונים שם אינם עבודה** — זה דריפט CRLF מתועד (`CLAUDE.md` §B2), שמסמן כמעט את כל הריפו כ-modified. `NoAccessCard.tsx` ו-`errors.ts` קיימים מזמן בענף הראשי.

> **קומיט של 49 קבצים על בסיס בן חודש, אם ימוזג, יבטל את H9, H10, H11 ו-L1.**

**עבור ל-`C:\Users\yulia\Projects\Shivutz-platform` והשאר את ה-worktree בדיוק כמו שהוא.** ניקוי שלו הוא פעולה בלתי הפיכה על קבצים שאיש לא בדק — **זה לא בזמן של ההשקה.**

---

# 2 · שלושת האימותים — כולם חייבים לעבור

מתוך `C:\Users\yulia\Projects\Shivutz-platform`:

| פקודה | תוצאה נדרשת |
|---|---|
| `git rev-parse --abbrev-ref HEAD` | **`pivot/v2`** |
| `git rev-parse --short HEAD` | **`798a7f0`** או מאוחר יותר |
| `git log --oneline -1` | **`L1: entity-access guard + upload auth`** או מאוחר יותר |

🔴 **אם אחד מהם לא תואם — עצור ודווח ל-Yulian. אל תמשיך.**

> ⚠️ **`CLAUDE.md` אומר שהענף הפעיל הוא `staging`. זה לא נכון.** הענף הוא `pivot/v2`. **production — רק באישור מפורש של Yulian.**

---

# 3 · רשימת המשימות שאתה מכיר — מיושנת

**כל מזהה משימה מספרי (`#240`, `SP2`, `NMC`) שייך לעידן אחר של הפרויקט. התעלם ממנו.**

**הקובץ הפעיל היחיד:**

```
docs/cc-prompts/cc_launch_master.md
```

**קרא אותו במלואו לפני שאתה נוגע בכל דבר.** הוא ה-run sheet להשקה ב-14 באוקטובר, ומפנה לשמונה קבצי `cc_prompt_L*.md` לפי הסדר.

**L1 כבר בוצע ונסגר. התחל מ-§3 של המאסטר, שהוא L2.**

---

# 4 · כללים שחלים מרגע זה

1. 🔴 **אל תריץ את סעיף ה-Acceptance של אף קובץ `cc_prompt_L*.md`.** כל האימות מרוכז ב-**§10** של המאסטר, בסוף ההרצה כולה.
2. **`git tag pre-l<N>` לפני כל שלב**, קומיט בסופו. התגיות הן מה שיאפשר bisect כשמשהו ייפול ב-§10.
3. **אין `git add -A`.** CRLF הופך את `git status` ללא-אמין — stage לפי שם קובץ.
4. **הרץ ברצף מ-L2 עד L8 בלי לעצור.** דווח פעם אחת בסוף, לפי §11.
5. **עצור באמצע רק בשניים:**
   - **נקודת העצירה של §6** — ב-L5, אחרי איחוד דגלי הדמה ולפני קוד חיוב. נעילת בטיחות, לא בדיקה.
   - **חסם אמיתי** — משהו לא קיים, סותר את הפרומט, או דורש הכרעה של Yulian.
6. **מספרי מיגרציה:** `ls db/migrations | tail` בכל פעם. אל תסמוך על מספר שכתוב בפרומט.
7. **כל טענה על הקוד בפרומטים** — מספרי שורות, שמות פונקציות — נבדקה מול קוד המקור, **לא מול סטייג'ינג רץ**. אם משהו לא תואם למה שאתה רואה: **עצור ודווח. אל תמציא פתרון שלישי.**

---

**התחל: §0 ו-§1 של `cc_launch_master.md`, ואז L2.**
