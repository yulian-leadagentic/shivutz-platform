# R8 §1b · ההכרעה על ה-worktree — ולמה ״לזרוק״ לא מאבד כלום

## ⭐ נספח ל-`cc_prompt_R8_cleanup.md` §1. **צדקת שעצרת ודיווחת.**

---

# §1 · ההכרעה

```
4352f1b  10.08  Entity switch: /select-entity works for authed users (no OTP loop)
2e67b7f  10.08  M2: onboarding top-align on mobile + G1 button lock globally
45e7189  10.08  M1: landing 390px reflow + G1 button color lock
```

## 🔴 אל תעשה cherry-pick. הסר את ה-worktree.

**הראיה — כמה השתנו הקבצים שהם נוגעים בהם מאז 10.08:**

| קובץ | קומיטים מאז | הדיף בקומיט הישן |
|---|---|---|
| `app/page.tsx` | **50** | 204 שורות |
| `select-entity/page.tsx` | 5 | 59 |
| `register/contractor/page.tsx` | 5 | 133 |
| `ui/button.tsx` | 2 | 10 |

> **`page.tsx` עבר U5 · U6 · U8 · U10 · R12 · R13 · R16 · R6 מאז.**
> **הסיכון האמיתי אינו הקונפליקט — הוא פתרון קונפליקט שגוי שמחזיר בשקט קוד מאוגוסט על עבודה מהשבוע.**
> **24 יום להשקה. לא שווה.**

---

# §2 · 🔴 אבל ״לזרוק״ כאן לא מאבד ולו קומיט אחד

**זו הנקודה שחשוב להבין לפני שנוגעים:**

```
ה-worktree   =  עותק עבודה בנפח 785MB על הדיסק
הענף         =  שלושת הקומיטים
```

**`git worktree prune` מסיר את עותק העבודה בלבד. הענף `claude/crazy-hermann-09dfbb` ממשיך להחזיק את שלושת הקומיטים, לנצח, בעלות אפס.**

**אנחנו מוחקים 785MB של כפילות ומשאירים את ההיסטוריה שלמה.**

## Do — בסדר הזה

1. 🔴 **תג ארכיון קודם.** ענף נמחק בטעות ע״י סקריפט ניקיון. תג לא:
   ```
   git tag archive/mobile-entity-10-08 4352f1b
   ```
   **הדבק `git tag -l 'archive/*'`**
2. **`git worktree prune -v`** — הדבק את הפלט
3. **אם התיקייה נשארה — `_to_delete/`.** 🔴 **אל תמחק בעצמך**
4. 🔴 **אל תמחק את הענף `claude/crazy-hermann-09dfbb`.** הוא רשת הביטחון. **אחרי 14.10 נחליט**
5. **אימות:** `git worktree list` → שורה אחת · `du -sh .` לפני/אחרי · **`grep -rn "flooring"` → אפס תוצאות מ-`.claude/`**

---

# §3 · 🔴 מה כן צריך — לאמת התנהגות, לא דיף

**שלושת הקומיטים תיארו ארבע התנהגויות. אנחנו לא ממזגים קוד — אנחנו בודקים אם ההתנהגות קיימת היום.**

**בדפדפן על סטייג׳ינג. צילום לכל אחת:**

| # | מה לבדוק | מקור |
|---|---|---|
| 1 | **`/select-entity` למשתמש כבר מחובר — נטען בלי לולאת OTP** | `4352f1b` |
| 2 | **דף הבית ב-390px — אין גלילה אופקית, אין חיתוך** | `45e7189` |
| 3 | **`/register/contractor` ו-`/register/corporation` במובייל — יישור לראש המסך, לא מרכוז אנכי** | `2e67b7f` |
| 4 | **צבע כפתור עקבי** — `bg-brand-600` ב-`ui/button.tsx`, בלי דריסות מקומיות | `45e7189` · `2e67b7f` |

🔴 **פריט 1 הוא החשוב.** השלושה האחרים הם ליטוש; **לולאת OTP ב-`/select-entity` היא משתמש תקוע בלי דרך החוצה.** ⚠️ **בדוק גם משתמש עם שתי ישויות ומעלה — זה המסלול שבשבילו המסך קיים.**

## אם התנהגות חסרה

🔴 **תקן אותה בקוד של היום. אל תמשוך את הקומיט מאוגוסט.**
**התג מ-§2 שם כדי לקרוא ממנו, לא כדי למזג ממנו:** `git show 45e7189 -- services/frontend/src/app/page.tsx`

**דווח לפני שאתה מתקן** — ייתכן שהתנהגות ״חסרה״ היא בכוונה, שינוי עיצוב מאוחר יותר.

---

# Acceptance

- [ ] 🔴 **התג `archive/mobile-entity-10-08` קיים ומצביע ל-`4352f1b`.** הדבק
- [ ] 🔴 **`git worktree list` → שורה אחת.** הדבק
- [ ] 🔴 **`grep -rn "flooring"` לפני ואחרי — אפס תוצאות מ-`.claude/`.** הדבק את שניהם
- [ ] **`du -sh .` לפני ואחרי.** ~785MB
- [ ] 🔴 **הענף `claude/crazy-hermann-09dfbb` עדיין קיים.** `git branch -a | grep hermann` — הדבק
- [ ] 🔴 **ארבע ההתנהגויות — ארבעה צילומים.** **פריט 1 עם משתמש בעל שתי ישויות**
- [ ] **מה שחסר — דווח לפני תיקון**
- [ ] **אפס שינוי קוד מוצר** אלא אם דיווחת קודם. `git diff --stat`
- [ ] `--suite all` · `--suite matrix` · `npm run build` · `npm test`
- [ ] 🔴 `git rev-list --left-right --count origin/staging...origin/pivot/v2` → **`0 0`. הדבק**

## דווח

1. 🔴 **ה-`grep` לפני ואחרי**
2. **ארבעת הצילומים + מה עבר ומה לא**
3. **`du -sh` לפני ואחרי**
4. `git diff -w --stat`

---

## Guardrails

`git tag pre-r8b`. **סטייג׳ינג בלבד. שני הענפים.**

🔴 **אפס cherry-pick. אפס merge מהענף הישן.**

🔴 **התג לפני ה-prune.** בסדר הזה.

🔴 **אל תמחק את הענף.**

🔴 **אל תמחק תיקיות — `_to_delete/`.**

🔴 **אפס נרמול שורות** — R8 §4 עדיין בתוקף.

🔴 **`git add` לפי שם קובץ. לעולם לא `-A`.**
