# דף הרצה · 24.09 — פרומפט מרכז ל-CC

## ⭐ ריצה רציפה. אל תעצור לאישור ויזואלי. צלם, המשך, דווח בסוף.

**ההכרעות הפתוחות סגורות בתחתית הקובץ. אין למה לחכות.**

---

# 🔴 0 · שמות של חברות אמיתיות — לפני כל דבר אחר

**בדיווח שלך מ-24.09:** *״Rows served silently via `placements IS NULL`: 2 … `בראנד אספקה טכנית` and **`איילון חברה לביטוח`**״*

> 🔴 **איילון היא חברת ביטוח ישראלית אמיתית.** **אנחנו מגישים את שמה, פעיל, על אתר ציבורי, כאילו היא מפרסמת אצלנו.**
> ⚠️ **בסטייג׳ינג זה מביך. בפרודקשן זו חשיפה משפטית — שימוש בשם של גוף קיים כלקוח שאינו לקוח.**
> **אותה משפחה כמו `buildup.co.il` ב-R24 §2, וחמורה יותר.**

## Do

- 🔴 **סרוק `sponsor_ads` · `marketplace_listings` · `leads` לשמות של גופים קיימים בישראל.** **דווח רשימה מלאה עם `id`, `advertiser_name`/`title`, `active`, `is_seed`**
- 🔴 **דווח ואל תפעל.** **Yulian מחליט מה מושבת ומה מנוסח מחדש**
- **כל שורת `is_seed` שנשארת חייבת שם בדוי מובהק** — ״חברה לדוגמה״, ״ספק הדגמה״. **לא שם של גוף קיים, ולא וריאציה שלו**
- **״אפס `is_seed` פעיל בפרודקשן״ — פריט חוסם ברשימת ההשקה**

---

# 🔴 1 · אל תריץ `DELETE`. אל תריץ `UPDATE`

**בדיווח שלך סירבת נכון לתקן שורת `cta_url` ב-`UPDATE`** — *״§27 says fix it through the admin screen״*. ✅
**ובפסקה הבאה הצעת שלוש `DELETE` דרך ה-proxy.** 🔴 **אותו כלל, ביתר שאת: `UPDATE` הפיך, `DELETE` לא.**

- 🔴 **אפס `DELETE`, אפס `UPDATE`, לא דרך ה-proxy ולא בשום דרך**
- **המטרה היא שמבקר לא יראה נתוני דמה.** **`active=0` משיג בדיוק את זה, הפיך בקליק, ומשאיר את ההדגמה לסטייג׳ינג — שבו אנחנו עובדים עוד שבועיים**
- **ה-`DELETE` שייך למעבר לפרודקשן. לא עכשיו**
- **הכן ל-Yulian רשימה מדויקת:** `id` · שם · מה ללחוץ ובאיזה מסך. **הוא מבצע**

---

# 2 · §29 — המדידות שלא בוצעו

**הדיפלוי נחת. הרץ ודווח:**

- [ ] **הרייל `300×600` ב-1920 וב-1440.** **הדבק `getBoundingClientRect()` של שניהם**
- [ ] **מבחן הגלילה ב-`/marketplace` שוב אחרי שינוי ה-aspect**
- [ ] **1280 ו-390 — הרייל לא מרונדר**
- [ ] **הקרוסלה `640×360` מהקטלוג (§30d)**

---

# 3 · §31 — ארבעה, והראשון הוא באג אמיתי

**מדדתי מול סטייג׳ינג. השרת נקי:**

```
POST /api/search {"query":"כלי"}
total 0 · results [] · near_matches [] · marketplace_matches 3
primary_section "marketplace"
```

🔴 **והמסך מציג ״14 מודעות״ ומלאי עובדים מלא. הבאג כולו בפרונט.**

- 🔴 **אתר מאיפה ה-14 ודווח לפני שאתה מתקן.** **חשד: `page.tsx:351` — `/ads/public/recent?limit=12`**
- 🔴 **`total === 0` אינו הזמנה להציג מלאי**
- 🔴 **הצ׳יפ מריץ חיפוש מיד** — **R28 §3 התהפך. Yulian השתמש ואמר שזה נקרא כתקלה**
- 🔴 **״זיהינו שאתה מחפש X״ מוצג רק כששדה אמיתי חולץ** — **`filters.ad_type` מוגדר `worker` כברירת מחדל, אז ההודעה מצהירה על הבנה שלא התרחשה**
- **`primary_section: marketplace` → השירותים והממומן הרלוונטי למעלה**
- ⚠️ **`פיגומים` מסווג `painting`. דווח בלבד — `query_rewriter.py` מחוץ להיקף**

---

# 4 · סריקת fallbacks קשיחים

**`SponsorSlot` נפל ל-`1200/628` — צורה שאינה קיימת באף סלוט בקטלוג.** **זה השלישי מאותו סוג אחרי §17b ו-§25.**

🔴 **`grep` על `628` · `1080` · `8/1` · `1200` בכל הפרונט ובשרת.** **כל מידה שאינה נקראת מ-`SPONSOR_SIZES` — דווח.**

---

# 5 · ואז גל 1 של R30

```
§13  כרטיסי ״פרסום חדש״ — ?ad= בלי קורא
§7   ״הצטרף עכשיו״ · ו-/how-it-works בלי CTA
§12a ה-0 שמרונדר בכל מודעה לא מרוהטת
§5   ״נקה״ שלא מנקה ב-/admin/ads
§14a ספק שירותים לא יכול לפתוח פנייה — ENUM חסר
```

---

# ההכרעות שחיכית להן — סגורות

## §31b · שאילתה בלי התאמות

**מצב ריק מנוסח. לא מלאי.**

> ״לא מצאנו מודעות עובדים שמתאימות. אלה שירותים נלווים רלוונטיים:״ + מה שהשרת החזיר.

🔴 **מלאי שמוצג כתוצאה הוא שקר קטן שהורס את האמון בחיפוש כולו.** **קבלן שקיבל 14 תוצאות לא רלוונטיות פעם אחת לא יסמוך על הבאה.**

## §4 · תמחור — מאושר

**לפני מע״מ:**

| סלוט | רגיל | בלעדיות |
|---|---|---|
| לידרבורד | ₪750 | ₪1,500 |
| בילבורד | ₪500 | ₪1,000 |
| רייל צדדי | ₪400 | ₪800 |
| באנר קטגוריה | ₪350 | ₪700 |
| ממוקד בתוצאות | ₪300 | ₪600 |
| כרטיס קרוסלה | ₪250 | ₪500 |

- **בלעדיות = תוספת 100%, על הקטגוריה בכל האתר, לרבעון**
- **שלושה חודשים מראש = 15% הנחה**
- ⚠️ **אלה ברירות מחדל למדף.** **`sponsor_ads_slots.price_nis` לכל שורה + `note` לתיעוד — Yulian מתמחר במשא ומתן**
- **מקור:** `claude/ad_slot_pricing_recommendation.md`

---

# Guardrails

⭐ **`git tag pre-0924`. סטייג׳ינג בלבד. שני הענפים. ריצה רציפה.**

🔴 **אפס `DELETE`. אפס `UPDATE`. תיקון נתונים דרך מסך האדמין בלבד.**

🔴 **אל תיגע** ב-`query_rewriter.py` · בחשיפה · במטריצת R13 · ב-R15 · ב-`cardcom.py` · במיגרציה שרצה.

🔴 **`git add` לפי שם קובץ. לעולם לא `-A`.**

🔴 **אחרי כל שלב:** `npm run build` · `npm test` · `--suite all` · `--suite matrix` · `git rev-list --left-right --count origin/staging...origin/pivot/v2` → `0 0`.
