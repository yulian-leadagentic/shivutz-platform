# R12 · סבב ידני 19.09 — חמישה ממצאים

## ⭐ `git tag pre-r12`. **§1 ראשון. הוא לא באג תצוגה.**

---

# §1 · 🔴 החיפוש — המונה, מצב הריק, והכיוונון לפי ישות

## 1a · מה שאובחן, ומה שהאבחון פספס

**האבחון שלך נכון: הבקאנד תקין.** `viewer_scope_wheres` מחיל את H12 — תאגיד רואה מודעות עובדים של עצמו בלבד. תאגיד שחיפש `רצפים` ואין לו מודעת ריצוף מקבל אפס, **וזו התנהגות מוכוונת מ-10.09.**

🔴 **אבל שלוש החלופות שהצעת מניחות שהחיפוש הוא חיפוש עובדים. הוא כבר לא.**

**U6 §2 הפך אותו לחיפוש מאוחד — הוא מחזיר גם `marketplace_matches`.** **ותאגיד הוא בדיוק הקהל שצריך לחפש דיור, הסעות וביטוח לעובדיו** — זה האפיק הרביעי בתוכנית העסקית.

| החלופה שהצעת | למה לא |
|---|---|
| **1 · להסתיר את החיפוש מתאגידים** | סוגר לתאגיד את השירותים הנלווים — הערוץ שנבנה עבורו |
| **3 · לפתוח לתאגידים את כל מודעות העובדים** | שובר את כלל אי-המנייה של H12. החלטת אבטחה, לא נוחות |
| **2 · מצב ריק לתאגיד** | **הכיוון הנכון — אבל לא ״מסך ריק״. ראה 1c** |

## 1b · 🔴 המונה — וזה מה שהטעה את האבחון שלך

`services/frontend/src/app/page.tsx:1480-1482`

```tsx
const exact = resp.results.length;            // מודעות בלבד
const near  = resp.near_matches?.length ?? 0;
if (exact === 0 && near === 0) return 'לא נמצאו מודעות התואמות לחיפוש';
```

**`marketplace_matches` לא נספר.** זה הבאג מ-R5 §4 שלא נסגר.

> 🔴 **הטבלה שלך מראה `service_provider → total 0`, ופירשת את זה כ-`1=0` שחוסם הכל.**
> **אימתתי: `search.py:294,297,351` מחיל את `viewer_scope_wheres` על שאילתות `ads` בלבד. `_search_marketplace(raw_query)` ב-`:215` לא מקבל scope כלל.**
> **ספק כן מקבל תוצאות שירותים נלווים. אין שם באג — המונה פשוט לא ספר אותן.**
> **המונה השבור הטעה את האבחון של עצמך. זו הסיבה שהוא ראשון.**

**Do:**
- **המונה סופר את שלושת המקורות:** `results` · `near_matches` · `marketplace_matches`
- **בלוק ״לא נמצאו״ מרונדר רק כששלושתם ריקים**
- **גם `0 תוצאות` בשורת החיפוש** — אותו חישוב, לא שני מקומות

## 1c · מצב הריק — לפי ישות

**כשמקטע העובדים ריק, ההודעה תלויה במי שואל:**

| הישות | מה מוצג במקטע העובדים כשהוא ריק |
|---|---|
| **קבלן** | הנוסח הקיים — ״לא נמצאו מודעות התואמות״ + הצעות NM |
| **תאגיד** | `מודעות עובדים של תאגידים אחרים אינן מוצגות לתאגידים. אלה המודעות שלך שתואמות.` **ואם אין לו אף אחת — כפתור ל-`/corporation/ads/new/worker`** |
| **ספק** | **המקטע לא מרונדר בכלל.** ספק לא אמור לראות אותו — **לא ריק, לא מוסבר. פשוט לא שם** |

🔴 **מקטע השירותים הנלווים מרונדר לכל שלושתם כרגיל.** זה העיקר.

## 1d · ה-placeholder

**״חפש עובדים״ מטעה תאגיד וספק.** התאם לפי ישות — לתאגיד ולספק: **״חפש דיור, הסעות, ביטוח, ציוד״**.

⚠️ **לאנונימי ולקבלן — אל תיגע.** F1 הכריע את הנוסח שם.

## Acceptance §1

- [ ] 🔴 **קבלן: `קורס עברית` → `תוצאה אחת`, בלי בלוק ״לא נמצאו״.** צילום
- [ ] 🔴 **תאגיד בלי מודעת ריצוף: `רצפים` → ההסבר + כפתור פרסום, ומקטע השירותים עובד.** צילום
- [ ] **תאגיד עם מודעת ריצוף: `רצפים` → המודעה שלו.** צילום
- [ ] 🔴 **ספק: `ביטוח` → תוצאות שירותים נלווים. מקטע עובדים לא מרונדר.** צילום
- [ ] **שאילתה בלי שום התאמה, לכל שלוש הישויות → הבלוק כן מופיע.** שלושה צילומים
- [ ] **המונה תואם לסכום התוצאות בכל מקרה.** הדבק את החישוב
- [ ] 🔴 **H12 לא נגעת בו.** `git diff` על `visibility.py` — ריק
- [ ] **קבלן `pending` → 403. אנונימי → 401.** ללא שינוי

---

# §2 · הודעת ״ח.פ כבר רשום״ נדבקת

**מסך:** `/register/provider`, שלב פרטי העסק אחרי אימות טלפון.

**שחזור:** ח.פ שרשום כקבלן → השגיאה מוצגת **נכון** → **מחליפים את הח.פ → אותה הודעה נשארת.**

**מה עובד:** הבדיקה החוצה־ישויות והנוסח — R5 §2a. **הבעיה במחזור החיים של השגיאה.**

**אבחן קודם — שני באגים שונים לגמרי:**

| ב-Network | המשמעות |
|---|---|
| **אין קריאה שנייה** | הטופס לא נשלח מחדש — ולידציה או `disabled` חוסמים |
| **יש קריאה, חוזרת 200, השגיאה על המסך** | `setError` לא מאופס |

**דווח מה מצאת לפני התיקון.**

**Do:**
- **`setError('')` בתחילת כל שליחה**, ובשינוי שדה הח.פ
- 🔴 **העבר את השגיאה לרמת השדה.** `Input` תומך ב-`error` (`input.tsx:6,57,63`) ורוב המסכים לא משתמשים בו — **U8 §5 כבר סימן את זה.** שגיאה שצמודה לשדה מתאפסת יחד איתו באופן טבעי
- **`grep` שדות אחרים באותו טופס** — טלפון, אימייל. **אותה תבנית, אותו באג**

---

# §3 · חסר רווח לפני תג ה-AI בכותרת

**דף הבית:** `פלטפורמת ה[AI] לקבלנים` — ה-`ה` נדבק למסגרת.

**מ-F2.** הכותרת מפוצלת לשלושה, ו-JSX מוחק whitespace בין אלמנטים בשורות נפרדות.

- **`{' '}` מפורש**, 🔴 **לא `padding`** — זה רווח בטקסט, וקורא מסך צריך לקרוא אותו
- **בדוק גם את הצד השני**, בין התג ל-`לקבלנים`

---

# §4 · ״איך זה עובד״ לא נגיש במובייל

**U5 §4 בנה את העמוד. U6 §7 מחק את המקטע המתקפל מדף הבית.** הניווט נשאר הדרך היחידה — **ובמובייל הוא לא שם, או קבור בתפריט.**

**אבחן:** `LandingNav.tsx` · `MobileNavDrawer.tsx` · `LandingFooter.tsx:38`. **דווח איפה הקישור כן קיים היום.**

🔴 **אבל אל תסתפק בהוספה לתפריט המגירה.** **מבקר במובייל שלא מבין מה האתר עושה נוטש תוך שניות, והוא לא יפתח המבורגר כדי לברר.**

**Do: קישור טקסטואלי נראה מתחת לשדה החיפוש** — בלי גלילה ובלי תפריט. ⚠️ **קטן ומשני. אסור שיתחרה בשדה החיפוש** — F1 הכריע שהחיפוש הוא הדבר הראשון.

**וגם בתפריט**, לשלמות.

---

# §5 · אין סמל נגישות באף עמוד

**העמוד `/accessibility` נבנה ב-L6 ותוכנו עריך מ-L10. אין אליו קישור משום מקום.**

🔴 **דרישה רגולטורית:** הצהרת הנגישות חייבת להיות נגישה **מכל עמוד**, בסימון ברור.

**Do:**
- **סמל נגישות קבוע** → `/accessibility`, **בכל עמוד** — כולל `/marketplace`, `/how-it-works`, ההרשמות והדשבורדים. **לא רק דף הבית**
- **פינה תחתונה קבועה.** ⚠️ **במובייל — לא מכסה כפתור פעולה, לא נחתך ב-390**
- **`aria-label="הצהרת נגישות"`** · יעד מגע ≥44×44 · נגיש במקלדת · טבעת מיקוד
- **גם קישור טקסטואלי בפוטר**

## 🔴 §5b · אל תתקין ווידג'ט נגישות

**לא overlay מסחרי, לא סרגל צף עם הגדלת גופן וניגודיות.**

**הוא אינו נדרש בתקנות, אינו תחליף לאתר נגיש, ולעיתים קרובות פוגע** — עוקף טכנולוגיות מסייעות שהמשתמש כבר הגדיר, ומתנגש עם קוראי מסך.

**מה שנדרש הוא שהאתר עצמו יהיה נגיש** — U8 §5 התחיל את זה. **הסמל הוא קישור להצהרה, לא כפתור שפותח סרגל.**

⚠️ **פרטי רכז הנגישות עדיין חסרים בהצהרה — זה של Yulian, לא שלך. אל תמציא טלפון או אימייל.**

---

# Acceptance

## §2

- [ ] 🔴 **ממצא ה-Network — קריאה שנייה או לא. לפני התיקון**
- [ ] ח.פ תפוס → שגיאה → **החלפה → השגיאה נעלמת.** צילום לפני/אחרי
- [ ] **השגיאה צמודה לשדה**, לא בראש הטופס. צילום
- [ ] **רשימת שדות נוספים באותה תבנית**

## §3

- [ ] רווח משני הצדדים. צילום דסקטופ + 390
- [ ] **`{' '}` ולא `padding`.** הדבק את ה-JSX

## §4

- [ ] **איפה הקישור קיים היום** — דוח
- [ ] **נראה במובייל בלי גלילה ובלי תפריט.** צילום 390
- [ ] **לא מתחרה בשדה החיפוש.** צילום של האזור כולו
- [ ] קיים גם בתפריט ובפוטר

## §5

- [ ] סמל **בכל עמוד** — **רשימת העמודים שנבדקו**, לא ״נוסף״
- [ ] צילום דסקטופ + 390
- [ ] **ב-390 לא מכסה כפתור ולא נחתך**
- [ ] `aria-label` · Tab מגיע · טבעת מיקוד. הדבק DOM
- [ ] קישור בפוטר
- [ ] 🔴 **אפס overlay.** `grep` על שמות ספקי נגישות נפוצים — ריק

## רגרסיה

- [ ] הרשמת קבלן · תאגיד · ספק — מקצה לקצה
- [ ] R9 · R10 · R11 — חיוב, `comped`, הקרון
- [ ] `--suite all` · `npm run build` · `npm test`
- [ ] 🔴 `git rev-list --left-right --count origin/staging...origin/pivot/v2` → `0 0`

## דווח

1. 🔴 **§1 — צילומי שלוש הישויות, והחישוב של המונה**
2. **§2 — ממצא ה-Network**
3. **§4 — איפה הקישור היה**
4. **§5 — רשימת העמודים**
5. `git diff -w --stat`

## Guardrails

`git tag pre-r12`. **סטייג׳ינג בלבד. שני הענפים, ובדוק `rev-list`.**

🔴 **אל תיגע ב-`visibility.py`.** H12 הוכרע. אתה משנה **מה מוצג כשאין תוצאות**, לא **מי רואה מה**.

🔴 **אל תסתיר את החיפוש מתאגידים ואל תפתח להם מודעות של אחרים.**

🔴 **מקטע השירותים הנלווים מרונדר לכל הישויות.**

🔴 **מונה אחד, לא שניים.**

🔴 **אל תתקין ווידג'ט נגישות.**

🔴 **אל תמציא פרטי רכז נגישות.**

🔴 **`{' '}` ולא `padding`.**

🔴 **`git add` לפי שם קובץ. לעולם לא `-A`.** **וכולל `docs/cc-prompts/*.md`.**

**אל תיגע** ב-חיוב · חשיפה · `query_rewriter.py` · דירוג התוצאות · R9/R10/R11.
