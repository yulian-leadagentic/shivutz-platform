# CC Run Sheet — 16/08/2026

**קובץ הרצה יחיד. שלבים 1→6 ברצף. שלב 1א׳ הוא STOP לדיווח — כל השאר רץ עד הסוף.**

כל ממצא כאן אומת בקוד מול `pivot/v2` ב-HEAD `9fa1254`. הנתיבים והשורות מדויקים. אם משהו לא תואם — **דווח על הפער, אל תנחש**.

## PRE-FLIGHT
1. `git rev-parse --abbrev-ref HEAD` → אמור להיות **`pivot/v2`**. קמט לענף שאתה באמת עליו. **אל תיגע ב-`main`.**
2. `git status` **לא אמין** — CRLF churn על כל הרפו. **אף פעם `git add -A`.** stage בשם. לסקירה: `git diff -w -- <file>`.
3. `git tag pre-<step>` לפני כל שלב; קומיט נפרד לכל שלב עם ההודעה שכתובה בו.
4. staging בלבד. אחרי כל שלב — שורת דיווח אחת, ואז הבא.

---

# שלב 1א׳ · ⛔ אבחון: חיפוש נכשל אחרי הרשמה — **עצור ודווח**

## מה נצפה
בעל המוצר נרשם כמשתמש חדש בסטייג'ינג, חיפש, **לא קיבל תוצאות וקיבל הודעת שגיאה לא ברורה**.
חיפוש **אנונימי עובד** — `"רתך"` החזיר `total=1` באותה פריסה. ההבדל הוא במצב המחובר.

## מה כבר נשלל (אל תחזור על זה)
ה-gateway מטפל נכון בטוקן על route ציבורי. `services/gateway/src/index.js:281`:
```js
} else if (req.headers.authorization) {
  const user = await validateToken(req).catch(() => null);
  if (user) attachUserHeaders(user);
}
```
`/api/search` נמצא ב-`PUBLIC_PREFIXES`, ולכן **טוקן פג או שגוי לא מייצר 401** — הוא פשוט לא מצרף headers. זו לא הסיבה.

## Do — לכידה, לא תיקון
הירשם כמשתמש חדש בסטייג'ינג, חפש `"רתך"` (שאילתה שידוע שמחזירה תוצאה כאנונימי), ודווח **כל אחד בנפרד**:

| # | מה לדווח |
|---|---|
| 1 | `POST /api/search` — **status + גוף התגובה המלא** מה-Network tab |
| 2 | האם `POST /api/search` בכלל נשלח? (אם לא — הכשל קרה לפניו) |
| 3 | **כל** קריאה אחרת שנכשלה באותו רגע — `/enums/*`, `/payments/subscriptions/me`, `/ads/usage`, reveal-resume. עם status וגוף |
| 4 | כל שגיאת console |
| 5 | **מה בדיוק הטקסט** שהופיע בבאנר האדום — העתק מילה במילה |
| 6 | האם אותה שאילתה עובדת בחלון פרטי (אנונימי) באותו רגע? |

> **חשד מוביל שכדאי לבדוק ראשון:** ב-`page.tsx` יש **state שגיאה יחיד** (`error`, שורה 116) שמשמש כמה נתיבים — החיפוש (238), ה-VoiceInputButton (`onError` בשורה 465), ועוד. **ייתכן שהחיפוש הצליח וקריאה אחרת לגמרי הציבה את `error`**, והבאנר בשורה 639 מרנדר אותה בתוך אזור התוצאות. אם זה המצב — אמור זאת במפורש; התיקון שונה לגמרי.

**⛔ עצור כאן ודווח את ששת הסעיפים לפני שאתה נוגע בקוד.**

---

# שלב 1ב׳ · הודעת השגיאה — לתקן בלי קשר לסיבה 🔴

`git tag pre-search-err`

## Root cause (מאומת)
`services/frontend/src/app/page.tsx:238`
```js
try { setResp(await searchApi.query(query)); }
catch (e) { setError((e as Error).message ?? 'שגיאה בחיפוש'); }
```
זו **בדיוק אותה תבנית ש-`ca91693` (QA-3) תיקן ב-`/billing`** — ששרדה במקום הכי חשוב במוצר: החיפוש בעמוד הבית. `error` מרונדר בשורה 639 כבאנר אדום, והתוכן שלו הוא הודעת המכונה הגולמית מהשרת.

## התבנית לחיקוי נמצאת באותו קובץ, 50 שורות מתחת
`revealFor` (שורות 285–310) עושה את זה **נכון**:
```js
const cause  = e instanceof ApiError ? e.cause : null;
const status = cause?.status;
const code   = cause?.error || cause?.code;
```
ואז ממפה לקודים ספציפיים עם הודעות עברית ממוקדות. **עשה אותו דבר לחיפוש.**

## Do
1. `page.tsx:238` → `mapApiError` (`services/frontend/src/lib/api/errors.ts:249`).
2. הוסף טיפול ממוקד לפי `ApiError.cause` לפחות ל: **429** (rate-limit → "יותר מדי חיפושים, נסה שוב בעוד רגע"), **5xx** ("החיפוש אינו זמין כרגע, נסה שוב"), **timeout/רשת** ("בעיית תקשורת — בדוק חיבור ונסה שוב").
3. **הפרד את מצבי השגיאה.** `error` משמש כמה נתיבים; ודא שהבאנר באזור התוצאות מציג **רק** שגיאות חיפוש. אם שגיאת קול/enum מגיעה לאותו state — תן לה state נפרד או קונטקסט משלה.
4. כל הודעה אומרת **מה קרה ומה לעשות**. בלי קוד מכונה באנגלית.

## Acceptance
- [ ] `git grep -n "(e as Error).message" -- services/frontend/src/app/page.tsx` = **0**
- [ ] כשל חיפוש מדומה (429 / 500 / ניתוק רשת) → שלוש הודעות עברית שונות ומובנות
- [ ] שגיאת תמלול לא מופיעה כשגיאת חיפוש

**Commit:** `search: route landing search errors through mapApiError with typed cases`

---

# שלב 2 · מלאי הסטייג'ינג 🔴

`git tag pre-seedcov`

**בצע את `docs/cc-prompts/cc_prompt_seed_coverage.md` במלואו**, כולל נספח L1/L2/L3 בסופו.

**שתי דרישות שנוספו אחרי הדיווח שלך היום:**

1. **הסבר את הסתירה.** דיווחת `worker_ads=3 · housing_ads=0 · active_corps=0`. **אם אין אף תאגיד פעיל — מי הבעלים של שלוש המודעות?** הסבר את הסתירה לפני שאתה מתקן. ייתכן שהיא עצמה הבאג.
2. **3 מודעות זה לא קטלוג.** גם אחרי תיקון תקרת ה-tier, 3 תאגידים × 5 = 15. **דווח כמה מודעות ה-seed אמור לייצר וכמה נוצרו בפועל**, ואם הפער נשאר — אמור זאת. אנחנו צריכים מלאי שאפשר לשפוט עליו איכות חיפוש, לא מינימום שעובר.
3. `housing_ads=0` — האם ה-seed בכלל מייצר מודעות דיור? אם לא, `ad_type=housing` הוא נתיב שלם שמעולם לא נבדק.

**Acceptance:** פלט `GROUP BY` **מלא** (לא סיכום) · כל 10 המקצועות מופיעים · לפחות זוג `general`+`CN` · הסתירה של `active_corps` מוסברת · `housing_ads > 0` או הסבר למה לא.

---

# שלב 3 · בועת ה-LIVE — אפשרות ב׳ (הוכרע) 🟡

`git tag pre-live-b`

## ההחלטה
**אפשרות ב׳: לשמור את ה-mock, להסיר את טענת ה-LIVE, לנסח מחדש כתוכן הדגמה.** אפשרות א׳ (חיבור ל-feed אמיתי) נשארת ליום שיש פעילות אמיתית להראות.

הנימוק: הבועה מציגה **38 פריטים מומצאים** מ-`features/live-activity/mocks.ts` תחת תווית LIVE אדומה עם נקודה מהבהבת — לפני השקה, בלי משתמש אחד אמיתי. `"עסקה נסגרה"` היא **טענה על הפלטפורמה**, לא קישוט. להסיר את הטענה זה זול; לוותר על החיות בדף זה מיותר.

## Do
1. **הסר את תווית ה-LIVE ואת הנקודה המהבהבת משני המשטחים:**
   - `services/frontend/src/components/landing/LiveActivityFeed.tsx:283-284` — `bg-rose-500 animate-live-dot` + `animate-live-dot-halo`
   - `services/frontend/src/features/live-activity/RoleLiveStrip.tsx:188-192` — ה-pill `bg-rose-500` עם המילה `LIVE` והנקודה
2. **⚠️ `LiveShowcase` יונק מאותו מקור** (`RoleLiveStrip` → `MOCK_ITEMS`). **טפל בשניהם.** `LiveShowcase.tsx:8` מתאר במפורש `●LIVE לקבלן` בכל כרטיס. אל תשאיר משטח אחד מתוקן והשני טוען LIVE.
3. **נסח מחדש את ה-copy ב-`mocks.ts` מעבר להווה/עתיד.** עבר משתמע באירוע שקרה: `"עסקה נסגרה"` → `"עסקאות נסגרות בפורטל"`, `"תאגיד פעיל"` → `"תאגידים מפרסמים כאן"`. **המסר: מה הפלטפורמה עושה — לא מה קרה לפני רגע.**
4. **מסגור גלוי.** כותרת אזור שאומרת שזו הדגמה — למשל `"כך נראית הפעילות בפורטל"` — כדי שהמשתמש לא יפרש כזרם חי. SR-2 כבר ריכך ל-`"מה קורה בפורטל"`; **זה עדיין קורא כהווה. חזק את זה.**
5. **השאר את השהיית הבועה מ-SR-2 כמו שהיא** — היא עובדת.
6. **הערת TODO אחת** ליד נקודת ההחלפה ל-`GET /api/marketplace/activity-feed`: כשיש פעילות אמיתית, עוברים לאפשרות א׳ ומחזירים את תווית ה-LIVE.

## Acceptance
- [ ] `git grep -n "animate-live-dot\|>LIVE<\|'LIVE'" -- services/frontend/src` = **0**
- [ ] אף משטח (בועה **וגם** showcase) לא טוען פעילות בזמן אמת
- [ ] copy בהווה/עתיד; אין אירוע-עבר מומצא
- [ ] נקודת ההחלפה ל-feed אמיתי מסומנת ב-TODO
- [ ] 390 + דסקטופ

**Commit:** `live-activity: drop LIVE claim from mock surfaces, reframe as demo content`

> אם Yulian יעדיף א׳ (חיבור ל-feed אמיתי) או ג׳ (כיבוי) — **החלף את השלב הזה בלבד**, השאר לא מושפע.

---

# שלב 4 · סוללת החיפוש מחדש

**רק אחרי 1ב׳ + 2, ורק אחרי ש-`ANTHROPIC_API_KEY` הוגדר על `user-org`.**

**קודם כל אמת את המצב — אל תסיק אותו.** קרא את הלוג בפועל:
```
docker logs user-org | grep '\[qrewrite\]'
```
דווח את השורה **כלשונה**. הדיווח הקודם סיווג `mode=fake` כ-`(inferred)` — הפעם צריך קריאה.

הרץ ודווח `total` + `filters` לכל אחת:

| שאילתה | למה |
|---|---|
| `"פועלים סינים"` | הכשל המקורי — `general`+`CN` |
| `"רתך"` | בקרה — החזיר 1 קודם |
| `"20 פועלים"` | S3 — quantity |
| `"חשמלאי במרכז"` | מקצוע + אזור |
| `"פועלים ל-3 חודשים"` | S3 — **חייב** `quantity=null` |
| `"דיור למרכז"` | `ad_type=housing` — נתיב שלא נבדק |
| `"laborer"` (אנגלית) | S1 — enum מומצא נחסם |

**וגם:** הרץ את כל השבע **גם מחובר וגם אנונימי**, ודווח כל הבדל. זה סוגר את שלב 1א׳.

**Acceptance:** `"פועלים סינים"` > 0 · `"פועלים ל-3 חודשים"` → `quantity=null` · מחובר = אנונימי · מצב מדווח מלוג אמיתי.

---

# שלב 5 · צילומים

`npm run capture` מול הפריסה האחרונה.

**זה החוב הפתוח היחיד ב-SR** — SR-1/2/4/5 נמסרו בלי אימות ויזואלי כי חלון הדפדפן המקומי לא רינדר (`window.innerWidth === 0`). ה-harness מול סטייג'ינג הוא הדרך הנקייה.

צלם **390 ודסקטופ**: עמוד הבית לפני חיפוש · מיד אחרי לחיצה על "חפש" (skeleton) · תוצאות מלאות · אפס תוצאות · אחרי תמלול קולי.

**דווח מפורשות:** האם התוצאות נראות **בלי גלילה ידנית** בשני הרוחבים. זו הייתה התלונה המקורית.

**⚠️ ה-harness נכנס כישות אחת** (`LOGIN_PHONE`) — מסכי `corporation__*` ו-`admin__*` ייצאו NoAccessCard. **צפוי, לא באג.**

---

# שלב 6 · דוח סיום

| שלב | סטטוס | SHA | הערה |
|---|---|---|---|
| 1א׳ אבחון | | | הסיבה האמיתית לכשל במצב מחובר |
| 1ב׳ שגיאה בעברית | | | |
| 2 seed | | | פלט GROUP BY מלא |
| 3 LIVE ב׳ | | | שני המשטחים? |
| 4 סוללה | | | שורת הלוג כלשונה |
| 5 צילומים | | | נראה בלי גלילה? |

**ובנוסף:**
- שורת `[qrewrite]` **המדויקת** מהלוג.
- כל מקום שבו המציאות בקוד לא תאמה ל-run sheet הזה.
- אם נשאר משהו לא-מאומת — **אמור זאת**. אל תסמן ✅ על סמך הסקה.

---

## מחוץ לתחום — אל תיגע היום

- **Track W1 (WhatsApp/Meta)** — חסום על Publish ל-Meta app, **ובכוונה נדחה**: אין טעם לפתוח ערוץ קלט שני למנוע שעדיין נכשל למשתמש מחובר.
- **נורמליזציית CRLF** — משנה כל קובץ ברפו, דורשת קומיט וסבב משלה.
- **harness דו-ישותי** — משימה נפרדת.
- **Ads · Tenders T2+ · L2/L3** — חסומים על החלטות מוצר.
- **`query_rewriter.py` / `query_reranker.py` / `routes/search.py`** — S1–S3 סגורים ואומתו. אל תיגע.
- **`subscription_limits.py`** — מספרים מוצריים, לא נוחות בדיקה.

אם תיקון מתחיל להיכנס ללוגיקת מנוי / reveal / boost / Cardcom — **עצור ודווח**.
