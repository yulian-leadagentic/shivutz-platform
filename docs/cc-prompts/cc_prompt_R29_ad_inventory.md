# R29 · מערך הפרסום — סלוטים, מידות נאכפות, וניצול רוחב המסך

## ⭐ `git tag pre-r29`. **ריצה רציפה.**

🔴 **R29 §1-§2 מחליפים את R28 §4.** **בצע את R28 §1-§3 כפי שהם, ודלג על R28 §4 — הוא מטופל כאן לעומק.**

**Yulian, 22.09 (עם חמישה צילומים מ-Yad2 ומ-דרושים):**
*״בהעלאה לחייב פורמט שמתאים לנו, גם אם צריך מספר קבצים בהתאמה למובייל ולדסקטופ. יש גם מודעה לאורך וגם קרוסלה במקביל. שים לב לניצול הנדל״ן הפרסומי ולניצול רוחב המסך במחשב נייח.״*

---

# §0 · מה קראתי בסכימה לפני שכתבתי

**כבר קיים, ועובד — אל תבנה מחדש:**

```
sponsor_ads          069 · headline_he · body_he · chips_he · cta_label_he
                           cta_url · logo_url · brand_bg · brand_fg
                           target_professions / ad_types / regions
                     078 · placements JSON
                     092 · creative_url · creative_w · creative_h
sponsor_ads_slots    084 · placement · category_code · starts_at · ends_at
                           price_nis · sponsor_ad_id     ← מדף ומכירה
```

**המודל העסקי בנוי. מה שחסר הוא שתי נקודות, ושתיהן מוכחות:**

## 🔴 א. אין אכיפת מידות. בכלל.

`services/admin/app/routes/sponsors.py:218`

```python
def _validate_creative(url, w, h):
    provided = [x for x in (url, w, h) if x is not None]
    if provided and len(provided) != 3:
        raise HTTPException(400, "creative_url_requires_width_and_height")
```

**זה בודק ששלושת השדות הגיעו יחד. זה הכל.** `creative_w`/`creative_h` הם `ge=1, le=8000` — **כל מספר בטווח עובר.**

⚠️ **והערת המיגרציה 092 מודה בזה במילים שלה:** *״Format enforcement is admin-side (R21 §2a upload step); the DB accepts whatever numbers the admin passed.״* 🔴 **בדקתי — שלב האכיפה הזה לא קיים בקוד.**

**ההוכחה חיה בסטייג׳ינג:** הקריאייטיב שעלה הוא **1037×609**, ולא ה-`1200×628` ש-R20 §3 קבע.

## 🔴 ב. קריאייטיב אחד לכל מקום ולכל מסך

**`sponsor_ads.creative_url` הוא עמודה אחת.** **אותה תמונה משרתת לידרבורד, קרוסלה, מובייל ודסקטופ.**

> 🔴 **וזה הגורם הישיר למה שYulian רואה:** תמונה 1.70:1 בתוך סלוט רצועה, עם `contain` שאסור להחליף — **מצטמצמת ל-340×200 וצפה במרכז שורה של 1120.**
> **אין CSS שמתקן את זה. צריך קובץ לכל סלוט.**

---

# §1 · 🔴 `sponsor_creatives` — קריאייטיב לכל סלוט ולכל מסך

**מיגרציה חדשה.** 🔴 **אל תיגע ב-069 · 078 · 084 · 092 — הן רצו. `run_migrations.py` יזהה sha ויתלונן לנצח.**

```
sponsor_creatives
  id             CHAR(36)     PK
  sponsor_ad_id  CHAR(36)     NOT NULL   → sponsor_ads.id  (ON DELETE CASCADE)
  placement      VARCHAR(32)  NOT NULL   ← אותו אוצר מילים של 078/084
  breakpoint     ENUM('desktop','mobile') NOT NULL
  url            VARCHAR(500) NOT NULL
  w              INT          NOT NULL
  h              INT          NOT NULL
  created_at · updated_at
  UNIQUE KEY (sponsor_ad_id, placement, breakpoint)
```

- 🔴 **`creative_url`/`creative_w`/`creative_h` על `sponsor_ads` נשארים.** **הם ה-fallback לשורות הקיימות.** **אל תמחק עמודה ואל תעשה מיגרציית נתונים בסבב הזה**
- **סדר הבחירה בהגשה:** `sponsor_creatives(placement, breakpoint)` → אם אין, `sponsor_creatives(placement, 'desktop')` → אם אין, `sponsor_ads.creative_url` → אם אין, **המודל המורכב של §3**
- **דווח את סדר הבחירה שמימשת**

---

# §2 · 🔴 טבלת המידות — ונאכפת בהעלאה

## המידות

| סלוט | דסקטופ | מובייל | יחס |
|---|---|---|---|
| `home_leaderboard` | **1200×150** | **720×200** | 8:1 · 3.6:1 |
| `home_billboard` | **1200×250** | **720×300** | 4.8:1 · 2.4:1 |
| `side_rail` | **300×600** | — | 1:2 |
| `home_carousel` · `marketplace_carousel` | **640×360** | **640×360** | 16:9 |
| `marketplace_banner` | **1200×250** | **720×300** | כמו billboard |
| `search_inline` | לוגו **240×240** | לוגו **240×240** | 1:1 |
| `logo_wall` | לוגו **240×120** | — | 2:1 |

✅ **המידות אושרו על ידי Yulian, 22.09. אין כאן מה לשאול — ממש כלשונן.**
**אלה מידות תקן שסוכנויות ומעצבים מכירים, כך שמפרסם יכול להזמין קריאייטיב בלי שנסביר לו דבר.**

## האכיפה

- 🔴 **`_validate_creative` בודק יחס מול הסלוט, בסבילות של ±3%.** **חריגה = `400` עם הודעה שאומרת את המידה הנדרשת בעברית, לא קוד**
- 🔴 **ורוחב מינימלי:** **לא פחות מרוחב הסלוט.** תמונה קטנה מדי נמתחת ומטשטשת
- 🔴 **המידות נמדדות בשרת מהקובץ עצמו — לא נלקחות מהלקוח.** **הלקוח יכול לשקר בשני שדות מספריים, וזה בדיוק מה שקרה**
- ⚠️ **ולידציה גם בטופס האדמין לפני השליחה**, עם הודעה שאומרת מה נדרש ומה הועלה. **זה נוחות — השרת הוא המקור**
- 🔴 **המידות בקבוע אחד משותף.** **אל תפזר מספרים בין השרת לפרונט**

## 🔴 והשורה שכבר בפנים

**`1037×609` חיה עכשיו בסטייג׳ינג ותיפול באכיפה החדשה.**
**Do: `SELECT` כל שורה עם `creative_url` והיחס שלה. דווח רשימה.** ⚠️ **אל תמחק ואל תשבית — Yulian יחליט. האכיפה חלה על העלאות חדשות בלבד**

---

# §3 · 🔴 הרצועות הרחבות נבנות מרכיבים, לא מ-JPG

**זה הלקח המרכזי מהצילומים של Yulian, ואני חושב שהוא החשוב ביותר כאן.**

**הבאנרים של Yad2 — Drushim, קפריסין, חברת החשמל — אינם תמונה שטוחה אחת.** **כל אחד מהם:**

```
רקע בצבע מותג  |  תמונה בצד אחד  |  כותרת גדולה + שורת משנה  |  כפתור CTA
```

> 🔴 **ולכן הם ממלאים כל רוחב, בכל מסך, בלי אות אחת שנחתכת ובלי לבן בצדדים.**
> **JPG שטוח אינו יכול לעשות את זה — הוא נעול ביחס אחד.**

🔴 **ומודל הרכיבים הזה כבר קיים אצלנו — מיגרציה 069:** `headline_he` · `body_he` · `cta_label_he` · `logo_url` · `brand_bg` · `brand_fg`. **הוא נבנה ואז נדחק לטובת `creative_url` ב-092.**

## Do

- 🔴 **ב-`home_leaderboard` וב-`home_billboard` — המודל המורכב הוא ברירת המחדל.** **JPG שטוח הוא המסלול החריג, למפרסם שמגיע עם אמנות מוגמרת**
- **פריסת הרצועה:** לוגו ותמונה בצד אחד · כותרת ושורת משנה במרכז · CTA בצד השני. **`brand_bg` ממלא את כל הרוחב** — 🔴 **וזה פותר את הלבן בצדדים אחת ולתמיד**
- ⚠️ **במובייל אותה רצועה נערמת:** תמונה למעלה, טקסט ו-CTA מתחת
- 🔴 **ניגודיות:** `brand_fg` על `brand_bg` חייב לעבור **4.5:1**. **חשב בשרת, ואם נכשל — החזר `400`.** ⚠️ **בעמוד שמצהיר על נגישות, באנר לא קריא הוא בדיוק ההפרה שההצהרה מבטיחה שאין**
- **`cta_url` ריק → CTA כ-`<span>`, לא `<a href="#">`.** **הכלל מ-069 נשאר**
- ✅ **`object-fit: contain` על כל JPG שטוח. לעולם לא `cover`** — R20 §3a

---

# §4 · ניצול רוחב המסך — `side_rail`

**ב-1920 עמוד הבית שלנו הוא עמודה של ~1150 ומשני צדדיה כ-380px ריקים.**
**בצילומים של Yulian — Yad2 ודרושים — בדיוק שם יושב באנר אנכי 300×600 שנגלל עם התוצאות.**

## Do

- **סלוט `side_rail` חדש. 300×600. דסקטופ בלבד**
- 🔴 **מופיע רק מ-1440 ומעלה.** ⚠️ **מתחת לזה הוא גונב רוחב מהתוכן — וזה בדיוק הבאג שאנחנו מתקנים ב-§0ב**
- **`position: sticky` עם `top` מתחת לניווט.** נגלל עם התוצאות
- 🔴 **אם אין מודעה פעילה — הסלוט לא מרונדר, והתוכן נשאר ממורכז.** **אל תשאיר מלבן ריק. ספק F3: ״אין מקטעים בלי מודעות פעילות״**
- ⚠️ **צד אחד בלבד בהתחלה.** **שני צדדים בעמוד עם תוכן דליל נראה כמו אתר תוכן זבל**
- **צילום ב-1920 · 1440 · 1280 · 390**

---

# §5 · 🔴 תקרת צפיפות — לידרבורד וקרוסלה יחד

**Yulian הזכיר במפורש שיש גם רצועה וגם קרוסלה במקביל. זה נכון, וזה גם הסיכון.**

**ב-Yad2 יש ארבע יחידות פרסום למסך — אבל מתחתיהן 1,102 מודעות אמיתיות. אצלנו יש 16.**

> 🔴 **יחס פרסום-לתוכן, לא מספר מוחלט.** **עמוד עם 16 מודעות שנושא ארבעה באנרים נראה כמו אתר ריק שמנסה למכור.**

## Do

- 🔴 **לכל היותר **שתי** יחידות פרסום מעל הקיפול.** **ההמלצה: `home_leaderboard` + `side_rail`.** **הקרוסלה מתחת**
- **`site_settings` — מפתח לתקרה, כמו `sponsor_carousel_limit` שכבר קיים מ-092.** **אל תקודד מספר**
- **בין שתי יחידות פרסום — לפחות מקטע תוכן אחד**
- ⚠️ **ובדוק: אם אותה מודעה מוגשת גם ללידרבורד וגם לקרוסלה** (`placements` מאפשר את זה במכוון) — **הצג אותה פעם אחת.** **זו בדיוק כפילות R23 §1 בלבוש חדש**

---

# §6 · מה *לא* לוקחים מהצילומים

**Yulian: ״לא בהכרח הכל רלוונטי לנו.״ אלה שלושת הדברים שבדקתי ופוסל:**

- 🔴 **שורת אייקוני הקטגוריות העגולים של Yad2** — **אנחנו עושים את זה כשורת טקסט ב-R28 §3, ו-Yulian אישר את הנוסח.** **אל תחליף אותה באייקונים**
- ⚠️ **`logo_wall` (״חברות מובילות״)** — **צריך שמונה מפרסמים כדי לא להיראות נטוש.** **יש לנו אפס אמיתיים. הגדר את הסלוט בטבלה, אל תרנדר אותו**
- ⚠️ **מודעה מוטמעת בשורת התוצאות** (האריח הצהוב אצל Yad2) — 🔴 **מודעה שנראית כמו תוצאה בעמוד שמוכר אמון בספקים. אחרי ההשקה, אם בכלל, ורק עם תיוג ״ממומן״ בולט**

---

# Acceptance

## §1

- [ ] 🔴 **`sponsor_creatives` נוצרה במיגרציה חדשה.** **`git diff` על `db/migrations/069|078|084|092` — ריק**
- [ ] **סדר הבחירה — דווח, והדגם עם שורה שיש לה קריאייטיב למובייל בלבד**
- [ ] **שורה ישנה עם `creative_url` בלבד עדיין מרונדרת.** צילום

## §2

- [ ] 🔴 **העלאת 1037×609 ל-`home_leaderboard` → `400`.** הדבק
- [ ] 🔴 **העלאת 1200×150 ל-`home_leaderboard` → `201`.** הדבק
- [ ] 🔴 **מידות נמדדות בשרת.** **שלח `creative_w` שקרי והוכח שנדחה.** הדבק
- [ ] **רוחב קטן מדי → `400`.** הדבק
- [ ] **הודעת השגיאה בעברית ואומרת את המידה הנדרשת.** צילום
- [ ] 🔴 **רשימת כל השורות הקיימות והיחס שלהן.** **דווח, אל תשנה**
- [ ] **המידות בקבוע אחד. הדבק את הקבוע**

## §3

- [ ] 🔴 **לידרבורד מורכב במלוא הרוחב ב-1920 · 1440 · 1280 · 390.** ארבעה צילומים
- [ ] 🔴 **אפס לבן בצדדים.** **זו התלונה המקורית**
- [ ] **מובייל — נערם.** צילום
- [ ] 🔴 **ניגודיות מתחת ל-4.5:1 → `400`.** הדבק
- [ ] **`cta_url` ריק → `<span>`.** הדבק
- [ ] **JPG שטוח עדיין `contain`.** הדבק

## §4

- [ ] **`side_rail` ב-1920 וב-1440.** שני צילומים
- [ ] 🔴 **ב-1280 וב-390 לא קיים, והתוכן ממורכז.** שני צילומים
- [ ] **בלי מודעה פעילה — לא מרונדר.** צילום
- [ ] **`sticky` עובד בגלילה.** צילום

## §5

- [ ] 🔴 **שתי יחידות לכל היותר מעל הקיפול ב-1920.** צילום
- [ ] **התקרה ב-`site_settings`.** הדבק
- [ ] 🔴 **מודעה בשני `placements` מוצגת פעם אחת.** צילום

## רגרסיה

- [ ] 🔴 **R28 §1-§3 — שורת השירותים ודירוג `primary_section`**
- [ ] 🔴 **R15 — אפס דליפה ב-`/api/marketplace`**
- [ ] R27 · R26 · R25 · R24 · R23 · R21 · R20 · R13 · R16
- [ ] 🔴 **`run_migrations.py` — אפס אזהרות sha**
- [ ] `npm run build` · `npm test` · `--suite all` · `--suite matrix`
- [ ] 🔴 `git rev-list --left-right --count origin/staging...origin/pivot/v2` → **`0 0`**

## דווח

1. 🔴 **ארבעת צילומי הלידרבורד — 1920 · 1440 · 1280 · 390**
2. 🔴 **דחייה וקבלה של העלאה — שתי הדבקות**
3. **רשימת השורות הקיימות והיחס שלהן**
4. **`side_rail` — ארבעה צילומים**
5. **סדר בחירת הקריאייטיב**
6. `git diff -w --stat`

---

## Guardrails

`git tag pre-r29`. **סטייג׳ינג בלבד. שני הענפים.**

🔴 **מיגרציה חדשה בלבד. אל תיגע ב-069 · 078 · 084 · 092** — `run_migrations.py` מזהה sha ומזהיר לנצח.

🔴 **אל תמחק עמודה ואל תעשה מיגרציית נתונים.** `creative_url` נשאר כ-fallback.

🔴 **אל תשבית ואל תמחק שורת מפרסם קיימת.** דווח, Yulian מחליט.

🔴 **`object-fit: contain`. לעולם לא `cover`** — R20 §3a.

🔴 **אל תיגע** בחשיפה · במטריצת R13 · ב-R15 · ב-R16 · ב-`cardcom.py` · ב-`query_rewriter.py`.

🔴 **אל תיגע במחירים ובמדף** — `sponsor_ads_slots.price_nis` ו-`sponsor_slots.py`. **סכימת קריאייטיב בלבד.**

🔴 **`git add` לפי שם קובץ. לעולם לא `-A`.** **וכולל `docs/cc-prompts/*.md`.**
