# TagidAI — סדר הרצה
**גרסה 2 · 27/08/2026 · מחליף את `cc_run_sheet.md`, `cc_run_sheet_0816.md`, `cc_workorder_0816.md` ו-`STATUS_0816.md`**

> **גרסה 2:** מסלול ספקי השירות **הופשר והוכנס לסדר**. כל הפרומטים כתובים. אין יותר פריט בלי קובץ.

> **זה קובץ ההרצה היחיד.** כל פרומט נקודתי כפוף לסדר שכאן.
> `cc_master_backlog.md` (13/08) נשאר כמסמך **רקע וניתוח שורש** — אבל **הסדר שלו כבר לא בתוקף**.

---

## ההכרעות שנסגרו — 27/08

| | |
|---|---|
| **הענף** | **`staging`.** יש בדיקות בשני הענפים → בדיקה ראשונה, איחוד אחריה |
| **ראשון בתור** | **בדיקת הענפים.** לפני כל קוד חדש |
| **אנימציה** | **דמו אינטראקטיבי, נבנה בקוד.** בלי Lottie, בלי וידאו, בלי מעצב חיצוני |
| **"נראות"** | **UI.** חשיפה שיווקית — לא בסבב הזה |
| **זמינות** | **נכנס** — `available_from DATE NULL` על המודעה. לא חובה |
| **באנר "הכל במקום אחד"** | **נשאר.** לא נוגעים |
| **מסלול ספקי שירות** | **מרחיבים את כל ה-ENUMs.** ⭐ **הופשר — נכנס לסדר כשלב 2** |
| **סנכרון** | **הכל מסונכרן ומוכן.** כל פריט ברשימה יש לו פרומט כתוב |

---

## ⭐ STANDING RULES — חלים על כל פריט

1. `git tag pre-<item-id>` **לפני** כל פריט
2. **`staging`** — ורק אחרי ש-X0 הכריע. `main` רק באישור מפורש של Yulian
3. **אין `git add -A`.** קבצים בשם מפורש — CRLF הופך את `git status` ללא-אמין
4. 390 **וגם** דסקטופ. RTL, CSS לוגי
5. DS tokens בלבד. כתום קנוני = **brand-600 `#f78203` + טקסט כהה**
6. **DoD = ראיה, לא דיווח.** צילום שמראה את המצב הסופי, אחרי טעינה — לא skeleton, לא hover תקוע
7. נגיעה ב-מנוי / reveal / boost / חיוב → **עצור ודווח**

> ### 🔴 הכלל שנוסף היום
> **דוח "בוצע" בלי ראיה נחשב לדוח שלילי.**
> חזר עלינו ארבע פעמים בסבב הזה: `%%` ב-dashboard · `nm-02` שצילם skeleton · צבע הכפתור · NMC שדווח כשוּנה. **אם אין צילום שמוכיח — הפריט לא נסגר.**

---

# שלב 0 · 🔴 חוסמים

| # | מה | בעלים | קובץ | זמן |
|---|---|---|---|---|
| **X0** | **מיפוי ענפים** — קריאה בלבד, אפס merge | CC | `cc_prompt_branch_reconcile.md` | סבב קצר |
| **X0b** | **החלטת איחוד** אחרי הדוח | Yulian + אני | — | שיחה |
| **X1** | **`docker logs user-org \| grep '\[qrewrite\]'`** → `mode=real`? | Yulian | — | **1 דק׳** |
| **X2** | **`seed_coverage`** — מלאי אמיתי, כולל באג ה-BN | CC | `cc_prompt_seed_coverage.md` | סבב |

> **X1 הוא דקה אחת והוא פתוח מ-16/08.**
> הוא מכריע אם רצועת "המנוע הבין" מציגה הבנה אמיתית או התאמת-מחרוזות. **בלעדיו נבנה LIVE-badge חדש מעל דאטת מוק** — הטעות שכבר עשינו.
>
> **X2 חוסם את הצ'יפים.** צ'יפ מובנה שמחזיר אפס תוצאות הוא הרושם הראשון הגרוע ביותר שקיים.

---

# שלב 1 · 🟠 החוב הפתוח — ארבעה תיקונים קטנים שקוברים כל דבר חדש

| # | מה | קובץ |
|---|---|---|
| **D1** | **`h1` עדיין "פלטפורמת השיבוץ הראשונה בישראל"** → `הפלטפורמה לקבלנים ולתאגידי כוח אדם` | `cc_prompt_sp2.md` §4 |
| **D2** | **NMC** — "לא נמצאו ריצוף מסין" מעל כרטיס ריצוף מסין | `cc_prompt_nm_copy.md` |
| **D3** | **שגיאת reveal** — למפות 5xx/503, ו**להפסיק `clearPendingReveal()` על `kind:'error'`** | `cc_prompt_d3_reveal_error.md` ✅ |
| **D4** | **נרדפות** — מקצועות, מוצאים, אזורים | `cc_prompt_synonyms.md` ✅ |

**D1 ו-D2 הם שינויי נוסח בלבד ואפשר לאחד אותם לסבב אחד.**
**D3 הוא נתיב הכנסה** — קבלן שנתקל בשגיאה טכנית מאבד את כוונת החשיפה שלו.
**D4 הוא תשתית ל-M4** — הוא מעביר את רשימות המילים מהקוד ל-DB, וזה בדיוק מה ש-M4 צריך.

---

# שלב 2 · 🏗 מסלול ספקי השירות

**איפיון מלא: `services_vertical_spec.md`.** ארבעה פרומטים, בסדר, **ואין לקצר ביניהם**:

| # | מה | קובץ |
|---|---|---|
| **M1** | **המיגרציה הרוחבית** — כל ה-`entity_type`, `ad_type`, `owner_entity_type`, מחיר ב-`subscription_plans` | `cc_prompt_m1_enum_migration.md` |
| **M2** | **ארבע הטבלאות החדשות** — סכימה בלבד | `cc_prompt_m2_service_tables.md` |
| **M3** | **אדמין קטגוריות** — CRUD, מחיר, נרדפות. **הבקשה המרכזית שלך** | `cc_prompt_m3_admin_categories.md` |
| **M3b** | **אתה מזין 8–12 קטגוריות אמיתיות** דרך המסך | Yulian |
| **M4** | **חיבור לחיפוש** — הנקודה המסוכנת | `cc_prompt_m4_search_wiring.md` |

## למה הסדר הזה, ולמה אסור לקצר

- **M1 לפני M2** — M1 נוגעת בכל מה שקיים ועובד; M2 רק מוסיפה. ערבוב אומר שכשל בחדש מחייב גלגול לאחור של הרוחבי
- **M3 לפני M4** — אין טעם לחבר חיפוש לטבלת קטגוריות ריקה
- **M3b הוא שלב אמיתי ולא פורמלי.** קטגוריות שתזין בעצמך הן הבדיקה היחידה שתגלה אם המודל מחזיק. קטגוריות שמישהו ימציא בקוד לא יבדקו כלום
- **M4 אחרון** — הוא נוגע ב-`query_rewriter.py`, הקובץ שכבר שרף אותנו פעמיים

> ### 🔴 שתי אזהרות שחוזרות בכל ארבעת הפרומטים
> **1 · קטגוריות נשארות דאטה.** לעולם לא `category ENUM(...)`. ברגע שמישהו יוסיף — כל ההכרעה הארכיטקטונית הייתה לשווא.
> **2 · M4 הוא S1 בלבוש חדש.** אז ה-LLM המציא ערך; שם האדמין ייצור ערך אמיתי שהקוד לא מכיר. **אותה תוצאה בדיוק: החיפוש מחזיר את כל הקטלוג.**

---

# שלב 3 · 🟢 המסך הראשון

**איפיון מלא: `spec_first_screen.md`.** חמישה פריטים, לפי הסדר:

| # | מה |
|---|---|
| **F1** | ארבעת הכרטיסים → **צ'יפים שמריצים חיפוש אמיתי**. מחיקת `ציוד ושירותים (בקרוב)` |
| **F2** | **השאילתה מוקלדת לתוך התיבה** בלחיצה על צ'יפ (~250–350ms), עם דילוג-לסוף וכיבוד `prefers-reduced-motion` |
| **F3** | **רצועת "הבנתי"** — בולטוּת + הסרת תג בקליק (זה גם L2 מהבקלוג, בחינם) |
| **F4** | **"איך זה עובד" נכתב מחדש** — `שואלים → המערכת מבינה → מתחברים וחושפים` — ויורד מתחת לתוצאות |
| **F5** | **סוללה + צילומים** ב-390 ובדסקטופ |

> **F3 חסום ב-X1.** אל תיתן בולטוּת להבנה לפני שהוכח שהיא אמיתית.

---

# שלב 4 · ⏱ שדה הזמינות

| # | מה |
|---|---|
| **V1** | `ALTER TABLE ads ADD COLUMN available_from DATE NULL` |
| **V2** | טופס המודעה — בורר תאריך אופציונלי + צ׳קבוקס `זמין מיידית` |
| **V3** | כרטיס התוצאה — `זמין מ-…` **רק כשהשדה מלא**. NULL = לא מוצג כלום |
| **V4** | rewriter — `בעוד שבועיים` / `מיידי` → תאריך. לא ניתן לפרסר → `None` |

> ### 🔴 האזהרה, ואין עליה ויכוח
> ```sql
> AND (a.available_from IS NULL OR a.available_from <= %s)
> ```
> **פילטר קשיח ימחק את כל המלאי הקיים**, כי כולו `NULL`. זה S1 בלבוש חדש.

---

# ⛔ מוקפא

W1/WhatsApp · Ads (חסום Y3) · Tenders T2+ · L3 · CRLF (B2) · harness דו-ישותי · placeholder בלולאה · **צד הספק וצד הקבלן במסלול השירותים** (אחרי M4).

---

# 🟠 עליך, במקביל, ולא חוסם את CC

| # | מה | זמן |
|---|---|---|
| Y-1 | **`mode=real`** — זה X1 למעלה | 1 דק׳ |
| Y-2 | **`PAYMENT_FAKE_MODE`** בסטייג'ינג — ברירת המחדל בקוד היא Cardcom **אמיתי** | 1 דק׳ |
| Y-3 | **WhatsApp A+B** — שישה ערכים ל-`notification`. **טוקן קבוע דרך System User**, לא הזמני | 20 דק׳ |
| Y-4 | **`/accessibility`** — לפרסם ולקשר מהפוטר של כל עמוד | — |
| Y-5 | **אימות Meta** — ממתין | — |

**⏰ 1 באוקטובר** — WhatsApp עובר לחיוב לפי הודעה.

---

# 🗂 מפת הקבצים — מה חי ומה מת

**חלק מהבלאגן הוא שיש 20 קבצי פרומט ואי אפשר לדעת מי בתוקף.** זו המפה:

| קובץ | מצב |
|---|---|
| `cc_run_sheet_0827.md` | ✅ **הקובץ הזה. היחיד בתוקף** |
| `spec_first_screen.md` | ✅ איפיון שלב 2 |
| `cc_prompt_branch_reconcile.md` | ✅ **X0 — ראשון** |
| `cc_prompt_synonyms.md` | ✅ D4, מוכן |
| `cc_prompt_d3_reveal_error.md` | ✅ D3, מוכן |
| `cc_prompt_seed_coverage.md` | ✅ X2, מוכן, **לא רץ** |
| `cc_prompt_m1_enum_migration.md` | ✅ M1, מוכן |
| `cc_prompt_m2_service_tables.md` | ✅ M2, מוכן |
| `cc_prompt_m3_admin_categories.md` | ✅ M3, מוכן |
| `cc_prompt_m4_search_wiring.md` | ✅ M4, מוכן |
| `services_vertical_spec.md` | ✅ איפיון שלב 2 |
| `cc_prompt_sp2.md` | 🟠 **רק §4** פתוח |
| `cc_prompt_nm_copy.md` | 🟠 פתוח |
| `cc_prompt_whatsapp_meta_W1.md` | ⏸ מוכן, מוקפא |
| `cc_master_backlog.md` | 📚 **רקע בלבד. הסדר שלו לא בתוקף** |
| `cc_run_sheet.md` · `cc_run_sheet_0816.md` · `cc_workorder_0816.md` · `STATUS_0816.md` | ❌ **מוחלפים ע"י הקובץ הזה** |
| `cc_prompts_P1_wave2.md` | ⚠️ **פריט L1 סותר** — מורה על brand-800+לבן. **אל תריץ אותו** (B8) |
| `cc_prompt_round2_halffixes.md` · `return_to_reveal` · `rewriter_hebrew` · `near_match` · `search_sticky_prominence` | ✅ בוצעו |

> ### ⚠️ תיקון שאני חייב לעשות
> **כל הפרומטים שכתבתי אומרים "staging בלבד" — כולל אלה שנשלחו כשה-HEAD היה `pivot/v2`.**
> אחרי ש-X0 יחזיר את המיפוי, **אני עובר על כולם ומיישר בבת אחת.** עד אז — CC מאשר את הענף מול Yulian לפני קומיט.

---

# תרשים הסדר

```
X0  מיפוי ענפים ─────────►  X0b החלטת איחוד
                                  │
X1  mode=real (דקה, עליך) ────────┤
X2  seed_coverage         ────────┤
                                  ▼
              D1+D2 נוסח · D3 reveal · D4 נרדפות
                                  │
                                  ▼
     M1 מיגרציה רוחבית  →  M2 טבלאות  →  M3 אדמין קטגוריות
                                             │
                                             ▼
                              M3b אתה מזין 8–12 קטגוריות
                                             │
                                             ▼
                                    M4 חיבור לחיפוש 🔴
                                             │
                                             ▼
              F1 צ'יפים → F2 הקלדה → F3 "הבנתי" → F4 נוסח → F5 צילומים
                                             │
                                             ▼
                                       V1–V4 זמינות
                                             │
                                             ▼
                            ⏸ צד הספק · צד הקבלן · WhatsApp · Ads
```

**שני צווארי הבקבוק היחידים שתלויים בך:** `X1` (דקה) ו-`M3b` (הזנת הקטגוריות). כל השאר רץ אצל CC.
