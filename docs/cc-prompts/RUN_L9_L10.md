# הרצה — L9 ואז L10

> **12.09.2026.** קובץ הרצה אחד לשני הפריטים. הפירוט נמצא בשני קבצי ה-L; **כאן הסדר, התנאים וההעצרות.**

---

# §0 · תנאי פתיחה — לפני שאתה נוגע בכל דבר

## 0.1 · הצ'קאאוט

```bash
git rev-parse --abbrev-ref HEAD     # → pivot/v2
git log --oneline -1                # → L8: monitoring, CORS, CI  (a4ab09f) או מאוחר יותר
```

🔴 **אם אתה תחת `.claude/.claude/worktrees/` — עצור.** ראה `START_HERE.md`.

## 0.2 · 🔴 לאיזה DB אתה מחובר

`scripts/run_migrations.py` קורא `MYSQL_HOST` · `MYSQL_PORT` · `MYSQL_USER` · `MYSQL_ROOT_PASSWORD`, **וברירות המחדל שלו הן הדוקר המקומי** (`mysql:3306`).

> **זו הטעות היקרה של הפריט הזה:** אם תריץ בלי להצביע על Staging, **תריץ על ה-DB המקומי, תדווח הצלחה, וסטייג'ינג יישאר שבור.**

**המסלול המומלץ:**

```bash
railway link            # BuildUp → Staging
railway status          # ← הדבק את הפלט בדיווח
railway run python scripts/run_migrations.py
```

⚠️ **`railway status` אינו פורמליות.** בפרויקט הזה ה-CLI כבר נקשר בעבר ל-**Planwise** במקום ל-BuildUp. **מיגרציה על הפרויקט הלא-נכון היא נזק אמיתי ובלתי הפיך.**
**אם `railway status` לא מראה BuildUp / Staging — עצור ודווח. אל תריץ.**

**אם Yulian אמר שהוא מריץ את המיגרציות בעצמו** — דלג על ההרצה, **ובצע רק את שאילתות האימות** של L9 §1.

---

# §1 · L9 — מיגרציות

📄 **`docs/cc-prompts/cc_prompt_L9_migrations.md`** — §1, §2, §2b, §2c, §3

**קודם, כי סטייג'ינג שבור בלעדיו.** הקוד של L4 ו-L5 כבר נפרס שם וקורא עמודות שלא קיימות.

> ## 🔴 נקודת עצירה — אחת
> ב-**§2b**, אחרי שאילתת הספירה ולפני ה-`UPDATE`:
> **אם `looks_real > 0` — עצור ודווח ל-Yulian. אל תריץ backfill.**
> זה אומר שחיוב אמיתי כבר קרה, וזו שיחה אחרת לגמרי.
>
> **כל שאר הבדיקות נדחות ל-§3 של הקובץ הזה.**

`git tag pre-l9` → בצע → קומיט `L9: run 071+072, payment_events.is_fake, migration-order rule`

---

# §2 · L10 — תוכן משפטי בניהול

📄 **`docs/cc-prompts/cc_prompt_L10_legal_admin.md`** — §1 עד §5

**אל תתחיל לפני ש-L9 §1 עבר** — L10 יוצר מיגרציה נוספת, ושתי מיגרציות שרצות על סכימה לא-מסונכרנת מקשות על האבחון.

**שלוש נקודות שהכי קל לקצר בהן, ואסור:**

1. 🔴 **XSS.** אין ספריית Markdown בפרויקט (`package.json` נקי ממנה) — **אתה בוחר מההתחלה.** בחר אחת עם HTML מושבת כברירת מחדל (`markdown-it` עם `html: false`), **והוסף סניטציה מעליה.** שתי שכבות. **הבדיקה עם `<script>` ו-`<img onerror>` היא חובה, עם צילום.**
2. 🔴 **הדפים המשפטיים נשארים ציבוריים.** L2 סגר את `/api/search`; **הוא לא סוגר את `/terms`.** בדוק בחלון פרטי.
3. **`a11y_coordinator_name = יוליאן אברמוביץ׳`.** טלפון ומייל נשארים `NULL` — **המקטע פשוט לא מרונדר** עד שימולאו, ובאדמין מוצגת אזהרה. **אל תמציא פרטי קשר.**

`git tag pre-l10` → בצע → קומיט `L10: legal content in admin + site settings`

---

# §3 · הבדיקות — בסוף, בבת אחת

**אל תריץ את סעיפי ה-Acceptance של שני הקבצים תוך כדי.** הם מרוכזים כאן.

## 3.1 · שער — סטייג'ינג חי שוב

- [ ] **הדבק** את `SELECT entity_type, tier, monthly_price_nis, included_users, extra_user_price_nis, max_users FROM payment_db.subscription_plans ORDER BY entity_type, tier;` — **שש שורות, אף מחיר קבלן אינו `NULL`**
- [ ] `SHOW TABLES IN payment_db LIKE 'payment_events'` · שלוש עמודות החידוש ב-`subscriptions`
- [ ] `/contractor/users` נטען, מציג **`X מתוך 5`**. צילום
- [ ] הזמנה מתחת ל-5 → עוברת · הזמנה ב-5 → **`402 seat_upgrade_required` עם מחיר.** הדבק
- [ ] `/billing` נטען. צילום
- [ ] **לוג עליית `payment` מראה `mode=fake`.** הדבק את השורה

## 3.2 · `is_fake`

- [ ] **טבלת הספירה מ-§2b — לפני ואחרי ה-backfill.** שתיהן
- [ ] פתיחת מנוי → **שורה חדשה עם `is_fake = TRUE`.** הדבק
- [ ] שורות שגיאה (`provider_transaction_id IS NULL`) → **`is_fake = FALSE`**
- [ ] `grep -rn "INSERT INTO payment_events"` → **נקודת כתיבה אחת**

## 3.3 · הדפים המשפטיים

- [ ] שלושת הדפים נטענים **מה-DB**. צילום
- [ ] 🔴 **נטענים בחלון פרטי, בלי טוקן.** צילום
- [ ] מחיקת שורה מ-`legal_documents` → **הדף עולה מהקובץ.** צילום. החזר אחרי
- [ ] עריכה באדמין → **הדף הציבורי משתנה בלי פריסה.** לפני/אחרי
- [ ] `version` עלה ב-1 · נוצרה שורת history. הדבק
- [ ] `is_draft = FALSE` → הערת הטיוטה נעלמת. צילום
- [ ] 🔴 **XSS: `<script>alert(1)</script>` וגם `<img src=x onerror=alert(1)>` → לא רץ כלום.** צילום הדף **והקונסולה**
- [ ] פרטיות מזכירה בשמם: **Anthropic · ElevenLabs · Cardcom · Vonage · data.gov.il**
- [ ] פרטיות מתייחסת ל-`contact_reveals`
- [ ] בלי טלפון/מייל → **מקטע הרכז לא מרונדר** + אזהרה באדמין. שני צילומים
- [ ] לא-אדמין → `POST /api/admin/legal/...` → **403.** הדבק
- [ ] `SELECT * FROM site_settings` — **שבע שורות.** הדבק

## 3.4 · רגרסיה

- [ ] חיפוש · חשיפה · היסטוריית חשיפות · תגי אמון · מודעות מפרסם
- [ ] מסלול ההתחברות מ-H11 מקצה לקצה
- [ ] 390 + דסקטופ · RTL
- [ ] `npm run build` · `npm test`

---

# §4 · דיווח — פעם אחת

1. **פלט `railway status`** — או "Yulian הריץ בעצמו"
2. שש שורות `subscription_plans`
3. **טבלאות הספירה של `payment_events`, לפני ואחרי**
4. **צילום בדיקת ה-XSS** — הדף והקונסולה
5. **איזו ספריית Markdown נבחרה ואיך הוגדרה** — ציטוט הקונפיגורציה
6. הרשימה הסופית של הצדדים השלישיים, **ואם מצאת ספק שישי**
7. `SELECT * FROM site_settings`
8. **ל-Yulian:** חסרים טלפון ומייל לרכז הנגישות
9. `git diff -w --stat` מ-`pre-l9` ועד HEAD

---

# §5 · Guardrails

🔴 **`railway status` לפני כל מיגרציה.** פרויקט לא-נכון = נזק בלתי הפיך.

🔴 **production — לא. בשום שלב.**

🔴 **`looks_real > 0` ⇒ עצור.**

🔴 **XSS — שתי שכבות ובדיקה מוכחת.** תוכן מהאדמין מרונדר לכל מבקר.

🔴 **הדפים המשפטיים ציבוריים.**

🔴 **אל תמציא ח.פ, כתובת או טלפון.** `NULL` ודיווח.

🔴 **אל תערוך את `071` או `072`** — הן בקומיט. תיקון = מיגרציה חדשה.

**אין `git add -A`** · **`git tag` לפני כל שלב** · **אל תיגע** ב-L1–L8 · `charge_token` · `query_rewriter.py` · דירוג התוצאות.
